# git CLI schema for fast-completer
# Generated from git 2.54.0

git # Distributed version control
	add # Add file contents to the index
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--interactive|-i @bool # interactive picking
		--no-interactive @bool # opposite of --interactive
		--patch|-p @bool # select hunks interactively
		--no-patch @bool # opposite of --patch
		--auto-advance @bool # auto advance to the next file when selecting hunks interactively
		--no-auto-advance @bool # opposite of --auto-advance
		--unified|-U # generate diffs with <n> lines context
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--edit|-e @bool # edit current diff and apply
		--no-edit @bool # opposite of --edit
		--force|-f @bool # allow adding otherwise ignored files
		--no-force @bool # opposite of --force
		--update|-u @bool # update tracked files
		--no-update @bool # opposite of --update
		--renormalize @bool # renormalize EOL of tracked files (implies -u)
		--no-renormalize @bool # opposite of --renormalize
		--intent-to-add|-N @bool # record only the fact that the path will be added later
		--no-intent-to-add @bool # opposite of --intent-to-add
		--all|-A @bool # add changes from all tracked and untracked files
		--no-all @bool # opposite of --all
		--refresh @bool # don't add, only refresh the index
		--no-refresh @bool # opposite of --refresh
		--ignore-errors @bool # just skip files which cannot be added because of errors
		--no-ignore-errors @bool # opposite of --ignore-errors
		--sparse @bool # allow updating entries outside of the sparse-checkout cone
		--no-sparse @bool # opposite of --sparse
		--chmod # override the executable bit of the listed files
		--no-chmod @bool # opposite of --chmod
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	am # Apply a series of patches from a mailbox
		--interactive|-i @bool # run interactively
		--no-interactive @bool # opposite of --interactive
		--no-verify|-n @bool # bypass pre-applypatch and applypatch-msg hooks
		--verify @bool # opposite of --no-verify
		--3way|-3 @bool # allow fall back on 3way merging if needed
		--no-3way @bool # opposite of --3way
		--quiet|-q @bool # be quiet
		--no-quiet @bool # opposite of --quiet
		--signoff|-s @bool # add a Signed-off-by trailer to the commit message
		--no-signoff @bool # opposite of --signoff
		--utf8|-u @bool # recode into utf8 (default)
		--no-utf8 @bool # opposite of --utf8
		--keep|-k @bool # pass -k flag to git-mailinfo
		--no-keep @bool # opposite of --keep
		--keep-cr @bool # pass --keep-cr flag to git-mailsplit for mbox format
		--no-keep-cr @bool # opposite of --keep-cr
		--scissors|-c @bool # strip everything before a scissors line
		--no-scissors @bool # opposite of --scissors
		--quoted-cr # pass it through git-mailinfo
		--whitespace # pass it through git-apply
		--no-whitespace @bool # opposite of --whitespace
		--ignore-space-change @bool # pass it through git-apply
		--no-ignore-space-change @bool # opposite of --ignore-space-change
		--ignore-whitespace @bool # pass it through git-apply
		--no-ignore-whitespace @bool # opposite of --ignore-whitespace
		--directory # pass it through git-apply
		--no-directory @bool # opposite of --directory
		--patch-format # format the patch(es) are in
		--no-patch-format @bool # opposite of --patch-format
		--reject @bool # pass it through git-apply
		--no-reject @bool # opposite of --reject
		--continue @bool # continue applying patches after resolving a conflict
		--resolved|-r @bool # synonyms for --continue
		--skip @bool # skip the current patch
		--abort @bool # restore the original branch and abort the patching operation
		--quit @bool # abort the patching operation but keep HEAD where it is
		--show-current-patch # show the patch being applied
		--retry @bool # try to apply current patch again
		--allow-empty @bool # record the empty patch as an empty commit
		--committer-date-is-author-date @bool # lie about committer date
		--no-committer-date-is-author-date @bool # opposite of --committer-date-is-author-date
		--ignore-date @bool # use current timestamp for author date
		--no-ignore-date @bool # opposite of --ignore-date
		--rerere-autoupdate @bool # update the index with reused conflict resolution if possible
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
		--gpg-sign|-S # GPG-sign commits
		--no-gpg-sign @bool # opposite of --gpg-sign
		--empty # how to handle empty patches
	annotate # Annotate file lines with commit information
		--incremental @bool # show blame entries as we find them, incrementally
		--no-incremental @bool # opposite of --incremental
		--root @bool # do not treat root commits as boundaries (Default: off)
		--no-root @bool # opposite of --root
		--show-stats @bool # show work cost statistics
		--no-show-stats @bool # opposite of --show-stats
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--score-debug @bool # show output score for blame entries
		--no-score-debug @bool # opposite of --score-debug
		--show-name|-f @bool # show original filename (Default: auto)
		--no-show-name @bool # opposite of --show-name
		--show-number|-n @bool # show original linenumber (Default: off)
		--no-show-number @bool # opposite of --show-number
		--porcelain|-p @bool # show in a format designed for machine consumption
		--no-porcelain @bool # opposite of --porcelain
		--diff-algorithm # choose a diff algorithm
		--ignore-rev # ignore <rev> when blaming
		--no-ignore-rev @bool # opposite of --ignore-rev
		--ignore-revs-file # ignore revisions from <file>
		--no-ignore-revs-file @bool # opposite of --ignore-revs-file
		--color-lines @bool # color redundant metadata from previous line differently
		--no-color-lines @bool # opposite of --color-lines
		--color-by-age @bool # color lines by age
		--no-color-by-age @bool # opposite of --color-by-age
		--contents # use <file>'s contents as the final image
		--no-contents @bool # opposite of --contents
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
	apply # Apply a patch to files and/or to the index
		--exclude # don't apply changes matching the given path
		--include # apply changes matching the given path
		--no-add @bool # ignore additions made by the patch
		--add @bool # opposite of --no-add
		--stat @bool # instead of applying the patch, output diffstat for the input
		--no-stat @bool # opposite of --stat
		--numstat @bool # show number of added and deleted lines in decimal notation
		--no-numstat @bool # opposite of --numstat
		--summary @bool # instead of applying the patch, output a summary for the input
		--no-summary @bool # opposite of --summary
		--check @bool # instead of applying the patch, see if the patch is applicable
		--no-check @bool # opposite of --check
		--index @bool # make sure the patch is applicable to the current index
		--no-index @bool # opposite of --index
		--intent-to-add|-N @bool # mark new files with `git add --intent-to-add`
		--no-intent-to-add @bool # opposite of --intent-to-add
		--cached @bool # apply a patch without touching the working tree
		--no-cached @bool # opposite of --cached
		--unsafe-paths @bool # accept a patch that touches outside the working area
		--no-unsafe-paths @bool # opposite of --unsafe-paths
		--apply @bool # also apply the patch (use with --stat/--summary/--check)
		--no-apply @bool # opposite of --apply
		--3way|-3 @bool # attempt three-way merge, fall back on normal patch if that fails
		--no-3way @bool # opposite of --3way
		--ours @bool # for conflicts, use our version
		--theirs @bool # for conflicts, use their version
		--union @bool # for conflicts, use a union version
		--build-fake-ancestor # build a temporary index based on embedded index information
		--no-build-fake-ancestor @bool # opposite of --build-fake-ancestor
		--whitespace # detect new or modified lines that have whitespace errors
		--no-whitespace @bool # opposite of --whitespace
		--ignore-space-change @bool # ignore changes in whitespace when finding context
		--no-ignore-space-change @bool # opposite of --ignore-space-change
		--ignore-whitespace @bool # ignore changes in whitespace when finding context
		--no-ignore-whitespace @bool # opposite of --ignore-whitespace
		--reverse|-R @bool # apply the patch in reverse
		--no-reverse @bool # opposite of --reverse
		--unidiff-zero @bool # don't expect at least one line of context
		--no-unidiff-zero @bool # opposite of --unidiff-zero
		--reject @bool # leave the rejected hunks in corresponding *.rej files
		--no-reject @bool # opposite of --reject
		--allow-overlap @bool # allow overlapping hunks
		--no-allow-overlap @bool # opposite of --allow-overlap
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--recount @bool # do not trust the line counts in the hunk headers
		--no-recount @bool # opposite of --recount
		--directory # prepend <root> to all filenames
		--no-directory @bool # opposite of --directory
		--allow-empty @bool # don't return error for empty patches
		--no-allow-empty @bool # opposite of --allow-empty
	archimport # Import a GNU Arch repository into Git
	archive # Create an archive of files from a named tree
		--format # archive format
		--no-format @bool # opposite of --format
		--prefix # prepend prefix to each pathname in the archive
		--no-prefix @bool # opposite of --prefix
		--add-file # add untracked file to archive
		--no-add-file @bool # opposite of --add-file
		--add-virtual-file # add untracked file to archive
		--no-add-virtual-file @bool # opposite of --add-virtual-file
		--output|-o # write the archive to this file
		--no-output @bool # opposite of --output
		--worktree-attributes @bool # read .gitattributes in working directory
		--no-worktree-attributes @bool # opposite of --worktree-attributes
		--verbose|-v @bool # report archived files on stderr
		--no-verbose @bool # opposite of --verbose
		--mtime # set modification time of archive entries
		--list|-l @bool # list supported archive formats
		--no-list @bool # opposite of --list
		--remote # retrieve the archive from remote repository <repo>
		--no-remote @bool # opposite of --remote
	attributes # Defining attributes per path
	backfill # Download missing objects in a partial clone
		--min-batch-size # Minimum number of objects to request at a time
		--sparse @bool # Restrict the missing objects to the current sparse-checkout
		--no-sparse @bool # opposite of --sparse
	bisect # Use binary search to find the commit that introduced a bug
	blame # Show what revision and author last modified each line of a file
		--incremental @bool # show blame entries as we find them, incrementally
		--no-incremental @bool # opposite of --incremental
		--root @bool # do not treat root commits as boundaries (Default: off)
		--no-root @bool # opposite of --root
		--show-stats @bool # show work cost statistics
		--no-show-stats @bool # opposite of --show-stats
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--score-debug @bool # show output score for blame entries
		--no-score-debug @bool # opposite of --score-debug
		--show-name|-f @bool # show original filename (Default: auto)
		--no-show-name @bool # opposite of --show-name
		--show-number|-n @bool # show original linenumber (Default: off)
		--no-show-number @bool # opposite of --show-number
		--porcelain|-p @bool # show in a format designed for machine consumption
		--no-porcelain @bool # opposite of --porcelain
		--diff-algorithm # choose a diff algorithm
		--ignore-rev # ignore <rev> when blaming
		--no-ignore-rev @bool # opposite of --ignore-rev
		--ignore-revs-file # ignore revisions from <file>
		--no-ignore-revs-file @bool # opposite of --ignore-revs-file
		--color-lines @bool # color redundant metadata from previous line differently
		--no-color-lines @bool # opposite of --color-lines
		--color-by-age @bool # color lines by age
		--no-color-by-age @bool # opposite of --color-by-age
		--contents # use <file>'s contents as the final image
		--no-contents @bool # opposite of --contents
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
	branch # List, create, or delete branches
		--verbose|-v @bool # show hash and subject, give twice for upstream branch
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # suppress informational messages
		--no-quiet @bool # opposite of --quiet
		--track|-t # set branch tracking configuration
		--no-track @bool # opposite of --track
		--set-upstream-to|-u # change the upstream info
		--no-set-upstream-to @bool # opposite of --set-upstream-to
		--remotes|-r @bool # act on remote-tracking branches
		--contains # print only branches that contain the commit
		--no-contains # print only branches that don't contain the commit
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--all|-a @bool # list both remote-tracking and local branches
		--delete|-d @bool # delete fully merged branch
		--no-delete @bool # opposite of --delete
		--move|-m @bool # move/rename a branch and its reflog
		--no-move @bool # opposite of --move
		--omit-empty @bool # do not output a newline after empty formatted refs
		--no-omit-empty @bool # opposite of --omit-empty
		--copy|-c @bool # copy a branch and its reflog
		--no-copy @bool # opposite of --copy
		--list|-l @bool # list branch names
		--no-list @bool # opposite of --list
		--show-current @bool # show current branch name
		--no-show-current @bool # opposite of --show-current
		--create-reflog @bool # create the branch's reflog
		--no-create-reflog @bool # opposite of --create-reflog
		--edit-description @bool # edit the description for the branch
		--no-edit-description @bool # opposite of --edit-description
		--force|-f @bool # force creation, move/rename, deletion
		--no-force @bool # opposite of --force
		--merged # print only branches that are merged
		--no-merged # print only branches that are not merged
		--column # list branches in columns
		--no-column @bool # opposite of --column
		--sort # field name to sort on
		--no-sort @bool # opposite of --sort
		--points-at # print only branches of the object
		--no-points-at @bool # opposite of --points-at
		--ignore-case|-i @bool # sorting and filtering are case insensitive
		--no-ignore-case @bool # opposite of --ignore-case
		--recurse-submodules @bool # recurse through submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--format # format to use for the output
		--no-format @bool # opposite of --format
	bugreport # Collect information for user to file a bug report
		--diagnose # create an additional zip archive of detailed diagnostics (default 'stats')
		--no-diagnose @bool # opposite of --diagnose
		--output-directory|-o # specify a destination for the bugreport file(s)
		--no-output-directory @bool # opposite of --output-directory
		--suffix|-s # specify a strftime format suffix for the filename(s)
		--no-suffix @bool # opposite of --suffix
	bundle # Move objects and refs by archive
	cat-file # Provide contents or details of repository objects
		--use-mailmap @bool # use mail map file
		--no-use-mailmap @bool # opposite of --use-mailmap
		--mailmap @bool # alias of --use-mailmap
		--no-mailmap @bool # opposite of --mailmap
		--batch # show full <object> or <rev> contents
		--batch-check # like --batch, but don't emit <contents>
		--batch-command # read commands from stdin
		--batch-all-objects @bool # with --batch[-check]: ignores stdin, batches all known objects
		--buffer @bool # buffer --batch output
		--no-buffer @bool # opposite of --buffer
		--follow-symlinks @bool # follow in-tree symlinks
		--no-follow-symlinks @bool # opposite of --follow-symlinks
		--unordered @bool # do not order objects before emitting them
		--no-unordered @bool # opposite of --unordered
		--textconv @bool # run textconv on object's content
		--filters @bool # run filters on object's content
		--filter # object filtering
		--no-filter @bool # opposite of --filter
	check-attr # Display gitattributes information
		--all|-a @bool # report all attributes set on file
		--no-all @bool # opposite of --all
		--cached @bool # use .gitattributes only from the index
		--no-cached @bool # opposite of --cached
		--stdin @bool # read file names from stdin
		--no-stdin @bool # opposite of --stdin
		--source # which tree-ish to check attributes at
		--no-source @bool # opposite of --source
	check-ignore # Debug gitignore / exclude files
		--quiet|-q @bool # suppress progress reporting
		--no-quiet @bool # opposite of --quiet
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--stdin @bool # read file names from stdin
		--no-stdin @bool # opposite of --stdin
		--non-matching|-n @bool # show non-matching input paths
		--no-non-matching @bool # opposite of --non-matching
		--no-index @bool # ignore index when checking
		--index @bool # opposite of --no-index
	check-mailmap # Show canonical names and email addresses of contacts
		--stdin @bool # also read contacts from stdin
		--no-stdin @bool # opposite of --stdin
		--mailmap-file # read additional mailmap entries from file
		--no-mailmap-file @bool # opposite of --mailmap-file
		--mailmap-blob # read additional mailmap entries from blob
		--no-mailmap-blob @bool # opposite of --mailmap-blob
	check-ref-format # Ensures that a reference name is well formed
	checkout # Switch branches or restore working tree files
		--guess @bool # second guess 'git checkout <no-such-branch>' (default)
		--no-guess @bool # opposite of --guess
		--overlay @bool # use overlay mode (default)
		--no-overlay @bool # opposite of --overlay
		--auto-advance @bool # auto advance to the next file when selecting hunks interactively
		--no-auto-advance @bool # opposite of --auto-advance
		--quiet|-q @bool # suppress progress reporting
		--no-quiet @bool # opposite of --quiet
		--recurse-submodules # control recursive updating of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--merge|-m @bool # perform a 3-way merge with the new branch
		--no-merge @bool # opposite of --merge
		--conflict # conflict style (merge, diff3, or zdiff3)
		--no-conflict @bool # opposite of --conflict
		--detach|-d @bool # detach HEAD at named commit
		--no-detach @bool # opposite of --detach
		--track|-t # set branch tracking configuration
		--no-track @bool # opposite of --track
		--force|-f @bool # force checkout (throw away local modifications)
		--no-force @bool # opposite of --force
		--orphan # new unborn branch
		--no-orphan @bool # opposite of --orphan
		--overwrite-ignore @bool # update ignored files (default)
		--no-overwrite-ignore @bool # opposite of --overwrite-ignore
		--ignore-other-worktrees @bool # do not check if another worktree is using this branch
		--no-ignore-other-worktrees @bool # opposite of --ignore-other-worktrees
		--ours|-2 @bool # checkout our version for unmerged files
		--theirs|-3 @bool # checkout their version for unmerged files
		--patch|-p @bool # select hunks interactively
		--no-patch @bool # opposite of --patch
		--unified|-U # generate diffs with <n> lines context
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--ignore-skip-worktree-bits @bool # do not limit pathspecs to sparse entries only
		--no-ignore-skip-worktree-bits @bool # opposite of --ignore-skip-worktree-bits
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	checkout-index # Copy files from the index to the working tree
		--all|-a @bool # check out all files in the index
		--no-all @bool # opposite of --all
		--ignore-skip-worktree-bits @bool # do not skip files with skip-worktree set
		--no-ignore-skip-worktree-bits @bool # opposite of --ignore-skip-worktree-bits
		--force|-f @bool # force overwrite of existing files
		--no-force @bool # opposite of --force
		--quiet|-q @bool # no warning for existing files and files not in index
		--no-quiet @bool # opposite of --quiet
		--no-create|-n @bool # don't checkout new files
		--create @bool # opposite of --no-create
		--index|-u @bool # update stat information in the index file
		--no-index @bool # opposite of --index
		--stdin @bool # read list of paths from the standard input
		--no-stdin @bool # opposite of --stdin
		--temp @bool # write the content to temporary files
		--no-temp @bool # opposite of --temp
		--prefix # when creating files, prepend <string>
		--no-prefix @bool # opposite of --prefix
		--stage # copy out the files from named stage
	cherry # Find commits yet to be applied to upstream
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
	cherry-pick # Apply the changes introduced by some existing commits
		--quit @bool # end revert or cherry-pick sequence
		--continue @bool # resume revert or cherry-pick sequence
		--abort @bool # cancel revert or cherry-pick sequence
		--skip @bool # skip current commit and continue
		--no-commit|-n @bool # don't automatically commit
		--commit @bool # opposite of --no-commit
		--edit|-e @bool # edit the commit message
		--no-edit @bool # opposite of --edit
		--signoff|-s @bool # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--mainline|-m # select mainline parent
		--no-mainline @bool # opposite of --mainline
		--rerere-autoupdate @bool # update the index with reused conflict resolution if possible
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
		--strategy # merge strategy
		--no-strategy @bool # opposite of --strategy
		--strategy-option|-X # option for merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
		--ff @bool # allow fast-forward
		--no-ff @bool # opposite of --ff
		--allow-empty @bool # preserve initially empty commits
		--no-allow-empty @bool # opposite of --allow-empty
		--allow-empty-message @bool # allow commits with empty messages
		--no-allow-empty-message @bool # opposite of --allow-empty-message
		--keep-redundant-commits @bool # deprecated: use --empty=keep instead
		--no-keep-redundant-commits @bool # opposite of --keep-redundant-commits
		--empty # how to handle commits that become empty
	citool # Graphical alternative to git-commit
	clean # Remove untracked files from the working tree
		--quiet|-q @bool # do not print names of files removed
		--no-quiet @bool # opposite of --quiet
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--force|-f @bool # force
		--no-force @bool # opposite of --force
		--interactive|-i @bool # interactive cleaning
		--no-interactive @bool # opposite of --interactive
		--exclude|-e # add <pattern> to ignore rules
	cli # Git command-line interface and conventions
	clone # Clone a repository into a new directory
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--no-checkout|-n @bool # don't create a checkout
		--checkout @bool # opposite of --no-checkout
		--bare @bool # create a bare repository
		--no-bare @bool # opposite of --bare
		--mirror @bool # create a mirror repository (implies --bare)
		--no-mirror @bool # opposite of --mirror
		--local|-l @bool # to clone from a local repository
		--no-local @bool # opposite of --local
		--no-hardlinks @bool # don't use local hardlinks, always copy
		--hardlinks @bool # opposite of --no-hardlinks
		--shared|-s @bool # setup as shared repository
		--no-shared @bool # opposite of --shared
		--recurse-submodules # initialize submodules in the clone
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--recursive # alias of --recurse-submodules
		--no-recursive @bool # opposite of --recursive
		--jobs|-j # number of submodules cloned in parallel
		--no-jobs @bool # opposite of --jobs
		--template # directory from which templates will be used
		--no-template @bool # opposite of --template
		--reference # reference repository
		--no-reference @bool # opposite of --reference
		--reference-if-able # reference repository
		--no-reference-if-able @bool # opposite of --reference-if-able
		--dissociate @bool # use --reference only while cloning
		--no-dissociate @bool # opposite of --dissociate
		--origin|-o # use <name> instead of 'origin' to track upstream
		--no-origin @bool # opposite of --origin
		--branch|-b # checkout <branch> instead of the remote's HEAD
		--no-branch @bool # opposite of --branch
		--upload-pack|-u # path to git-upload-pack on the remote
		--no-upload-pack @bool # opposite of --upload-pack
		--depth # create a shallow clone of that depth
		--no-depth @bool # opposite of --depth
		--shallow-since # create a shallow clone since a specific time
		--no-shallow-since @bool # opposite of --shallow-since
		--shallow-exclude # deepen history of shallow clone, excluding ref
		--no-shallow-exclude @bool # opposite of --shallow-exclude
		--single-branch @bool # clone only one branch, HEAD or --branch
		--no-single-branch @bool # opposite of --single-branch
		--tags @bool # clone tags, and make later fetches not to follow them
		--no-tags @bool # opposite of --tags
		--shallow-submodules @bool # any cloned submodules will be shallow
		--no-shallow-submodules @bool # opposite of --shallow-submodules
		--separate-git-dir # separate git dir from working tree
		--no-separate-git-dir @bool # opposite of --separate-git-dir
		--ref-format # specify the reference format to use
		--no-ref-format @bool # opposite of --ref-format
		--config|-c # set config inside the new repository
		--no-config @bool # opposite of --config
		--server-option # option to transmit
		--no-server-option @bool # opposite of --server-option
		--ipv4|-4 @bool # use IPv4 addresses only
		--ipv6|-6 @bool # use IPv6 addresses only
		--filter # object filtering
		--no-filter @bool # opposite of --filter
		--also-filter-submodules @bool # apply partial clone filters to submodules
		--no-also-filter-submodules @bool # opposite of --also-filter-submodules
		--remote-submodules @bool # any cloned submodules will use their remote-tracking branch
		--no-remote-submodules @bool # opposite of --remote-submodules
		--sparse @bool # initialize sparse-checkout file to include only files at root
		--no-sparse @bool # opposite of --sparse
		--bundle-uri # a URI for downloading bundles before fetching from origin remote
		--no-bundle-uri @bool # opposite of --bundle-uri
	column # Display data in columns
		--raw-mode # layout to use
		--width # maximum width
		--no-width @bool # opposite of --width
		--indent # padding space on left border
		--no-indent @bool # opposite of --indent
		--nl # padding space on right border
		--no-nl @bool # opposite of --nl
		--padding # padding space between columns
		--no-padding @bool # opposite of --padding
	commit # Record changes to the repository
		--quiet|-q @bool # suppress summary after successful commit
		--no-quiet @bool # opposite of --quiet
		--verbose|-v @bool # show diff in commit message template
		--no-verbose @bool # opposite of --verbose
		--file|-F # read message from file
		--no-file @bool # opposite of --file
		--author # override author for commit
		--no-author @bool # opposite of --author
		--date # override date for commit
		--no-date @bool # opposite of --date
		--message|-m # commit message
		--no-message @bool # opposite of --message
		--reedit-message|-c # reuse and edit message from specified commit
		--no-reedit-message @bool # opposite of --reedit-message
		--reuse-message|-C # reuse message from specified commit
		--no-reuse-message @bool # opposite of --reuse-message
		--squash # use autosquash formatted message to squash specified commit
		--no-squash @bool # opposite of --squash
		--reset-author @bool # the commit is authored by me now (used with -C/-c/--amend)
		--no-reset-author @bool # opposite of --reset-author
		--trailer # add custom trailer(s)
		--no-trailer @bool # opposite of --trailer
		--signoff|-s @bool # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--template|-t # use specified template file
		--no-template @bool # opposite of --template
		--edit|-e @bool # force edit of commit
		--no-edit @bool # opposite of --edit
		--status @bool # include status in commit message template
		--no-status @bool # opposite of --status
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
		--all|-a @bool # commit all changed files
		--no-all @bool # opposite of --all
		--include|-i @bool # add specified files to index for commit
		--no-include @bool # opposite of --include
		--interactive @bool # interactively add files
		--no-interactive @bool # opposite of --interactive
		--patch|-p @bool # interactively add changes
		--no-patch @bool # opposite of --patch
		--unified|-U # generate diffs with <n> lines context
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--only|-o @bool # commit only specified files
		--no-only @bool # opposite of --only
		--no-verify|-n @bool # bypass pre-commit and commit-msg hooks
		--verify @bool # opposite of --no-verify
		--dry-run @bool # show what would be committed
		--no-dry-run @bool # opposite of --dry-run
		--short @bool # show status concisely
		--no-short @bool # opposite of --short
		--branch @bool # show branch information
		--no-branch @bool # opposite of --branch
		--ahead-behind @bool # compute full ahead/behind values
		--no-ahead-behind @bool # opposite of --ahead-behind
		--porcelain @bool # machine-readable output
		--no-porcelain @bool # opposite of --porcelain
		--long @bool # show status in long format (default)
		--no-long @bool # opposite of --long
		--null|-z @bool # terminate entries with NUL
		--no-null @bool # opposite of --null
		--amend @bool # amend previous commit
		--no-amend @bool # opposite of --amend
		--no-post-rewrite @bool # bypass post-rewrite hook
		--post-rewrite @bool # opposite of --no-post-rewrite
		--untracked-files|-u # show untracked files, optional modes: all, normal, no. (Default: all)
		--no-untracked-files @bool # opposite of --untracked-files
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	commit-graph # Write and verify Git commit-graph files
		--object-dir # the object directory to store the graph
		--no-object-dir @bool # opposite of --object-dir
	commit-tree # Create a new commit object
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
	config # Get and set repository or global options
	count-objects # Count unpacked number of objects and their disk consumption
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--human-readable|-H @bool # print sizes in human readable format
		--no-human-readable @bool # opposite of --human-readable
	credential # Retrieve and store user credentials
	credential-cache # Helper to temporarily store passwords in memory
		--timeout # number of seconds to cache credentials
		--no-timeout @bool # opposite of --timeout
		--socket # path of cache-daemon socket
		--no-socket @bool # opposite of --socket
	credential-store # Helper to store credentials on disk
		--file # fetch and store credentials in <path>
		--no-file @bool # opposite of --file
	cvsexportcommit # Export a single commit to a CVS checkout
	cvsimport # Salvage your data out of another SCM people love to hate
	cvsserver # A CVS server emulator for Git
	daemon # A really simple server for Git repositories
	describe # Give an object a human readable name based on an available ref
		--contains @bool # find the tag that comes after the commit
		--no-contains @bool # opposite of --contains
		--debug @bool # debug search strategy on stderr
		--no-debug @bool # opposite of --debug
		--all @bool # use any ref
		--no-all @bool # opposite of --all
		--tags @bool # use any tag, even unannotated
		--no-tags @bool # opposite of --tags
		--long @bool # always use long format
		--no-long @bool # opposite of --long
		--first-parent @bool # only follow first parent
		--no-first-parent @bool # opposite of --first-parent
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--exact-match @bool # only output exact matches
		--no-exact-match @bool # opposite of --exact-match
		--match # only consider tags matching <pattern>
		--no-match @bool # opposite of --match
		--exclude # do not consider tags matching <pattern>
		--no-exclude @bool # opposite of --exclude
		--always @bool # show abbreviated commit object as fallback
		--no-always @bool # opposite of --always
		--broken # append <mark> on broken working tree (default: "-broken")
		--no-broken @bool # opposite of --broken
	diagnose # Generate a zip archive of diagnostic information
		--output-directory|-o # specify a destination for the diagnostics archive
		--no-output-directory @bool # opposite of --output-directory
		--suffix|-s # specify a strftime format suffix for the filename
		--no-suffix @bool # opposite of --suffix
		--mode # specify the content of the diagnostic archive
	diff # Show changes between commits, commit and working tree, etc
		--patch-with-raw @bool # output both a patch and the diff-raw format.
		--stat @bool # show diffstat instead of patch.
		--numstat @bool # show numeric diffstat instead of patch.
		--patch-with-stat @bool # output a patch and prepend its diffstat.
		--name-only @bool # show only names of changed files.
		--full-index @bool # show full object name on index lines.
		--abbrev # abbreviate object names in diff-tree header and diff-raw.
		--find-copies-harder @bool # try unchanged files as candidate for copy detection.
		--pickaxe-all @bool # show all files diff when -S is used and hit is found.
	diff-files # Compares files in the working tree and the index
		--patch-with-raw @bool # output both a patch and the diff-raw format.
		--stat @bool # show diffstat instead of patch.
		--numstat @bool # show numeric diffstat instead of patch.
		--patch-with-stat @bool # output a patch and prepend its diffstat.
		--name-only @bool # show only names of changed files.
		--full-index @bool # show full object name on index lines.
		--abbrev # abbreviate object names in diff-tree header and diff-raw.
		--find-copies-harder @bool # try unchanged files as candidate for copy detection.
		--pickaxe-all @bool # show all files diff when -S is used and hit is found.
	diff-index # Compare a tree to the working tree or index
		--patch-with-raw @bool # output both a patch and the diff-raw format.
		--stat @bool # show diffstat instead of patch.
		--numstat @bool # show numeric diffstat instead of patch.
		--patch-with-stat @bool # output a patch and prepend its diffstat.
		--name-only @bool # show only names of changed files.
		--full-index @bool # show full object name on index lines.
		--abbrev # abbreviate object names in diff-tree header and diff-raw.
		--find-copies-harder @bool # try unchanged files as candidate for copy detection.
		--pickaxe-all @bool # show all files diff when -S is used and hit is found.
	diff-pairs # Compare the content and mode of provided blob pairs
		--patch|-p @bool # generate patch
		--no-patch|-s @bool # suppress diff output
		--unified|-U # generate diffs with <n> lines context
		--function-context|-W @bool # generate diffs with <n> lines context
		--no-function-context @bool # opposite of --function-context
		--raw @bool # generate the diff in raw format
		--patch-with-raw @bool # synonym for '-p --raw'
		--patch-with-stat @bool # synonym for '-p --stat'
		--numstat @bool # machine friendly --stat
		--shortstat @bool # output only the last line of --stat
		--cumulative @bool # synonym for --dirstat=cumulative
		--check @bool # warn if changes introduce conflict markers or whitespace errors
		--summary @bool # condensed summary such as creations, renames and mode changes
		--name-only @bool # show only names of changed files
		--name-status @bool # show only names and status of changed files
		--stat-width # generate diffstat with a given width
		--stat-name-width # generate diffstat with a given name width
		--stat-graph-width # generate diffstat with a given graph width
		--stat-count # generate diffstat with limited lines
		--compact-summary @bool # generate compact summary in diffstat
		--no-compact-summary @bool # opposite of --compact-summary
		--binary @bool # output a binary diff that can be applied
		--full-index @bool # show full pre- and post-image object names on the "index" lines
		--no-full-index @bool # opposite of --full-index
		--ws-error-highlight # highlight whitespace errors in the 'context', 'old' or 'new' lines in the diff
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--line-prefix # prepend an additional prefix to every line of output
		--no-prefix @bool # do not show any source or destination prefix
		--default-prefix @bool # use default prefixes a/ and b/
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--output-indicator-new # specify the character to indicate a new line instead of '+'
		--output-indicator-old # specify the character to indicate an old line instead of '-'
		--output-indicator-context # specify the character to indicate a context instead of ' '
		--find-renames|-M # detect renames
		--irreversible-delete|-D @bool # omit the preimage for deletes
		--find-copies|-C # detect copies
		--find-copies-harder @bool # use unmodified files as source to find copies
		--no-find-copies-harder @bool # opposite of --find-copies-harder
		--no-renames @bool # disable rename detection
		--rename-empty @bool # use empty blobs as rename source
		--no-rename-empty @bool # opposite of --rename-empty
		--follow @bool # continue listing the history of a file beyond renames
		--no-follow @bool # opposite of --follow
		--minimal @bool # produce the smallest possible diff
		--ignore-all-space|-w @bool # ignore whitespace when comparing lines
		--ignore-space-change|-b @bool # ignore changes in amount of whitespace
		--ignore-cr-at-eol @bool # ignore carrier-return at the end of line
		--ignore-blank-lines @bool # ignore changes whose lines are all blank
		--ignore-matching-lines|-I # ignore changes whose all lines match <regex>
		--no-ignore-matching-lines @bool # opposite of --ignore-matching-lines
		--indent-heuristic @bool # heuristic to shift diff hunk boundaries for easy reading
		--no-indent-heuristic @bool # opposite of --indent-heuristic
		--patience @bool # generate diff using the "patience diff" algorithm
		--histogram @bool # generate diff using the "histogram diff" algorithm
		--diff-algorithm # choose a diff algorithm
		--anchored # generate diff using the "anchored diff" algorithm
		--word-diff # show word diff, using <mode> to delimit changed words
		--word-diff-regex # use <regex> to decide what a word is
		--color-words # equivalent to --word-diff=color --word-diff-regex=<regex>
		--color-moved # moved lines of code are colored differently
		--no-color-moved @bool # opposite of --color-moved
		--color-moved-ws # how white spaces are ignored in --color-moved
		--no-color-moved-ws @bool # opposite of --color-moved-ws
		--relative # when run from subdir, exclude changes outside and show relative paths
		--no-relative @bool # opposite of --relative
		--text|-a @bool # treat all files as text
		--no-text @bool # opposite of --text
		--exit-code @bool # exit with 1 if there were differences, 0 otherwise
		--no-exit-code @bool # opposite of --exit-code
		--quiet @bool # disable all output of the program
		--no-quiet @bool # opposite of --quiet
		--ext-diff @bool # allow an external diff helper to be executed
		--no-ext-diff @bool # opposite of --ext-diff
		--textconv @bool # run external text conversion filters when comparing binary files
		--no-textconv @bool # opposite of --textconv
		--ignore-submodules # ignore changes to submodules in the diff generation
		--submodule # specify how differences in submodules are shown
		--ita-invisible-in-index @bool # hide 'git add -N' entries from the index
		--ita-visible-in-index @bool # treat 'git add -N' entries as real in the index
		--pickaxe-all @bool # show all changes in the changeset with -S or -G
		--pickaxe-regex @bool # treat <string> in -S as extended POSIX regular expression
		--rotate-to # show the change in the specified path first
		--skip-to # skip the output to the specified path
		--find-object # look for differences that change the number of occurrences of the specified object
		--max-depth # maximum tree depth to recurse
		--output # output to a specific file
	diff-tree # Compares the content and mode of blobs found via two tree objects
		--cc @bool # show combined diff for merge commits removing uninteresting hunks
		--combined-all-paths @bool # show name of file in all parents for combined diffs
		--root @bool # include the initial commit as diff against /dev/null
		--patch-with-raw @bool # output both a patch and the diff-raw format.
		--stat @bool # show diffstat instead of patch.
		--numstat @bool # show numeric diffstat instead of patch.
		--patch-with-stat @bool # output a patch and prepend its diffstat.
		--name-only @bool # show only names of changed files.
		--full-index @bool # show full object name on index lines.
		--abbrev # abbreviate object names in diff-tree header and diff-raw.
		--find-copies-harder @bool # try unchanged files as candidate for copy detection.
		--pickaxe-all @bool # show all files diff when -S is used and hit is found.
	difftool # Show changes using common diff tools
		--gui|-g @bool # use `diff.guitool` instead of `diff.tool`
		--no-gui @bool # opposite of --gui
		--dir-diff|-d @bool # perform a full-directory diff
		--no-dir-diff @bool # opposite of --dir-diff
		--no-prompt|-y @bool # do not prompt before launching a diff tool
		--symlinks @bool # use symlinks in dir-diff mode
		--no-symlinks @bool # opposite of --symlinks
		--tool|-t # use the specified diff tool
		--no-tool @bool # opposite of --tool
		--tool-help @bool # print a list of diff tools that may be used with `--tool`
		--no-tool-help @bool # opposite of --tool-help
		--trust-exit-code @bool # make 'git-difftool' exit when an invoked diff tool returns a non-zero exit code
		--no-trust-exit-code @bool # opposite of --trust-exit-code
		--extcmd|-x # specify a custom command for viewing diffs
		--no-extcmd @bool # opposite of --extcmd
		--no-index @bool # passed to `diff`
		--index @bool # opposite of --no-index
	fast-export # Git data exporter
		--progress # show progress after <n> objects
		--no-progress @bool # opposite of --progress
		--signed-tags # select handling of signed tags
		--no-signed-tags @bool # opposite of --signed-tags
		--signed-commits # select handling of signed commits
		--no-signed-commits @bool # opposite of --signed-commits
		--tag-of-filtered-object # select handling of tags that tag filtered objects
		--no-tag-of-filtered-object @bool # opposite of --tag-of-filtered-object
		--reencode # select handling of commit messages in an alternate encoding
		--no-reencode @bool # opposite of --reencode
		--export-marks # dump marks to this file
		--no-export-marks @bool # opposite of --export-marks
		--import-marks # import marks from this file
		--no-import-marks @bool # opposite of --import-marks
		--import-marks-if-exists # import marks from this file if it exists
		--no-import-marks-if-exists @bool # opposite of --import-marks-if-exists
		--fake-missing-tagger @bool # fake a tagger when tags lack one
		--no-fake-missing-tagger @bool # opposite of --fake-missing-tagger
		--full-tree @bool # output full tree for each commit
		--no-full-tree @bool # opposite of --full-tree
		--use-done-feature @bool # use the done feature to terminate the stream
		--no-use-done-feature @bool # opposite of --use-done-feature
		--no-data @bool # skip output of blob data
		--data @bool # opposite of --no-data
		--refspec # apply refspec to exported refs
		--no-refspec @bool # opposite of --refspec
		--anonymize @bool # anonymize output
		--no-anonymize @bool # opposite of --anonymize
		--anonymize-map # convert <from> to <to> in anonymized output
		--reference-excluded-parents @bool # reference parents which are not in fast-export stream by object id
		--no-reference-excluded-parents @bool # opposite of --reference-excluded-parents
		--show-original-ids @bool # show original object ids of blobs/commits
		--no-show-original-ids @bool # opposite of --show-original-ids
		--mark-tags @bool # label tags with mark ids
		--no-mark-tags @bool # opposite of --mark-tags
	fast-import # Backend for fast Git data importers
	fetch # Download objects and refs from another repository
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--all @bool # fetch from all remotes
		--no-all @bool # opposite of --all
		--set-upstream @bool # set upstream for git pull/fetch
		--no-set-upstream @bool # opposite of --set-upstream
		--append|-a @bool # append to .git/FETCH_HEAD instead of overwriting
		--no-append @bool # opposite of --append
		--atomic @bool # use atomic transaction to update references
		--no-atomic @bool # opposite of --atomic
		--upload-pack # path to upload pack on remote end
		--no-upload-pack @bool # opposite of --upload-pack
		--force|-f @bool # force overwrite of local reference
		--no-force @bool # opposite of --force
		--multiple|-m @bool # fetch from multiple remotes
		--no-multiple @bool # opposite of --multiple
		--tags|-t @bool # fetch all tags and associated objects
		--no-tags @bool # opposite of --tags
		--jobs|-j # number of submodules fetched in parallel
		--no-jobs @bool # opposite of --jobs
		--prefetch @bool # modify the refspec to place all refs within refs/prefetch/
		--no-prefetch @bool # opposite of --prefetch
		--prune|-p @bool # prune remote-tracking branches no longer on remote
		--no-prune @bool # opposite of --prune
		--recurse-submodules # control recursive fetching of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--dry-run @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--porcelain @bool # machine-readable output
		--no-porcelain @bool # opposite of --porcelain
		--write-fetch-head @bool # write fetched references to the FETCH_HEAD file
		--no-write-fetch-head @bool # opposite of --write-fetch-head
		--keep|-k @bool # keep downloaded pack
		--no-keep @bool # opposite of --keep
		--update-head-ok|-u @bool # allow updating of HEAD ref
		--no-update-head-ok @bool # opposite of --update-head-ok
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--depth # deepen history of shallow clone
		--no-depth @bool # opposite of --depth
		--shallow-since # deepen history of shallow repository based on time
		--no-shallow-since @bool # opposite of --shallow-since
		--shallow-exclude # deepen history of shallow clone, excluding ref
		--no-shallow-exclude @bool # opposite of --shallow-exclude
		--deepen # deepen history of shallow clone
		--no-deepen @bool # opposite of --deepen
		--unshallow @bool # convert to a complete repository
		--refetch @bool # re-fetch without negotiating common commits
		--refmap # specify fetch refmap
		--server-option|-o # option to transmit
		--no-server-option @bool # opposite of --server-option
		--ipv4|-4 @bool # use IPv4 addresses only
		--ipv6|-6 @bool # use IPv6 addresses only
		--negotiation-tip # report that we have only objects reachable from this object
		--no-negotiation-tip @bool # opposite of --negotiation-tip
		--filter # object filtering
		--no-filter @bool # opposite of --filter
		--auto-maintenance @bool # run 'maintenance --auto' after fetching
		--no-auto-maintenance @bool # opposite of --auto-maintenance
		--auto-gc @bool # run 'maintenance --auto' after fetching
		--no-auto-gc @bool # opposite of --auto-gc
		--show-forced-updates @bool # check for forced-updates on all updated branches
		--no-show-forced-updates @bool # opposite of --show-forced-updates
		--write-commit-graph @bool # write the commit-graph after fetching
		--no-write-commit-graph @bool # opposite of --write-commit-graph
		--stdin @bool # accept refspecs from stdin
		--no-stdin @bool # opposite of --stdin
	fetch-pack # Receive missing objects from another repository
	filter-branch # Rewrite branches
	fmt-merge-msg # Produce a merge commit message
		--log # populate log with at most <n> entries from shortlog
		--no-log @bool # opposite of --log
		--message|-m # use <text> as start of message
		--no-message @bool # opposite of --message
		--into-name # use <name> instead of the real target branch
		--no-into-name @bool # opposite of --into-name
		--file|-F # file to read from
		--no-file @bool # opposite of --file
	for-each-ref # Output information on each ref
		--shell|-s @bool # quote placeholders suitably for shells
		--no-shell @bool # opposite of --shell
		--perl|-p @bool # quote placeholders suitably for perl
		--no-perl @bool # opposite of --perl
		--python @bool # quote placeholders suitably for python
		--no-python @bool # opposite of --python
		--tcl @bool # quote placeholders suitably for Tcl
		--no-tcl @bool # opposite of --tcl
		--omit-empty @bool # do not output a newline after empty formatted refs
		--no-omit-empty @bool # opposite of --omit-empty
		--count # show only <n> matched refs
		--no-count @bool # opposite of --count
		--format # format to use for the output
		--no-format @bool # opposite of --format
		--start-after # start iteration after the provided marker
		--no-start-after @bool # opposite of --start-after
		--exclude # exclude refs which match pattern
		--no-exclude @bool # opposite of --exclude
		--sort # field name to sort on
		--no-sort @bool # opposite of --sort
		--points-at # print only refs which points at the given object
		--no-points-at @bool # opposite of --points-at
		--merged # print only refs that are merged
		--no-merged # print only refs that are not merged
		--contains # print only refs which contain the commit
		--no-contains # print only refs which don't contain the commit
		--ignore-case @bool # sorting and filtering are case insensitive
		--no-ignore-case @bool # opposite of --ignore-case
		--stdin @bool # read reference patterns from stdin
		--no-stdin @bool # opposite of --stdin
		--include-root-refs @bool # also include HEAD ref and pseudorefs
		--no-include-root-refs @bool # opposite of --include-root-refs
	for-each-repo # Run a Git command on a list of repositories
		--config # config key storing a list of repository paths
		--no-config @bool # opposite of --config
		--keep-going @bool # keep going even if command fails in a repository
		--no-keep-going @bool # opposite of --keep-going
	format-bundle # The bundle file format
	format-chunk # Chunk-based file formats
	format-commit-graph # Git commit-graph format
	format-index # Git index format
	format-pack # Git pack format
	format-patch # Prepare patches for e-mail submission
		--numbered|-n @bool # use [PATCH n/m] even with a single patch
		--no-numbered @bool # opposite of --numbered
		--no-numbered|-N @bool # use [PATCH] even with multiple patches
		--signoff|-s @bool # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--stdout @bool # print patches to standard out
		--no-stdout @bool # opposite of --stdout
		--cover-letter @bool # generate a cover letter
		--no-cover-letter @bool # opposite of --cover-letter
		--commit-list-format # format spec used for the commit list in the cover letter
		--no-commit-list-format @bool # opposite of --commit-list-format
		--suffix # use <sfx> instead of '.patch'
		--no-suffix @bool # opposite of --suffix
		--start-number # start numbering patches at <n> instead of 1
		--no-start-number @bool # opposite of --start-number
		--reroll-count|-v # mark the series as Nth re-roll
		--no-reroll-count @bool # opposite of --reroll-count
		--filename-max-length # max length of output filename
		--no-filename-max-length @bool # opposite of --filename-max-length
		--rfc # add <rfc> (default 'RFC') before 'PATCH'
		--no-rfc @bool # opposite of --rfc
		--cover-from-description # generate parts of a cover letter based on a branch's description
		--no-cover-from-description @bool # opposite of --cover-from-description
		--description-file # use branch description from file
		--no-description-file @bool # opposite of --description-file
		--subject-prefix # use [<prefix>] instead of [PATCH]
		--output-directory|-o # store resulting files in <dir>
		--keep-subject|-k @bool # don't strip/add [PATCH]
		--no-binary @bool # don't output binary diffs
		--binary @bool # opposite of --no-binary
		--zero-commit @bool # output all-zero hash in From header
		--no-zero-commit @bool # opposite of --zero-commit
		--ignore-if-in-upstream @bool # don't include a patch matching a commit upstream
		--no-ignore-if-in-upstream @bool # opposite of --ignore-if-in-upstream
		--no-stat|-p @bool # show patch format instead of default (patch + stat)
		--add-header # add email header
		--no-add-header @bool # opposite of --add-header
		--to # add To: header
		--no-to @bool # opposite of --to
		--cc # add Cc: header
		--no-cc @bool # opposite of --cc
		--in-reply-to # make first mail a reply to <message-id>
		--no-in-reply-to @bool # opposite of --in-reply-to
		--attach # attach the patch
		--no-attach @bool # opposite of --attach
		--thread # enable message threading, styles: shallow, deep
		--no-thread @bool # opposite of --thread
		--signature # add a signature
		--no-signature @bool # opposite of --signature
		--base # add prerequisite tree info to the patch series
		--no-base @bool # opposite of --base
		--signature-file # add a signature from a file
		--no-signature-file @bool # opposite of --signature-file
		--quiet|-q @bool # don't print the patch filenames
		--no-quiet @bool # opposite of --quiet
		--progress @bool # show progress while generating patches
		--no-progress @bool # opposite of --progress
		--interdiff # show changes against <rev> in cover letter or single patch
		--no-interdiff @bool # opposite of --interdiff
		--range-diff # show changes against <refspec> in cover letter or single patch
		--no-range-diff @bool # opposite of --range-diff
		--creation-factor # percentage by which creation is weighted
		--no-creation-factor @bool # opposite of --creation-factor
		--force-in-body-from @bool # show in-body From: even if identical to the e-mail header
		--no-force-in-body-from @bool # opposite of --force-in-body-from
	format-signature # Git cryptographic signature formats
	fsck # Verifies the connectivity and validity of the objects in the database
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--unreachable @bool # show unreachable objects
		--no-unreachable @bool # opposite of --unreachable
		--dangling @bool # show dangling objects
		--no-dangling @bool # opposite of --dangling
		--tags @bool # report tags
		--no-tags @bool # opposite of --tags
		--root @bool # report root nodes
		--no-root @bool # opposite of --root
		--cache @bool # make index objects head nodes
		--no-cache @bool # opposite of --cache
		--reflogs @bool # make reflogs head nodes (default)
		--no-reflogs @bool # opposite of --reflogs
		--full @bool # also consider packs and alternate objects
		--no-full @bool # opposite of --full
		--connectivity-only @bool # check only connectivity
		--no-connectivity-only @bool # opposite of --connectivity-only
		--strict @bool # enable more strict checking
		--no-strict @bool # opposite of --strict
		--lost-found @bool # write dangling objects in .git/lost-found
		--no-lost-found @bool # opposite of --lost-found
		--progress @bool # show progress
		--no-progress @bool # opposite of --progress
		--name-objects @bool # show verbose names for reachable objects
		--no-name-objects @bool # opposite of --name-objects
		--references @bool # check reference database consistency
		--no-references @bool # opposite of --references
	gc # Cleanup unnecessary files and optimize the local repository
		--quiet|-q @bool # suppress progress reporting
		--no-quiet @bool # opposite of --quiet
		--cruft @bool # pack unreferenced objects separately
		--no-cruft @bool # opposite of --cruft
		--max-cruft-size # with --cruft, limit the size of new cruft packs
		--aggressive @bool # be more thorough (increased runtime)
		--no-aggressive @bool # opposite of --aggressive
		--auto @bool # enable auto-gc mode
		--no-auto @bool # opposite of --auto
		--detach @bool # perform garbage collection in the background
		--no-detach @bool # opposite of --detach
		--force @bool # force running gc even if there may be another gc running
		--no-force @bool # opposite of --force
		--keep-largest-pack @bool # repack all other packs except the largest pack
		--no-keep-largest-pack @bool # opposite of --keep-largest-pack
		--expire-to # pack prefix to store a pack containing pruned objects
		--no-expire-to @bool # opposite of --expire-to
	get-tar-commit-id # Extract commit ID from an archive created using git-archive
	gitk # The Git repository browser
	gitweb # Git web interface (web frontend to Git repositories)
	grep # Print lines matching a pattern
		--cached @bool # search in index instead of in the work tree
		--no-cached @bool # opposite of --cached
		--no-index @bool # find in contents not managed by git
		--index @bool # opposite of --no-index
		--untracked @bool # search in both tracked and untracked files
		--no-untracked @bool # opposite of --untracked
		--exclude-standard @bool # ignore files specified via '.gitignore'
		--no-exclude-standard @bool # opposite of --exclude-standard
		--recurse-submodules @bool # recursively search in each submodule
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--invert-match|-v @bool # show non-matching lines
		--no-invert-match @bool # opposite of --invert-match
		--ignore-case|-i @bool # case insensitive matching
		--no-ignore-case @bool # opposite of --ignore-case
		--word-regexp|-w @bool # match patterns only at word boundaries
		--no-word-regexp @bool # opposite of --word-regexp
		--text|-a @bool # process binary files as text
		--no-text @bool # opposite of --text
		--textconv @bool # process binary files with textconv filters
		--no-textconv @bool # opposite of --textconv
		--recursive|-r @bool # search in subdirectories (default)
		--no-recursive @bool # opposite of --recursive
		--max-depth # descend at most <n> levels
		--extended-regexp|-E @bool # use extended POSIX regular expressions
		--no-extended-regexp @bool # opposite of --extended-regexp
		--basic-regexp|-G @bool # use basic POSIX regular expressions (default)
		--no-basic-regexp @bool # opposite of --basic-regexp
		--fixed-strings|-F @bool # interpret patterns as fixed strings
		--no-fixed-strings @bool # opposite of --fixed-strings
		--perl-regexp|-P @bool # use Perl-compatible regular expressions
		--no-perl-regexp @bool # opposite of --perl-regexp
		--line-number|-n @bool # show line numbers
		--no-line-number @bool # opposite of --line-number
		--column @bool # show column number of first match
		--no-column @bool # opposite of --column
		--full-name @bool # show filenames relative to top directory
		--no-full-name @bool # opposite of --full-name
		--files-with-matches|-l @bool # show only filenames instead of matching lines
		--no-files-with-matches @bool # opposite of --files-with-matches
		--name-only @bool # synonym for --files-with-matches
		--no-name-only @bool # opposite of --name-only
		--files-without-match|-L @bool # show only the names of files without match
		--no-files-without-match @bool # opposite of --files-without-match
		--null|-z @bool # print NUL after filenames
		--no-null @bool # opposite of --null
		--only-matching|-o @bool # show only matching parts of a line
		--no-only-matching @bool # opposite of --only-matching
		--count|-c @bool # show the number of matches instead of matching lines
		--no-count @bool # opposite of --count
		--break @bool # print empty line between matches from different files
		--no-break @bool # opposite of --break
		--heading @bool # show filename only once above matches from same file
		--no-heading @bool # opposite of --heading
		--context|-C # show <n> context lines before and after matches
		--no-context @bool # opposite of --context
		--before-context|-B # show <n> context lines before matches
		--after-context|-A # show <n> context lines after matches
		--threads # use <n> worker threads
		--no-threads @bool # opposite of --threads
		--show-function|-p @bool # show a line with the function name before matches
		--no-show-function @bool # opposite of --show-function
		--function-context|-W @bool # show the surrounding function
		--no-function-context @bool # opposite of --function-context
		--and @bool # combine patterns specified with -e
		--or @bool # --not
		--not @bool # (
		--quiet|-q @bool # indicate hit with exit status without output
		--no-quiet @bool # opposite of --quiet
		--all-match @bool # show only matches from files that match all patterns
		--no-all-match @bool # opposite of --all-match
		--open-files-in-pager|-O # show matching files in the pager
		--no-open-files-in-pager @bool # opposite of --open-files-in-pager
		--ext-grep @bool # allow calling of grep(1) (ignored by this build)
		--no-ext-grep @bool # opposite of --ext-grep
		--max-count|-m # maximum number of results per file
		--no-max-count @bool # opposite of --max-count
	gui # A portable graphical interface to Git
	hash-object # Compute object ID and optionally create an object from a file
		--stdin @bool # read the object from stdin
		--no-stdin @bool # opposite of --stdin
		--stdin-paths @bool # read file names from stdin
		--no-stdin-paths @bool # opposite of --stdin-paths
		--no-filters @bool # store file as is without filters
		--filters @bool # opposite of --no-filters
		--literally @bool # just hash any random garbage to create corrupt objects for debugging Git
		--no-literally @bool # opposite of --literally
		--path # process file as it were from this path
		--no-path @bool # opposite of --path
	history # EXPERIMENTAL: Rewrite history
	hook # Run git hooks
	hooks # Hooks used by Git
	http-backend # Server side implementation of Git over HTTP
	ignore # Specifies intentionally untracked files to ignore
	imap-send # Send a collection of patches from stdin to an IMAP folder
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--curl @bool # use libcurl to communicate with the IMAP server
		--no-curl @bool # opposite of --curl
		--folder|-f # specify the IMAP folder
		--no-folder @bool # opposite of --folder
		--list @bool # list all folders on the IMAP server
		--no-list @bool # opposite of --list
	index-pack # Build pack index file for an existing packed archive
	init # Create an empty Git repository or reinitialize an existing one
		--template # directory from which templates will be used
		--no-template @bool # opposite of --template
		--bare @bool # create a bare repository
		--no-bare @bool # opposite of --bare
		--shared # specify that the git repository is to be shared amongst several users
		--quiet|-q @bool # be quiet
		--no-quiet @bool # opposite of --quiet
		--separate-git-dir # separate git dir from working tree
		--no-separate-git-dir @bool # opposite of --separate-git-dir
		--initial-branch|-b # override the name of the initial branch
		--no-initial-branch @bool # opposite of --initial-branch
		--object-format # specify the hash algorithm to use
		--no-object-format @bool # opposite of --object-format
		--ref-format # specify the reference format to use
		--no-ref-format @bool # opposite of --ref-format
	instaweb # Instantly browse your working repository in gitweb
		--local|-l @bool # only bind on 127.0.0.1
		--no-local @bool # opposite of --local
		--port|-p # the port to bind to
		--no-port @bool # opposite of --port
		--httpd|-d # the command to launch
		--no-httpd @bool # opposite of --httpd
		--browser|-b # the browser to launch
		--no-browser @bool # opposite of --browser
		--module-path|-m # the module path (only needed for apache2)
		--no-module-path @bool # opposite of --module-path
		--stop @bool # stop the web server
		--no-stop @bool # opposite of --stop
		--start @bool # start the web server
		--no-start @bool # opposite of --start
		--restart @bool # restart the web server
		--no-restart @bool # opposite of --restart
	interpret-trailers # Add or parse structured information in commit messages
		--in-place @bool # edit files in place
		--no-in-place @bool # opposite of --in-place
		--trim-empty @bool # trim empty trailers
		--no-trim-empty @bool # opposite of --trim-empty
		--where # where to place the new trailer
		--no-where @bool # opposite of --where
		--if-exists # action if trailer already exists
		--no-if-exists @bool # opposite of --if-exists
		--if-missing # action if trailer is missing
		--no-if-missing @bool # opposite of --if-missing
		--only-trailers @bool # output only the trailers
		--no-only-trailers @bool # opposite of --only-trailers
		--only-input @bool # do not apply trailer.<key-alias> configuration variables
		--no-only-input @bool # opposite of --only-input
		--unfold @bool # reformat multiline trailer values as single-line values
		--no-unfold @bool # opposite of --unfold
		--parse @bool # alias for --only-trailers --only-input --unfold
		--no-divider @bool # do not treat "---" as the end of input
		--divider @bool # opposite of --no-divider
		--trailer # trailer(s) to add
		--no-trailer @bool # opposite of --trailer
	last-modified # EXPERIMENTAL: Show when files were last modified
		--recursive|-r @bool # recurse into subtrees
		--no-recursive @bool # opposite of --recursive
		--max-depth # maximum tree depth to recurse
	log # Show commit logs
		--quiet|-q @bool # suppress diff output
		--no-quiet @bool # opposite of --quiet
		--source @bool # show source
		--no-source @bool # opposite of --source
		--use-mailmap @bool # use mail map file
		--no-use-mailmap @bool # opposite of --use-mailmap
		--mailmap @bool # alias of --use-mailmap
		--no-mailmap @bool # opposite of --mailmap
		--clear-decorations @bool # clear all previously-defined decoration filters
		--decorate-refs # only decorate refs that match <pattern>
		--no-decorate-refs @bool # opposite of --decorate-refs
		--decorate-refs-exclude # do not decorate refs that match <pattern>
		--no-decorate-refs-exclude @bool # opposite of --decorate-refs-exclude
	ls-files # Show information about files in the index and the working tree
		--cached|-c @bool # show cached files in the output (default)
		--no-cached @bool # opposite of --cached
		--deleted|-d @bool # show deleted files in the output
		--no-deleted @bool # opposite of --deleted
		--modified|-m @bool # show modified files in the output
		--no-modified @bool # opposite of --modified
		--others|-o @bool # show other files in the output
		--no-others @bool # opposite of --others
		--ignored|-i @bool # show ignored files in the output
		--no-ignored @bool # opposite of --ignored
		--stage|-s @bool # show staged contents' object name in the output
		--no-stage @bool # opposite of --stage
		--killed|-k @bool # show files on the filesystem that need to be removed
		--no-killed @bool # opposite of --killed
		--directory @bool # show 'other' directories' names only
		--no-directory @bool # opposite of --directory
		--eol @bool # show line endings of files
		--no-eol @bool # opposite of --eol
		--empty-directory @bool # don't show empty directories
		--no-empty-directory @bool # opposite of --empty-directory
		--unmerged|-u @bool # show unmerged files in the output
		--no-unmerged @bool # opposite of --unmerged
		--resolve-undo @bool # show resolve-undo information
		--no-resolve-undo @bool # opposite of --resolve-undo
		--exclude|-x # skip files matching pattern
		--exclude-from|-X # read exclude patterns from <file>
		--exclude-per-directory # read additional per-directory exclude patterns in <file>
		--no-exclude-per-directory @bool # opposite of --exclude-per-directory
		--exclude-standard @bool # add the standard git exclusions
		--full-name @bool # make the output relative to the project top directory
		--recurse-submodules @bool # recurse through submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--error-unmatch @bool # if any <file> is not in the index, treat this as an error
		--no-error-unmatch @bool # opposite of --error-unmatch
		--with-tree # pretend that paths removed since <tree-ish> are still present
		--no-with-tree @bool # opposite of --with-tree
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--debug @bool # show debugging data
		--no-debug @bool # opposite of --debug
		--deduplicate @bool # suppress duplicate entries
		--no-deduplicate @bool # opposite of --deduplicate
		--sparse @bool # show sparse directories in the presence of a sparse index
		--no-sparse @bool # opposite of --sparse
		--format # format to use for the output
	ls-remote # List references in a remote repository
		--quiet|-q @bool # do not print remote URL
		--no-quiet @bool # opposite of --quiet
		--upload-pack # path of git-upload-pack on the remote host
		--no-upload-pack @bool # opposite of --upload-pack
		--tags|-t @bool # limit to tags
		--no-tags @bool # opposite of --tags
		--branches|-b @bool # limit to branches
		--no-branches @bool # opposite of --branches
		--refs @bool # do not show peeled tags
		--no-refs @bool # opposite of --refs
		--get-url @bool # take url.<base>.insteadOf into account
		--no-get-url @bool # opposite of --get-url
		--sort # field name to sort on
		--no-sort @bool # opposite of --sort
		--exit-code @bool # exit with exit code 2 if no matching refs are found
		--no-exit-code @bool # opposite of --exit-code
		--symref @bool # show underlying ref in addition to the object pointed by it
		--no-symref @bool # opposite of --symref
		--server-option|-o # option to transmit
		--no-server-option @bool # opposite of --server-option
	ls-tree # List the contents of a tree object
		--long|-l @bool # include object size
		--name-only @bool # list only filenames
		--name-status @bool # list only filenames
		--object-only @bool # list only objects
		--full-name @bool # use full path names
		--no-full-name @bool # opposite of --full-name
		--full-tree @bool # list entire tree; not just current directory (implies --full-name)
		--no-full-tree @bool # opposite of --full-tree
		--format # format to use for the output
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
	mailinfo # Extracts patch and authorship from a single e-mail message
		--scissors @bool # use scissors
		--no-scissors @bool # opposite of --scissors
		--quoted-cr # action when quoted CR is found
	mailmap # Map author/committer names and/or E-Mail addresses
	mailsplit # Simple UNIX mbox splitter program
	maintenance # Run tasks to optimize Git repository data
	merge # Join two or more development histories together
		--stat @bool # show a diffstat at the end of the merge
		--no-stat @bool # opposite of --stat
		--summary @bool # (synonym to --stat)
		--no-summary @bool # opposite of --summary
		--compact-summary @bool # show a compact-summary at the end of the merge
		--no-compact-summary @bool # opposite of --compact-summary
		--log # add (at most <n>) entries from shortlog to merge commit message
		--no-log @bool # opposite of --log
		--squash @bool # create a single commit instead of doing a merge
		--no-squash @bool # opposite of --squash
		--commit @bool # perform a commit if the merge succeeds (default)
		--no-commit @bool # opposite of --commit
		--edit|-e @bool # edit message before committing
		--no-edit @bool # opposite of --edit
		--ff @bool # allow fast-forward (default)
		--no-ff @bool # opposite of --ff
		--ff-only @bool # abort if fast-forward is not possible
		--rerere-autoupdate @bool # update the index with reused conflict resolution if possible
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
		--verify-signatures @bool # verify that the named commit has a valid GPG signature
		--no-verify-signatures @bool # opposite of --verify-signatures
		--strategy|-s # merge strategy to use
		--no-strategy @bool # opposite of --strategy
		--strategy-option|-X # option for selected merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
		--message|-m # merge commit message (for a non-fast-forward merge)
		--no-message @bool # opposite of --message
		--file|-F # read message from file
		--into-name # use <name> instead of the real target
		--no-into-name @bool # opposite of --into-name
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--abort @bool # abort the current in-progress merge
		--no-abort @bool # opposite of --abort
		--quit @bool # --abort but leave index and working tree alone
		--no-quit @bool # opposite of --quit
		--continue @bool # continue the current in-progress merge
		--no-continue @bool # opposite of --continue
		--allow-unrelated-histories @bool # allow merging unrelated histories
		--no-allow-unrelated-histories @bool # opposite of --allow-unrelated-histories
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
		--autostash @bool # automatically stash/stash pop before and after
		--no-autostash @bool # opposite of --autostash
		--overwrite-ignore @bool # update ignored files (default)
		--no-overwrite-ignore @bool # opposite of --overwrite-ignore
		--signoff @bool # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--no-verify @bool # bypass pre-merge-commit and commit-msg hooks
		--verify @bool # opposite of --no-verify
	merge-base # Find as good common ancestors as possible for a merge
		--all|-a @bool # output all common ancestors
		--no-all @bool # opposite of --all
		--octopus @bool # find ancestors for a single n-way merge
		--independent @bool # list revs not reachable from others
		--is-ancestor @bool # is the first one ancestor of the other?
		--fork-point @bool # find where <commit> forked from reflog of <ref>
	merge-file # Run a three-way file merge
		--stdout|-p @bool # send results to standard output
		--no-stdout @bool # opposite of --stdout
		--object-id @bool # use object IDs instead of filenames
		--no-object-id @bool # opposite of --object-id
		--diff3 @bool # use a diff3 based merge
		--no-diff3 @bool # opposite of --diff3
		--zdiff3 @bool # use a zealous diff3 based merge
		--no-zdiff3 @bool # opposite of --zdiff3
		--ours @bool # for conflicts, use our version
		--no-ours @bool # opposite of --ours
		--theirs @bool # for conflicts, use their version
		--no-theirs @bool # opposite of --theirs
		--union @bool # for conflicts, use a union version
		--no-union @bool # opposite of --union
		--diff-algorithm # choose a diff algorithm
		--marker-size # for conflicts, use this marker size
		--no-marker-size @bool # opposite of --marker-size
		--quiet|-q @bool # do not warn about conflicts
		--no-quiet @bool # opposite of --quiet
	merge-index # Run a merge for files needing merging
	merge-one-file # The standard helper program to use with git-merge-index
	merge-tree # Perform merge without touching index or working tree
		--write-tree @bool # do a real merge instead of a trivial merge
		--trivial-merge @bool # do a trivial merge only
		--messages @bool # also show informational/conflict messages
		--no-messages @bool # opposite of --messages
		--quiet @bool # suppress all output; only exit status wanted
		--name-only @bool # list filenames without modes/oids/stages
		--allow-unrelated-histories @bool # allow merging unrelated histories
		--stdin @bool # perform multiple merges, one per line of input
		--merge-base # specify a merge-base for the merge
		--no-merge-base @bool # opposite of --merge-base
		--strategy-option|-X # option for selected merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
	mergetool # Run merge conflict resolution tools to resolve merge conflicts
	mktag # Creates a tag object with extra validation
		--strict @bool # enable more strict checking
		--no-strict @bool # opposite of --strict
	mktree # Build a tree-object from ls-tree formatted text
		--missing @bool # allow missing objects
		--no-missing @bool # opposite of --missing
		--batch @bool # allow creation of more than one tree
		--no-batch @bool # opposite of --batch
	modules # Defining submodule properties
	multi-pack-index # Write and verify multi-pack-indexes
		--object-dir # object directory containing set of packfile and pack-index pairs
		--no-object-dir @bool # opposite of --object-dir
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
	mv # Move or rename a file, a directory, or a symlink
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--force|-f @bool # force move/rename even if target exists
		--no-force @bool # opposite of --force
		--sparse @bool # allow updating entries outside of the sparse-checkout cone
		--no-sparse @bool # opposite of --sparse
	name-rev # Find symbolic names for given revs
		--name-only @bool # print only ref-based names (no object names)
		--no-name-only @bool # opposite of --name-only
		--tags @bool # only use tags to name the commits
		--no-tags @bool # opposite of --tags
		--exclude # ignore refs matching <pattern>
		--no-exclude @bool # opposite of --exclude
		--all @bool # list all commits reachable from all refs
		--no-all @bool # opposite of --all
		--undefined @bool # allow to print `undefined` names (default)
		--no-undefined @bool # opposite of --undefined
		--always @bool # show abbreviated commit object as fallback
		--no-always @bool # opposite of --always
	notes # Add or inspect object notes
		--ref # use notes from <notes-ref>
		--no-ref @bool # opposite of --ref
	p4 # Import from and submit to Perforce repositories
	pack-objects # Create a packed archive of objects
		--quiet|-q @bool # do not show progress meter
		--no-quiet @bool # opposite of --quiet
		--progress @bool # show progress meter
		--no-progress @bool # opposite of --progress
		--all-progress @bool # show progress meter during object writing phase
		--no-all-progress @bool # opposite of --all-progress
		--all-progress-implied @bool # similar to --all-progress when progress meter is shown
		--no-all-progress-implied @bool # opposite of --all-progress-implied
		--max-pack-size # maximum size of each output pack file
		--local @bool # ignore borrowed objects from alternate object store
		--no-local @bool # opposite of --local
		--incremental @bool # ignore packed objects
		--no-incremental @bool # opposite of --incremental
		--window # limit pack window by objects
		--no-window @bool # opposite of --window
		--window-memory # limit pack window by memory in addition to object limit
		--depth # maximum length of delta chain allowed in the resulting pack
		--no-depth @bool # opposite of --depth
		--reuse-delta @bool # reuse existing deltas
		--no-reuse-delta @bool # opposite of --reuse-delta
		--reuse-object @bool # reuse existing objects
		--no-reuse-object @bool # opposite of --reuse-object
		--delta-base-offset @bool # use OFS_DELTA objects
		--no-delta-base-offset @bool # opposite of --delta-base-offset
		--threads # use threads when searching for best delta matches
		--no-threads @bool # opposite of --threads
		--non-empty @bool # do not create an empty pack output
		--no-non-empty @bool # opposite of --non-empty
		--revs @bool # read revision arguments from standard input
		--no-revs @bool # opposite of --revs
		--unpacked @bool # limit the objects to those that are not yet packed
		--all @bool # include objects reachable from any reference
		--reflog @bool # include objects referred by reflog entries
		--indexed-objects @bool # include objects referred to by the index
		--stdin-packs # read packs from stdin
		--no-stdin-packs @bool # opposite of --stdin-packs
		--stdout @bool # output pack to stdout
		--no-stdout @bool # opposite of --stdout
		--include-tag @bool # include tag objects that refer to objects to be packed
		--no-include-tag @bool # opposite of --include-tag
		--keep-unreachable @bool # keep unreachable objects
		--no-keep-unreachable @bool # opposite of --keep-unreachable
		--pack-loose-unreachable @bool # pack loose unreachable objects
		--no-pack-loose-unreachable @bool # opposite of --pack-loose-unreachable
		--unpack-unreachable # unpack unreachable objects newer than <time>
		--no-unpack-unreachable @bool # opposite of --unpack-unreachable
		--cruft @bool # create a cruft pack
		--no-cruft @bool # opposite of --cruft
		--cruft-expiration # expire cruft objects older than <time>
		--no-cruft-expiration @bool # opposite of --cruft-expiration
		--sparse @bool # use the sparse reachability algorithm
		--no-sparse @bool # opposite of --sparse
		--thin @bool # create thin packs
		--no-thin @bool # opposite of --thin
		--path-walk @bool # use the path-walk API to walk objects when possible
		--no-path-walk @bool # opposite of --path-walk
		--shallow @bool # create packs suitable for shallow fetches
		--no-shallow @bool # opposite of --shallow
		--honor-pack-keep @bool # ignore packs that have companion .keep file
		--no-honor-pack-keep @bool # opposite of --honor-pack-keep
		--keep-pack # ignore this pack
		--no-keep-pack @bool # opposite of --keep-pack
		--compression # pack compression level
		--no-compression @bool # opposite of --compression
		--keep-true-parents @bool # do not hide commits by grafts
		--no-keep-true-parents @bool # opposite of --keep-true-parents
		--use-bitmap-index @bool # use a bitmap index if available to speed up counting objects
		--no-use-bitmap-index @bool # opposite of --use-bitmap-index
		--write-bitmap-index @bool # write a bitmap index together with the pack index
		--no-write-bitmap-index @bool # opposite of --write-bitmap-index
		--filter # object filtering
		--no-filter @bool # opposite of --filter
		--missing # handling for missing objects
		--exclude-promisor-objects @bool # do not pack objects in promisor packfiles
		--no-exclude-promisor-objects @bool # opposite of --exclude-promisor-objects
		--exclude-promisor-objects-best-effort @bool # implies --missing=allow-any
		--no-exclude-promisor-objects-best-effort @bool # opposite of --exclude-promisor-objects-best-effort
		--delta-islands @bool # respect islands during delta compression
		--no-delta-islands @bool # opposite of --delta-islands
		--uri-protocol # exclude any configured uploadpack.blobpackfileuri with this protocol
		--no-uri-protocol @bool # opposite of --uri-protocol
		--name-hash-version # use the specified name-hash function to group similar objects
		--no-name-hash-version @bool # opposite of --name-hash-version
	pack-redundant # Find redundant pack files
	pack-refs # Pack heads and tags for efficient repository access
		--all @bool # pack everything
		--no-all @bool # opposite of --all
		--prune @bool # prune loose refs (default)
		--no-prune @bool # opposite of --prune
		--auto @bool # auto-pack refs as needed
		--no-auto @bool # opposite of --auto
		--include # references to include
		--no-include @bool # opposite of --include
		--exclude # references to exclude
		--no-exclude @bool # opposite of --exclude
	patch-id # Compute unique IDs for patches
		--unstable @bool # use the unstable patch ID algorithm
		--stable @bool # use the stable patch ID algorithm
		--verbatim @bool # don't strip whitespace from the patch
	protocol-capabilities # Protocol v0 and v1 capabilities
	protocol-common # Things common to various protocols
	protocol-http # Git HTTP-based protocols
	protocol-pack # How packs are transferred over-the-wire
	protocol-v2 # Git Wire Protocol, Version 2
	prune # Prune all unreachable objects from the object database
		--dry-run|-n @bool # do not remove, show only
		--no-dry-run @bool # opposite of --dry-run
		--verbose|-v @bool # report pruned objects
		--no-verbose @bool # opposite of --verbose
		--progress @bool # show progress
		--no-progress @bool # opposite of --progress
		--expire # expire objects older than <time>
		--no-expire @bool # opposite of --expire
		--exclude-promisor-objects @bool # limit traversal to objects outside promisor packfiles
		--no-exclude-promisor-objects @bool # opposite of --exclude-promisor-objects
	prune-packed # Remove extra objects that are already in pack files
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--quiet|-q @bool # be quiet
		--no-quiet @bool # opposite of --quiet
	pull # Fetch from and integrate with another repository or a local branch
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--recurse-submodules # control for recursive fetching of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--rebase|-r # incorporate changes by rebasing rather than merging
		--no-rebase @bool # opposite of --rebase
		--stat @bool # show a diffstat at the end of the merge
		--no-stat @bool # opposite of --stat
		--compact-summary @bool # show a compact-summary at the end of the merge
		--no-compact-summary @bool # opposite of --compact-summary
		--log # add (at most <n>) entries from shortlog to merge commit message
		--no-log @bool # opposite of --log
		--signoff # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--squash @bool # create a single commit instead of doing a merge
		--no-squash @bool # opposite of --squash
		--commit @bool # perform a commit if the merge succeeds (default)
		--no-commit @bool # opposite of --commit
		--edit @bool # edit message before committing
		--no-edit @bool # opposite of --edit
		--ff @bool # allow fast-forward
		--no-ff @bool # opposite of --ff
		--ff-only @bool # abort if fast-forward is not possible
		--verify @bool # control use of pre-merge-commit and commit-msg hooks
		--no-verify @bool # opposite of --verify
		--verify-signatures @bool # verify that the named commit has a valid GPG signature
		--no-verify-signatures @bool # opposite of --verify-signatures
		--autostash @bool # automatically stash/stash pop before and after
		--no-autostash @bool # opposite of --autostash
		--strategy|-s # merge strategy to use
		--no-strategy @bool # opposite of --strategy
		--strategy-option|-X # option for selected merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
		--allow-unrelated-histories @bool # allow merging unrelated histories
		--no-allow-unrelated-histories @bool # opposite of --allow-unrelated-histories
		--all @bool # fetch from all remotes
		--no-all @bool # opposite of --all
		--append|-a @bool # append to .git/FETCH_HEAD instead of overwriting
		--no-append @bool # opposite of --append
		--upload-pack # path to upload pack on remote end
		--no-upload-pack @bool # opposite of --upload-pack
		--force|-f @bool # force overwrite of local branch
		--no-force @bool # opposite of --force
		--tags|-t @bool # fetch all tags and associated objects
		--no-tags @bool # opposite of --tags
		--prune|-p @bool # prune remote-tracking branches no longer on remote
		--no-prune @bool # opposite of --prune
		--dry-run @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--keep|-k @bool # keep downloaded pack
		--no-keep @bool # opposite of --keep
		--depth # deepen history of shallow clone
		--no-depth @bool # opposite of --depth
		--shallow-since # deepen history of shallow repository based on time
		--no-shallow-since @bool # opposite of --shallow-since
		--shallow-exclude # deepen history of shallow clone, excluding ref
		--no-shallow-exclude @bool # opposite of --shallow-exclude
		--deepen # deepen history of shallow clone
		--no-deepen @bool # opposite of --deepen
		--unshallow @bool # convert to a complete repository
		--refmap # specify fetch refmap
		--server-option|-o # option to transmit
		--no-server-option @bool # opposite of --server-option
		--ipv4|-4 @bool # use IPv4 addresses only
		--no-ipv4 @bool # opposite of --ipv4
		--ipv6|-6 @bool # use IPv6 addresses only
		--no-ipv6 @bool # opposite of --ipv6
		--negotiation-tip # report that we have only objects reachable from this object
		--no-negotiation-tip @bool # opposite of --negotiation-tip
		--show-forced-updates @bool # check for forced-updates on all updated branches
		--no-show-forced-updates @bool # opposite of --show-forced-updates
		--set-upstream @bool # set upstream for git pull/fetch
		--no-set-upstream @bool # opposite of --set-upstream
	push # Update remote refs along with associated objects
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--repo # repository
		--no-repo @bool # opposite of --repo
		--all @bool # push all branches
		--no-all @bool # opposite of --all
		--branches @bool # alias of --all
		--no-branches @bool # opposite of --branches
		--mirror @bool # mirror all refs
		--no-mirror @bool # opposite of --mirror
		--delete|-d @bool # delete refs
		--no-delete @bool # opposite of --delete
		--tags @bool # push tags (can't be used with --all or --branches or --mirror)
		--no-tags @bool # opposite of --tags
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--porcelain @bool # machine-readable output
		--no-porcelain @bool # opposite of --porcelain
		--force|-f @bool # force updates
		--no-force @bool # opposite of --force
		--force-if-includes @bool # require remote updates to be integrated locally
		--no-force-if-includes @bool # opposite of --force-if-includes
		--recurse-submodules # control recursive pushing of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--thin @bool # use thin pack
		--no-thin @bool # opposite of --thin
		--receive-pack # receive pack program
		--no-receive-pack @bool # opposite of --receive-pack
		--exec # receive pack program
		--no-exec @bool # opposite of --exec
		--set-upstream|-u @bool # set upstream for git pull/status
		--no-set-upstream @bool # opposite of --set-upstream
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--prune @bool # prune locally removed refs
		--no-prune @bool # opposite of --prune
		--no-verify @bool # bypass pre-push hook
		--verify @bool # opposite of --no-verify
		--follow-tags @bool # push missing but relevant tags
		--no-follow-tags @bool # opposite of --follow-tags
		--signed # GPG sign the push
		--no-signed @bool # opposite of --signed
		--atomic @bool # request atomic transaction on remote side
		--no-atomic @bool # opposite of --atomic
		--push-option|-o # option to transmit
		--no-push-option @bool # opposite of --push-option
		--ipv4|-4 @bool # use IPv4 addresses only
		--ipv6|-6 @bool # use IPv6 addresses only
	quiltimport # Applies a quilt patchset onto the current branch
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--author # author name and email address for patches without any
		--no-author @bool # opposite of --author
		--patches # path to the quilt patches
		--no-patches @bool # opposite of --patches
		--series # path to the quilt series file
		--no-series @bool # opposite of --series
	range-diff # Compare two commit ranges (e.g. two versions of a branch)
		--creation-factor # percentage by which creation is weighted
		--no-creation-factor @bool # opposite of --creation-factor
		--no-dual-color @bool # use simple diff colors
		--dual-color @bool # opposite of --no-dual-color
		--notes # passed to 'git log'
		--no-notes @bool # opposite of --notes
		--diff-merges # passed to 'git log'
		--no-diff-merges @bool # opposite of --diff-merges
		--max-memory # maximum memory for cost matrix (default 4G)
		--no-max-memory @bool # opposite of --max-memory
		--remerge-diff @bool # passed to 'git log'
		--no-remerge-diff @bool # opposite of --remerge-diff
		--left-only @bool # only emit output related to the first range
		--no-left-only @bool # opposite of --left-only
		--right-only @bool # only emit output related to the second range
		--no-right-only @bool # opposite of --right-only
		--patch|-p @bool # generate patch
		--no-patch|-s @bool # suppress diff output
		--unified|-U # generate diffs with <n> lines context
		--function-context|-W @bool # generate diffs with <n> lines context
		--no-function-context @bool # opposite of --function-context
		--raw @bool # generate the diff in raw format
		--patch-with-raw @bool # synonym for '-p --raw'
		--patch-with-stat @bool # synonym for '-p --stat'
		--numstat @bool # machine friendly --stat
		--shortstat @bool # output only the last line of --stat
		--cumulative @bool # synonym for --dirstat=cumulative
		--check @bool # warn if changes introduce conflict markers or whitespace errors
		--summary @bool # condensed summary such as creations, renames and mode changes
		--name-only @bool # show only names of changed files
		--name-status @bool # show only names and status of changed files
		--stat-width # generate diffstat with a given width
		--stat-name-width # generate diffstat with a given name width
		--stat-graph-width # generate diffstat with a given graph width
		--stat-count # generate diffstat with limited lines
		--compact-summary @bool # generate compact summary in diffstat
		--no-compact-summary @bool # opposite of --compact-summary
		--binary @bool # output a binary diff that can be applied
		--full-index @bool # show full pre- and post-image object names on the "index" lines
		--no-full-index @bool # opposite of --full-index
		--ws-error-highlight # highlight whitespace errors in the 'context', 'old' or 'new' lines in the diff
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--line-prefix # prepend an additional prefix to every line of output
		--no-prefix @bool # do not show any source or destination prefix
		--default-prefix @bool # use default prefixes a/ and b/
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--output-indicator-new # specify the character to indicate a new line instead of '+'
		--output-indicator-old # specify the character to indicate an old line instead of '-'
		--output-indicator-context # specify the character to indicate a context instead of ' '
		--find-renames|-M # detect renames
		--irreversible-delete|-D @bool # omit the preimage for deletes
		--find-copies|-C # detect copies
		--find-copies-harder @bool # use unmodified files as source to find copies
		--no-find-copies-harder @bool # opposite of --find-copies-harder
		--no-renames @bool # disable rename detection
		--rename-empty @bool # use empty blobs as rename source
		--no-rename-empty @bool # opposite of --rename-empty
		--follow @bool # continue listing the history of a file beyond renames
		--no-follow @bool # opposite of --follow
		--minimal @bool # produce the smallest possible diff
		--ignore-all-space|-w @bool # ignore whitespace when comparing lines
		--ignore-space-change|-b @bool # ignore changes in amount of whitespace
		--ignore-cr-at-eol @bool # ignore carrier-return at the end of line
		--ignore-blank-lines @bool # ignore changes whose lines are all blank
		--ignore-matching-lines|-I # ignore changes whose all lines match <regex>
		--no-ignore-matching-lines @bool # opposite of --ignore-matching-lines
		--indent-heuristic @bool # heuristic to shift diff hunk boundaries for easy reading
		--no-indent-heuristic @bool # opposite of --indent-heuristic
		--patience @bool # generate diff using the "patience diff" algorithm
		--histogram @bool # generate diff using the "histogram diff" algorithm
		--diff-algorithm # choose a diff algorithm
		--anchored # generate diff using the "anchored diff" algorithm
		--word-diff # show word diff, using <mode> to delimit changed words
		--word-diff-regex # use <regex> to decide what a word is
		--color-words # equivalent to --word-diff=color --word-diff-regex=<regex>
		--color-moved # moved lines of code are colored differently
		--no-color-moved @bool # opposite of --color-moved
		--color-moved-ws # how white spaces are ignored in --color-moved
		--no-color-moved-ws @bool # opposite of --color-moved-ws
		--relative # when run from subdir, exclude changes outside and show relative paths
		--no-relative @bool # opposite of --relative
		--text|-a @bool # treat all files as text
		--no-text @bool # opposite of --text
		--exit-code @bool # exit with 1 if there were differences, 0 otherwise
		--no-exit-code @bool # opposite of --exit-code
		--quiet @bool # disable all output of the program
		--no-quiet @bool # opposite of --quiet
		--ext-diff @bool # allow an external diff helper to be executed
		--no-ext-diff @bool # opposite of --ext-diff
		--textconv @bool # run external text conversion filters when comparing binary files
		--no-textconv @bool # opposite of --textconv
		--ignore-submodules # ignore changes to submodules in the diff generation
		--submodule # specify how differences in submodules are shown
		--ita-invisible-in-index @bool # hide 'git add -N' entries from the index
		--ita-visible-in-index @bool # treat 'git add -N' entries as real in the index
		--pickaxe-all @bool # show all changes in the changeset with -S or -G
		--pickaxe-regex @bool # treat <string> in -S as extended POSIX regular expression
		--rotate-to # show the change in the specified path first
		--skip-to # skip the output to the specified path
		--find-object # look for differences that change the number of occurrences of the specified object
		--max-depth # maximum tree depth to recurse
		--output # output to a specific file
	read-tree # Reads tree information into the index
		--empty @bool # only empty the index
		--no-empty @bool # opposite of --empty
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--trivial @bool # 3-way merge if no file level merging required
		--no-trivial @bool # opposite of --trivial
		--aggressive @bool # 3-way merge in presence of adds and removes
		--no-aggressive @bool # opposite of --aggressive
		--reset @bool # same as -m, but discard unmerged entries
		--no-reset @bool # opposite of --reset
		--exclude-per-directory # allow explicitly ignored files to be overwritten
		--dry-run|-n @bool # don't update the index or the work tree
		--no-dry-run @bool # opposite of --dry-run
		--no-sparse-checkout @bool # skip applying sparse checkout filter
		--sparse-checkout @bool # opposite of --no-sparse-checkout
		--debug-unpack @bool # debug unpack-trees
		--no-debug-unpack @bool # opposite of --debug-unpack
		--recurse-submodules # control recursive updating of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--quiet|-q @bool # suppress feedback messages
		--no-quiet @bool # opposite of --quiet
	rebase # Reapply commits on top of another base tip
		--onto # rebase onto given branch instead of upstream
		--no-onto @bool # opposite of --onto
		--keep-base @bool # use the merge-base of upstream and branch as the current base
		--no-keep-base @bool # opposite of --keep-base
		--no-verify @bool # allow pre-rebase hook to run
		--verify @bool # opposite of --no-verify
		--quiet|-q @bool # be quiet. implies --no-stat
		--no-quiet @bool # opposite of --quiet
		--verbose|-v @bool # display a diffstat of what changed upstream
		--no-verbose @bool # opposite of --verbose
		--no-stat|-n @bool # do not show diffstat of what changed upstream
		--stat @bool # opposite of --no-stat
		--trailer # add custom trailer(s)
		--no-trailer @bool # opposite of --trailer
		--signoff @bool # add a Signed-off-by trailer to each commit
		--no-signoff @bool # opposite of --signoff
		--committer-date-is-author-date @bool # make committer date match author date
		--no-committer-date-is-author-date @bool # opposite of --committer-date-is-author-date
		--reset-author-date @bool # ignore author date and use current date
		--no-reset-author-date @bool # opposite of --reset-author-date
		--ignore-whitespace @bool # ignore changes in whitespace
		--no-ignore-whitespace @bool # opposite of --ignore-whitespace
		--whitespace # passed to 'git apply'
		--no-whitespace @bool # opposite of --whitespace
		--force-rebase|-f @bool # cherry-pick all commits, even if unchanged
		--no-force-rebase @bool # opposite of --force-rebase
		--no-ff @bool # cherry-pick all commits, even if unchanged
		--ff @bool # opposite of --no-ff
		--continue @bool # continue
		--skip @bool # skip current patch and continue
		--abort @bool # abort and check out the original branch
		--quit @bool # abort but keep HEAD where it is
		--edit-todo @bool # edit the todo list during an interactive rebase
		--show-current-patch @bool # show the patch file being applied or merged
		--apply @bool # use apply strategies to rebase
		--merge|-m @bool # use merging strategies to rebase
		--interactive|-i @bool # let the user edit the list of commits to rebase
		--rerere-autoupdate @bool # update the index with reused conflict resolution if possible
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
		--empty # how to handle commits that become empty
		--autosquash @bool # move commits that begin with squash!/fixup! under -i
		--no-autosquash @bool # opposite of --autosquash
		--update-refs @bool # update branches that point to commits that are being rebased
		--no-update-refs @bool # opposite of --update-refs
		--gpg-sign|-S # GPG-sign commits
		--no-gpg-sign @bool # opposite of --gpg-sign
		--autostash @bool # automatically stash/stash pop before and after
		--no-autostash @bool # opposite of --autostash
		--exec|-x # add exec lines after each commit of the editable list
		--no-exec @bool # opposite of --exec
		--rebase-merges|-r # try to rebase merges instead of skipping them
		--no-rebase-merges @bool # opposite of --rebase-merges
		--fork-point @bool # use 'merge-base --fork-point' to refine upstream
		--no-fork-point @bool # opposite of --fork-point
		--strategy|-s # use the given merge strategy
		--no-strategy @bool # opposite of --strategy
		--strategy-option|-X # pass the argument through to the merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
		--root @bool # rebase all reachable commits up to the root(s)
		--no-root @bool # opposite of --root
		--reschedule-failed-exec @bool # automatically re-schedule any `exec` that fails
		--no-reschedule-failed-exec @bool # opposite of --reschedule-failed-exec
		--reapply-cherry-picks @bool # apply all changes, even those already present upstream
		--no-reapply-cherry-picks @bool # opposite of --reapply-cherry-picks
	reflog # Manage reflog information
	refs # Low-level access to refs
	remote # Manage set of tracked repositories
		--verbose|-v @bool # be verbose; must be placed before a subcommand
		--no-verbose @bool # opposite of --verbose
	repack # Pack unpacked objects in a repository
		--cruft @bool # same as -a, pack unreachable cruft objects separately
		--no-cruft @bool # opposite of --cruft
		--cruft-expiration # with --cruft, expire objects older than this
		--no-cruft-expiration @bool # opposite of --cruft-expiration
		--combine-cruft-below-size # with --cruft, only repack cruft packs smaller than this
		--max-cruft-size # with --cruft, limit the size of new cruft packs
		--name-hash-version # specify the name hash version to use for grouping similar objects by path
		--no-name-hash-version @bool # opposite of --name-hash-version
		--path-walk @bool # pass --path-walk to git-pack-objects
		--no-path-walk @bool # opposite of --path-walk
		--quiet|-q @bool # be quiet
		--no-quiet @bool # opposite of --quiet
		--local|-l @bool # pass --local to git-pack-objects
		--no-local @bool # opposite of --local
		--write-bitmap-index|-b @bool # write bitmap index
		--no-write-bitmap-index @bool # opposite of --write-bitmap-index
		--delta-islands|-i @bool # pass --delta-islands to git-pack-objects
		--no-delta-islands @bool # opposite of --delta-islands
		--unpack-unreachable # with -A, do not loosen objects older than this
		--no-unpack-unreachable @bool # opposite of --unpack-unreachable
		--keep-unreachable|-k @bool # with -a, repack unreachable objects
		--no-keep-unreachable @bool # opposite of --keep-unreachable
		--window # size of the window used for delta compression
		--no-window @bool # opposite of --window
		--window-memory # same as the above, but limit memory size instead of entries count
		--no-window-memory @bool # opposite of --window-memory
		--depth # limits the maximum delta depth
		--no-depth @bool # opposite of --depth
		--threads # limits the maximum number of threads
		--no-threads @bool # opposite of --threads
		--max-pack-size # maximum size of each packfile
		--filter # object filtering
		--no-filter @bool # opposite of --filter
		--pack-kept-objects @bool # repack objects in packs marked with .keep
		--no-pack-kept-objects @bool # opposite of --pack-kept-objects
		--keep-pack # do not repack this pack
		--no-keep-pack @bool # opposite of --keep-pack
		--geometric|-g # find a geometric progression with factor <N>
		--no-geometric @bool # opposite of --geometric
		--expire-to # pack prefix to store a pack containing pruned objects
		--no-expire-to @bool # opposite of --expire-to
		--filter-to # pack prefix to store a pack containing filtered out objects
		--no-filter-to @bool # opposite of --filter-to
	replace # Create, list, delete refs to replace objects
		--list|-l @bool # list replace refs
		--delete|-d @bool # delete replace refs
		--edit|-e @bool # edit existing object
		--graft|-g @bool # change a commit's parents
		--convert-graft-file @bool # convert existing graft file
		--force|-f @bool # replace the ref if it exists
		--no-force @bool # opposite of --force
		--raw @bool # do not pretty-print contents for --edit
		--no-raw @bool # opposite of --raw
		--format # use this format
		--no-format @bool # opposite of --format
	replay # EXPERIMENTAL: Replay commits on a new base, works with bare repos too
		--contained @bool # update all branches that point at commits in <revision-range>
		--no-contained @bool # opposite of --contained
		--onto # replay onto given commit
		--advance # make replay advance given branch
		--revert # revert commits onto given branch
		--ref # reference to update with result
		--ref-action # control ref update behavior (update|print)
	repo # Retrieve information about the repository
	repository-layout # Git Repository Layout
	request-pull # Generates a summary of pending changes
	rerere # Reuse recorded resolution of conflicted merges
		--rerere-autoupdate @bool # register clean resolutions in index
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
	reset # Set `HEAD` or the index to a known state
		--quiet|-q @bool # be quiet, only report errors
		--no-quiet @bool # opposite of --quiet
		--no-refresh @bool # skip refreshing the index after reset
		--refresh @bool # opposite of --no-refresh
		--mixed @bool # reset HEAD and index
		--soft @bool # reset only HEAD
		--hard @bool # reset HEAD, index and working tree
		--merge @bool # reset HEAD, index and working tree
		--keep @bool # reset HEAD but keep local changes
		--recurse-submodules # control recursive updating of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--patch|-p @bool # select hunks interactively
		--no-patch @bool # opposite of --patch
		--auto-advance @bool # auto advance to the next file when selecting hunks interactively
		--no-auto-advance @bool # opposite of --auto-advance
		--unified|-U # generate diffs with <n> lines context
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--intent-to-add|-N @bool # record only the fact that removed paths will be added later
		--no-intent-to-add @bool # opposite of --intent-to-add
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	restore # Restore working tree files
		--source|-s # which tree-ish to checkout from
		--no-source @bool # opposite of --source
		--staged|-S @bool # restore the index
		--no-staged @bool # opposite of --staged
		--worktree|-W @bool # restore the working tree (default)
		--no-worktree @bool # opposite of --worktree
		--ignore-unmerged @bool # ignore unmerged entries
		--no-ignore-unmerged @bool # opposite of --ignore-unmerged
		--overlay @bool # use overlay mode
		--no-overlay @bool # opposite of --overlay
		--quiet|-q @bool # suppress progress reporting
		--no-quiet @bool # opposite of --quiet
		--recurse-submodules # control recursive updating of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--merge|-m @bool # perform a 3-way merge with the new branch
		--no-merge @bool # opposite of --merge
		--conflict # conflict style (merge, diff3, or zdiff3)
		--no-conflict @bool # opposite of --conflict
		--ours|-2 @bool # checkout our version for unmerged files
		--theirs|-3 @bool # checkout their version for unmerged files
		--patch|-p @bool # select hunks interactively
		--no-patch @bool # opposite of --patch
		--unified|-U # generate diffs with <n> lines context
		--inter-hunk-context # show context between diff hunks up to the specified number of lines
		--ignore-skip-worktree-bits @bool # do not limit pathspecs to sparse entries only
		--no-ignore-skip-worktree-bits @bool # opposite of --ignore-skip-worktree-bits
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	rev-list # Lists commit objects in reverse chronological order
		--max-count # --max-age=<epoch>
		--max-age # --min-age=<epoch>
		--min-age # --sparse
		--sparse @bool # --no-merges
		--no-merges @bool # --min-parents=<n>
		--min-parents # --no-min-parents
		--no-min-parents @bool # --max-parents=<n>
		--max-parents # --no-max-parents
		--no-max-parents @bool # --remove-empty
		--remove-empty @bool # --all
		--all @bool # --branches
		--branches @bool # --tags
		--tags @bool # --remotes
		--remotes @bool # --stdin
		--stdin @bool # --exclude-hidden=[fetch|receive|uploadpack]
		--exclude-hidden # --quiet
		--quiet @bool
		--topo-order @bool # --date-order
		--date-order @bool # --reverse
		--reverse @bool
		--parents @bool # --children
		--children @bool # --objects | --objects-edge
		--objects-edge @bool # --disk-usage[=human]
		--disk-usage # --unpacked
		--unpacked @bool # --header | --pretty
		--header @bool # --[no-]object-names
		--object-names @bool # --abbrev=<n> | --no-abbrev
		--no-object-names @bool # opposite of --object-names
		--abbrev-commit @bool # --left-right
		--left-right @bool # --count
		--count @bool # -z
		--bisect @bool # --bisect-vars
		--bisect-vars @bool # --bisect-all
		--bisect-all @bool
	rev-parse # Pick out and massage parameters
	revert # Revert some existing commits
		--quit @bool # end revert or cherry-pick sequence
		--continue @bool # resume revert or cherry-pick sequence
		--abort @bool # cancel revert or cherry-pick sequence
		--skip @bool # skip current commit and continue
		--no-commit|-n @bool # don't automatically commit
		--commit @bool # opposite of --no-commit
		--edit|-e @bool # edit the commit message
		--no-edit @bool # opposite of --edit
		--signoff|-s @bool # add a Signed-off-by trailer
		--no-signoff @bool # opposite of --signoff
		--mainline|-m # select mainline parent
		--no-mainline @bool # opposite of --mainline
		--rerere-autoupdate @bool # update the index with reused conflict resolution if possible
		--no-rerere-autoupdate @bool # opposite of --rerere-autoupdate
		--strategy # merge strategy
		--no-strategy @bool # opposite of --strategy
		--strategy-option|-X # option for merge strategy
		--no-strategy-option @bool # opposite of --strategy-option
		--gpg-sign|-S # GPG sign commit
		--no-gpg-sign @bool # opposite of --gpg-sign
		--reference @bool # use the 'reference' format to refer to commits
		--no-reference @bool # opposite of --reference
	revisions # Specifying revisions and ranges for Git
	rm # Remove files from the working tree and from the index
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--quiet|-q @bool # do not list removed files
		--no-quiet @bool # opposite of --quiet
		--cached @bool # only remove from the index
		--no-cached @bool # opposite of --cached
		--force|-f @bool # override the up-to-date check
		--no-force @bool # opposite of --force
		--sparse @bool # allow updating entries outside of the sparse-checkout cone
		--no-sparse @bool # opposite of --sparse
		--pathspec-from-file # read pathspec from file
		--no-pathspec-from-file @bool # opposite of --pathspec-from-file
		--pathspec-file-nul @bool # with --pathspec-from-file, pathspec elements are separated with NUL character
		--no-pathspec-file-nul @bool # opposite of --pathspec-file-nul
	scalar # A tool for managing large Git repositories
	send-email # Send a collection of patches as emails
		--from # * Email From:
		--to # * Email To:
		--no-to @bool # opposite of --to
		--cc # * Email Cc:
		--no-cc @bool # opposite of --cc
		--bcc # * Email Bcc:
		--no-bcc @bool # opposite of --bcc
		--subject # * Email "Subject:"
		--reply-to # * Email "Reply-To:"
		--in-reply-to # * Email "In-Reply-To:"
		--outlook-id-fix @bool # * The SMTP host is an Outlook server that munges the
		--no-outlook-id-fix @bool # opposite of --outlook-id-fix
		--xmailer @bool # * Add "X-Mailer:" header (default).
		--no-xmailer @bool # opposite of --xmailer
		--annotate @bool # * Review each patch that will be sent in an editor.
		--no-annotate @bool # opposite of --annotate
		--compose @bool # * Open an editor for introduction.
		--compose-encoding # * Encoding to assume for introduction.
		--8bit-encoding # * Encoding to assume 8bit mails if undeclared
		--transfer-encoding # * Transfer encoding to use (quoted-printable, 8bit, base64)
		--mailmap @bool # * Use mailmap file to map all email addresses to canonical
		--no-mailmap @bool # opposite of --mailmap
		--envelope-sender # * Email envelope sender.
		--sendmail-cmd # * Command to run to send email.
		--smtp-server # * Outgoing SMTP server to use. The port
		--smtp-server-option # * Outgoing SMTP server option to use.
		--smtp-server-port # * Outgoing SMTP server port.
		--smtp-user # * Username for SMTP-AUTH.
		--smtp-pass # * Password for SMTP-AUTH; not necessary.
		--smtp-encryption # * tls or ssl; anything else disables.
		--smtp-ssl @bool # * Deprecated. Use `--smtp-encryption ssl`.
		--smtp-ssl-cert-path # * Path to ca-certificates (either directory or file).
		--smtp-ssl-client-cert # * Path to the client certificate file
		--smtp-ssl-client-key # * Path to the private key file for the client certificate
		--smtp-domain # * The domain name sent to HELO/EHLO handshake
		--smtp-auth # * Space-separated list of allowed AUTH mechanisms, or
		--no-smtp-auth @bool # * Disable SMTP authentication. Shorthand for
		--smtp-debug # * Disable, enable Net::SMTP debug.
		--imap-sent-folder # * IMAP folder where a copy of the emails should be sent.
		--use-imap-only @bool # * Only copy emails to the IMAP folder specified by
		--no-use-imap-only @bool # opposite of --use-imap-only
		--batch-size # * send max <int> message per connection.
		--relogin-delay # * delay <int> seconds between two successive login.
		--identity # * Use the sendemail.<id> options.
		--to-cmd # * Email To: via `<str> $patch_path`.
		--cc-cmd # * Email Cc: via `<str> $patch_path`.
		--header-cmd # * Add headers via `<str> $patch_path`.
		--no-header-cmd @bool # * Disable any header command in use.
		--suppress-cc # * author, self, sob, cc, cccmd, body, bodycc, misc-by, all.
		--cc-cover @bool # * Email Cc: addresses in the cover letter.
		--no-cc-cover @bool # opposite of --cc-cover
		--to-cover @bool # * Email To: addresses in the cover letter.
		--no-to-cover @bool # opposite of --to-cover
		--signed-off-by-cc @bool # * Send to Signed-off-by: addresses. Default on.
		--no-signed-off-by-cc @bool # opposite of --signed-off-by-cc
		--suppress-from @bool # * Send to self. Default off.
		--no-suppress-from @bool # opposite of --suppress-from
		--chain-reply-to @bool # * Chain In-Reply-To: fields. Default off.
		--no-chain-reply-to @bool # opposite of --chain-reply-to
		--thread @bool # * Use In-Reply-To: field. Default on.
		--no-thread @bool # opposite of --thread
		--confirm # * Confirm recipients before sending;
		--quiet @bool # * Output one line of info per email.
		--dry-run @bool # * Don't actually send the emails.
		--validate @bool # * Perform patch sanity checks. Default on.
		--no-validate @bool # opposite of --validate
		--format-patch @bool # * understand any non optional arguments as
		--no-format-patch @bool # opposite of --format-patch
		--force @bool # * Send even if safety checks would prevent it.
		--dump-aliases @bool # * Dump configured aliases and exit.
		--translate-aliases @bool # * Translate aliases read from standard
	send-pack # Push objects over Git protocol to another repository
		--verbose|-v @bool # be more verbose
		--no-verbose @bool # opposite of --verbose
		--quiet|-q @bool # be more quiet
		--no-quiet @bool # opposite of --quiet
		--receive-pack # receive pack program
		--no-receive-pack @bool # opposite of --receive-pack
		--exec # receive pack program
		--no-exec @bool # opposite of --exec
		--remote # remote name
		--no-remote @bool # opposite of --remote
		--all @bool # push all refs
		--no-all @bool # opposite of --all
		--dry-run|-n @bool # dry run
		--no-dry-run @bool # opposite of --dry-run
		--mirror @bool # mirror all refs
		--no-mirror @bool # opposite of --mirror
		--force|-f @bool # force updates
		--no-force @bool # opposite of --force
		--signed # GPG sign the push
		--no-signed @bool # opposite of --signed
		--push-option # option to transmit
		--no-push-option @bool # opposite of --push-option
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--thin @bool # use thin pack
		--no-thin @bool # opposite of --thin
		--atomic @bool # request atomic transaction on remote side
		--no-atomic @bool # opposite of --atomic
		--stateless-rpc @bool # use stateless RPC protocol
		--no-stateless-rpc @bool # opposite of --stateless-rpc
		--stdin @bool # read refs from stdin
		--no-stdin @bool # opposite of --stdin
		--helper-status @bool # print status from remote helper
		--no-helper-status @bool # opposite of --helper-status
		--force-if-includes @bool # require remote updates to be integrated locally
		--no-force-if-includes @bool # opposite of --force-if-includes
	sh-i18n # Git's i18n setup code for shell scripts
	sh-setup # Common Git shell script setup code
	shortlog # Summarize 'git log' output
		--committer|-c @bool # group by committer rather than author
		--no-committer @bool # opposite of --committer
		--numbered|-n @bool # sort output according to the number of commits per author
		--no-numbered @bool # opposite of --numbered
		--summary|-s @bool # suppress commit descriptions, only provides commit count
		--no-summary @bool # opposite of --summary
		--email|-e @bool # show the email address of each author
		--no-email @bool # opposite of --email
		--group # group by field
		--no-group @bool # opposite of --group
	show # Show various types of objects
		--quiet|-q @bool # suppress diff output
		--no-quiet @bool # opposite of --quiet
		--source @bool # show source
		--no-source @bool # opposite of --source
		--use-mailmap @bool # use mail map file
		--no-use-mailmap @bool # opposite of --use-mailmap
		--mailmap @bool # alias of --use-mailmap
		--no-mailmap @bool # opposite of --mailmap
		--clear-decorations @bool # clear all previously-defined decoration filters
		--decorate-refs # only decorate refs that match <pattern>
		--no-decorate-refs @bool # opposite of --decorate-refs
		--decorate-refs-exclude # do not decorate refs that match <pattern>
		--no-decorate-refs-exclude @bool # opposite of --decorate-refs-exclude
	show-branch # Show branches and their commits
		--all|-a @bool # show remote-tracking and local branches
		--no-all @bool # opposite of --all
		--remotes|-r @bool # show remote-tracking branches
		--no-remotes @bool # opposite of --remotes
		--more # show <n> more commits after the common ancestor
		--no-more @bool # opposite of --more
		--list @bool # synonym to more=-1
		--no-list @bool # opposite of --list
		--no-name @bool # suppress naming strings
		--name @bool # opposite of --no-name
		--current @bool # include the current branch
		--no-current @bool # opposite of --current
		--sha1-name @bool # name commits with their object names
		--no-sha1-name @bool # opposite of --sha1-name
		--merge-base @bool # show possible merge bases
		--no-merge-base @bool # opposite of --merge-base
		--independent @bool # show refs unreachable from any other ref
		--no-independent @bool # opposite of --independent
		--topo-order @bool # show commits in topological order
		--topics @bool # show only commits not on the first branch
		--no-topics @bool # opposite of --topics
		--sparse @bool # show merges reachable from only one tip
		--no-sparse @bool # opposite of --sparse
		--date-order @bool # topologically sort, maintaining date order where possible
	show-index # Show packed archive index
		--object-format # specify the hash algorithm to use
		--no-object-format @bool # opposite of --object-format
	show-ref # List references in a local repository
		--tags @bool # only show tags (can be combined with --branches)
		--no-tags @bool # opposite of --tags
		--branches @bool # only show branches (can be combined with --tags)
		--no-branches @bool # opposite of --branches
		--exists @bool # check for reference existence without resolving
		--no-exists @bool # opposite of --exists
		--verify @bool # stricter reference checking, requires exact ref path
		--no-verify @bool # opposite of --verify
		--head @bool # show the HEAD reference, even if it would be filtered out
		--no-head @bool # opposite of --head
		--dereference|-d @bool # dereference tags into object IDs
		--no-dereference @bool # opposite of --dereference
		--abbrev # use <n> digits to display object names
		--no-abbrev @bool # opposite of --abbrev
		--quiet|-q @bool # do not print results to stdout (useful with --verify)
		--no-quiet @bool # opposite of --quiet
		--exclude-existing # show refs from stdin that aren't in local repository
	sparse-checkout # Reduce your working tree to a subset of tracked files
	stash # Stash the changes in a dirty working directory away
	status # Show the working tree status
		--verbose|-v @bool # be verbose
		--no-verbose @bool # opposite of --verbose
		--short|-s @bool # show status concisely
		--no-short @bool # opposite of --short
		--branch|-b @bool # show branch information
		--no-branch @bool # opposite of --branch
		--show-stash @bool # show stash information
		--no-show-stash @bool # opposite of --show-stash
		--ahead-behind @bool # compute full ahead/behind values
		--no-ahead-behind @bool # opposite of --ahead-behind
		--porcelain # machine-readable output
		--no-porcelain @bool # opposite of --porcelain
		--long @bool # show status in long format (default)
		--no-long @bool # opposite of --long
		--null|-z @bool # terminate entries with NUL
		--no-null @bool # opposite of --null
		--untracked-files|-u # show untracked files, optional modes: all, normal, no. (Default: all)
		--no-untracked-files @bool # opposite of --untracked-files
		--ignored # show ignored files, optional modes: traditional, matching, no. (Default: traditional)
		--no-ignored @bool # opposite of --ignored
		--ignore-submodules # ignore changes to submodules, optional when: all, dirty, untracked. (Default: all)
		--no-ignore-submodules @bool # opposite of --ignore-submodules
		--column # list untracked files in columns
		--no-column @bool # opposite of --column
		--no-renames @bool # do not detect renames
		--renames @bool # opposite of --no-renames
		--find-renames|-M # detect renames, optionally set similarity index
	stripspace # Remove unnecessary whitespace
		--strip-comments|-s @bool # skip and remove all lines starting with comment character
		--comment-lines|-c @bool # prepend comment character and space to each line
	submodule # Initialize, update or inspect submodules
	svn # Bidirectional operation between a Subversion repository and Git
	switch # Switch branches
		--create|-c # create and switch to a new branch
		--no-create @bool # opposite of --create
		--force-create|-C # create/reset and switch to a branch
		--no-force-create @bool # opposite of --force-create
		--guess @bool # second guess 'git switch <no-such-branch>'
		--no-guess @bool # opposite of --guess
		--discard-changes @bool # throw away local modifications
		--no-discard-changes @bool # opposite of --discard-changes
		--quiet|-q @bool # suppress progress reporting
		--no-quiet @bool # opposite of --quiet
		--recurse-submodules # control recursive updating of submodules
		--no-recurse-submodules @bool # opposite of --recurse-submodules
		--progress @bool # force progress reporting
		--no-progress @bool # opposite of --progress
		--merge|-m @bool # perform a 3-way merge with the new branch
		--no-merge @bool # opposite of --merge
		--conflict # conflict style (merge, diff3, or zdiff3)
		--no-conflict @bool # opposite of --conflict
		--detach|-d @bool # detach HEAD at named commit
		--no-detach @bool # opposite of --detach
		--track|-t # set branch tracking configuration
		--no-track @bool # opposite of --track
		--force|-f @bool # force checkout (throw away local modifications)
		--no-force @bool # opposite of --force
		--orphan # new unborn branch
		--no-orphan @bool # opposite of --orphan
		--overwrite-ignore @bool # update ignored files (default)
		--no-overwrite-ignore @bool # opposite of --overwrite-ignore
		--ignore-other-worktrees @bool # do not check if another worktree is using this branch
		--no-ignore-other-worktrees @bool # opposite of --ignore-other-worktrees
	symbolic-ref # Read, modify and delete symbolic refs
		--quiet|-q @bool # suppress error message for non-symbolic (detached) refs
		--no-quiet @bool # opposite of --quiet
		--delete|-d @bool # delete symbolic ref
		--no-delete @bool # opposite of --delete
		--short @bool # shorten ref output
		--no-short @bool # opposite of --short
		--recurse @bool # recursively dereference (default)
		--no-recurse @bool # opposite of --recurse
	tag # Create, list, delete or verify tags
		--list|-l @bool # list tag names
		--delete|-d @bool # delete tags
		--verify|-v @bool # verify tags
		--annotate|-a @bool # annotated tag, needs a message
		--no-annotate @bool # opposite of --annotate
		--message|-m # tag message
		--file|-F # read message from file
		--no-file @bool # opposite of --file
		--trailer # add custom trailer(s)
		--no-trailer @bool # opposite of --trailer
		--edit|-e @bool # force edit of tag message
		--no-edit @bool # opposite of --edit
		--sign|-s @bool # annotated and GPG-signed tag
		--no-sign @bool # opposite of --sign
		--local-user|-u # use another key to sign the tag
		--no-local-user @bool # opposite of --local-user
		--force|-f @bool # replace the tag if exists
		--no-force @bool # opposite of --force
		--create-reflog @bool # create a reflog
		--no-create-reflog @bool # opposite of --create-reflog
		--column # show tag list in columns
		--no-column @bool # opposite of --column
		--contains # print only tags that contain the commit
		--no-contains # print only tags that don't contain the commit
		--merged # print only tags that are merged
		--no-merged # print only tags that are not merged
		--omit-empty @bool # do not output a newline after empty formatted refs
		--no-omit-empty @bool # opposite of --omit-empty
		--sort # field name to sort on
		--no-sort @bool # opposite of --sort
		--points-at # print only tags of the object
		--no-points-at @bool # opposite of --points-at
		--format # format to use for the output
		--no-format @bool # opposite of --format
		--ignore-case|-i @bool # sorting and filtering are case insensitive
		--no-ignore-case @bool # opposite of --ignore-case
	unpack-file # Creates a temporary file with a blob's contents
	unpack-objects # Unpack objects from a packed archive
	update-index # Register file contents in the working tree to the index
		--ignore-submodules @bool # refresh: ignore submodules
		--no-ignore-submodules @bool # opposite of --ignore-submodules
		--add @bool # do not ignore new files
		--no-add @bool # opposite of --add
		--replace @bool # let files replace directories and vice-versa
		--no-replace @bool # opposite of --replace
		--remove @bool # notice files missing from worktree
		--no-remove @bool # opposite of --remove
		--unmerged @bool # refresh even if index contains unmerged entries
		--no-unmerged @bool # opposite of --unmerged
		--refresh @bool # refresh stat information
		--really-refresh @bool # like --refresh, but ignore assume-unchanged setting
		--chmod # override the executable bit of the listed files
		--assume-unchanged @bool # mark files as "not changing"
		--skip-worktree @bool # mark files as "index-only"
		--no-skip-worktree @bool # clear skip-worktree bit
		--ignore-skip-worktree-entries @bool # do not touch index-only entries
		--no-ignore-skip-worktree-entries @bool # opposite of --ignore-skip-worktree-entries
		--info-only @bool # add to index only; do not add content to object database
		--no-info-only @bool # opposite of --info-only
		--force-remove @bool # remove named paths even if present in worktree
		--no-force-remove @bool # opposite of --force-remove
		--stdin @bool # read list of paths to be updated from standard input
		--index-info @bool # add entries from standard input to the index
		--unresolve @bool # repopulate stages #2 and #3 for the listed paths
		--again|-g @bool # only update entries that differ from HEAD
		--verbose @bool # report actions to standard output
		--no-verbose @bool # opposite of --verbose
		--clear-resolve-undo @bool # (for porcelains) forget saved unresolved conflicts
		--index-version # write index in this format
		--no-index-version @bool # opposite of --index-version
		--show-index-version @bool # report on-disk index format version
		--no-show-index-version @bool # opposite of --show-index-version
		--split-index @bool # enable or disable split index
		--no-split-index @bool # opposite of --split-index
		--untracked-cache @bool # enable/disable untracked cache
		--no-untracked-cache @bool # opposite of --untracked-cache
		--test-untracked-cache @bool # test if the filesystem supports untracked cache
		--no-test-untracked-cache @bool # opposite of --test-untracked-cache
		--force-untracked-cache @bool # enable untracked cache without testing the filesystem
		--no-force-untracked-cache @bool # opposite of --force-untracked-cache
		--force-write-index @bool # write out the index even if is not flagged as changed
		--no-force-write-index @bool # opposite of --force-write-index
		--fsmonitor @bool # enable or disable file system monitor
		--no-fsmonitor @bool # opposite of --fsmonitor
		--fsmonitor-valid @bool # mark files as fsmonitor valid
		--no-fsmonitor-valid @bool # clear fsmonitor valid bit
	update-ref # Update the object name stored in a ref safely
		--no-deref @bool # update <refname> not the one it points to
		--deref @bool # opposite of --no-deref
		--stdin @bool # read updates from stdin
		--no-stdin @bool # opposite of --stdin
		--create-reflog @bool # create a reflog
		--no-create-reflog @bool # opposite of --create-reflog
		--batch-updates|-0 @bool # batch reference updates
		--no-batch-updates @bool # opposite of --batch-updates
	update-server-info # Update auxiliary info file to help dumb servers
		--force|-f @bool # update the info files from scratch
		--no-force @bool # opposite of --force
	var # Show a Git logical variable
	verify-commit # Check the GPG signature of commits
		--verbose|-v @bool # print commit contents
		--no-verbose @bool # opposite of --verbose
		--raw @bool # print raw gpg status output
		--no-raw @bool # opposite of --raw
	verify-pack # Validate packed Git archive files
		--verbose|-v @bool # verbose
		--no-verbose @bool # opposite of --verbose
		--stat-only|-s @bool # show statistics only
		--no-stat-only @bool # opposite of --stat-only
		--object-format # specify the hash algorithm to use
		--no-object-format @bool # opposite of --object-format
	verify-tag # Check the GPG signature of tags
		--verbose|-v @bool # print tag contents
		--no-verbose @bool # opposite of --verbose
		--raw @bool # print raw gpg status output
		--no-raw @bool # opposite of --raw
		--format # format to use for the output
		--no-format @bool # opposite of --format
	version # Display version information about Git
		--build-options @bool # also print build options
		--no-build-options @bool # opposite of --build-options
	whatchanged # Show logs with differences each commit introduces
		--quiet|-q @bool # suppress diff output
		--no-quiet @bool # opposite of --quiet
		--source @bool # show source
		--no-source @bool # opposite of --source
		--use-mailmap @bool # use mail map file
		--no-use-mailmap @bool # opposite of --use-mailmap
		--mailmap @bool # alias of --use-mailmap
		--no-mailmap @bool # opposite of --mailmap
		--clear-decorations @bool # clear all previously-defined decoration filters
		--decorate-refs # only decorate refs that match <pattern>
		--no-decorate-refs @bool # opposite of --decorate-refs
		--decorate-refs-exclude # do not decorate refs that match <pattern>
		--no-decorate-refs-exclude @bool # opposite of --decorate-refs-exclude
	worktree # Manage multiple working trees
	write-tree # Create a tree object from the current index
		--missing-ok @bool # allow missing objects
		--no-missing-ok @bool # opposite of --missing-ok
