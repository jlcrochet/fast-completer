[project]
name = "oc-schema-export"
version = "0.1.0"
description = "Export OpenShift CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-oc = "export_command_tree:main"
