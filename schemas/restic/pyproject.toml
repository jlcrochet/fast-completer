[project]
name = "restic-schema-export"
version = "0.1.0"
description = "Export restic command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-restic = "export_command_tree:main"
