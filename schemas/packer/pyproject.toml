[project]
name = "packer-schema-export"
version = "0.1.0"
description = "Export Packer command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-packer = "export_command_tree:main"
