[project]
name = "wrangler-schema-export"
version = "0.1.0"
description = "Export Wrangler command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-wrangler = "export_command_tree:main"
