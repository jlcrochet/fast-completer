[project]
name = "node-schema-export"
version = "0.1.0"
description = "Export Node command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-node = "export_command_tree:main"
