[project]
name = "copilot-schema-export"
version = "0.1.0"
description = "Export Copilot command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-copilot = "export_command_tree:main"
