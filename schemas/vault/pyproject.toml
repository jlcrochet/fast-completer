[project]
name = "vault-schema-export"
version = "0.1.0"
description = "Export Vault command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-vault = "export_command_tree:main"
