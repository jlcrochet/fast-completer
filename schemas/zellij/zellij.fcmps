# Zellij CLI schema for fast-completer
# Generated from zellij 0.43.1

zellij # Terminal multiplexer
	--config|-c # Change where zellij looks for the configuration file [env: ZELLIJ_CONFIG_FILE=]
	--config-dir # Change where zellij looks for the configuration directory [env: ZELLIJ_CONFIG_DIR=]
	--debug|-d @bool # Specify emitting additional debug information
	--data-dir # Change where zellij looks for plugins
	--layout|-l # Name of a predefined layout inside the layout directory or the path to a layout file if
	--max-panes # Maximum panes on screen, caution: opening more panes will close old ones
	--new-session-with-layout|-n # Name of a predefined layout inside the layout directory or the path to a layout file
	--session|-s # Specify name of a new session
	action # Send actions to a specific session [aliases: ac]
		change-floating-pane-coordinates
			--height # The height if the pane is floating as a bare integer (eg. 1) or
			--pane-id|-p # The pane_id of the floating pane, eg.  terminal_1, plugin_2 or 3
			--pinned # Whether to pin a floating pane so that it is always on top
			--width # The width if the pane is floating as a bare integer (eg. 1) or
			--x|-x # The x coordinates if the pane is floating as a bare integer (eg. 1)
			--y|-y # The y coordinates if the pane is floating as a bare integer (eg. 1)
		clear
		close-pane
		close-tab
		dump-layout
		dump-screen
			--full|-f @bool # Dump the pane with full scrollback
		edit
			--cwd # Change the working directory of the editor
			--direction|-d # Direction to open the new pane in
			--floating|-f @bool # Open the new pane in floating mode
			--height # The height if the pane is floating as a bare integer (eg. 1)
			--in-place|-i @bool # Open the new pane in place of the current pane, temporarily
			--line-number|-l # Open the file in the specified line number
			--pinned # Whether to pin a floating pane so that it is always on top
			--width # The width if the pane is floating as a bare integer (eg. 1)
			--x|-x # The x coordinates if the pane is floating as a bare integer
			--y|-y # The y coordinates if the pane is floating as a bare integer
		edit-scrollback
		focus-next-pane
		focus-previous-pane
		go-to-next-tab
		go-to-previous-tab
		go-to-tab
		go-to-tab-name
			--create|-c @bool # Create a tab if one does not exist
		half-page-scroll-down
		half-page-scroll-up
		launch-or-focus-plugin
			--configuration|-c
			--floating|-f @bool
			--in-place|-i @bool
			--move-to-focused-tab|-m @bool
			--skip-plugin-cache|-s @bool
		launch-plugin
			--configuration|-c
			--floating|-f @bool
			--in-place|-i @bool
			--skip-plugin-cache|-s @bool
		list-clients
		move-focus
		move-focus-or-tab
		move-pane
		move-pane-backwards
		move-tab
		new-pane
			--close-on-exit|-c @bool # Close the pane immediately when its command exits
			--configuration
			--cwd # Change the working directory of the new pane
			--direction|-d # Direction to open the new pane in
			--floating|-f @bool # Open the new pane in floating mode
			--height # The height if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
			--in-place|-i @bool # Open the new pane in place of the current pane, temporarily suspending it
			--name|-n # Name of the new pane
			--plugin|-p
			--pinned # Whether to pin a floating pane so that it is always on top
			--start-suspended|-s @bool # Start the command suspended, only running it after the you first press ENTER
			--skip-plugin-cache @bool
			--stacked @bool
			--width # The width if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
			--x|-x # The x coordinates if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
			--y|-y # The y coordinates if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
		new-tab
			--cwd|-c # Change the working directory of the new tab
			--layout|-l # Layout to use for the new tab
			--layout-dir # Default folder to look for layouts
			--name|-n # Name of the new tab
		next-swap-layout
		page-scroll-down
		page-scroll-up
		pipe
			--name|-n # The name of the pipe
			--args|-a # The args of the pipe
			--plugin|-p # The plugin url (eg. file:/tmp/my-plugin.wasm) to direct this pipe to, if not specified,
			--plugin-configuration|-c # The plugin configuration (note: the same plugin with different configuration is
			--force-launch-plugin|-l @bool # Launch a new plugin even if one is already running
			--skip-plugin-cache|-s @bool # If launching a new plugin, skip cache and force-compile the plugin
			--floating-plugin|-f # If launching a plugin, should it be floating or not, defaults to floating [possible
			--in-place-plugin|-i # If launching a plugin, launch it in-place (on top of the current pane) [possible values:
			--plugin-cwd|-w # If launching a plugin, specify its working directory
			--plugin-title|-t # If launching a plugin, specify its pane title
		previous-swap-layout
		query-tab-names
		rename-pane
		rename-session
		rename-tab
		resize
		scroll-down
		scroll-to-bottom
		scroll-to-top
		scroll-up
		stack-panes
		start-or-reload-plugin
			--configuration|-c
		switch-mode
		toggle-active-sync-tab
		toggle-floating-panes
		toggle-fullscreen
		toggle-pane-embed-or-floating
		toggle-pane-frames
		toggle-pane-pinned
		undo-rename-pane
		undo-rename-tab
		write
		write-chars
	attach # Attach to a session [aliases: a]
		--create-background|-b @bool # Create a detached session in the background if one does not exist
		--create|-c @bool # Create a session if one does not exist
		--force-run-commands|-f @bool # If resurrecting a dead session, immediately run all its commands
		--index # Number of the session index in the active sessions ordered creation
		options
			--advanced-mouse-actions # Whether to enable mouse hover effects and pane grouping functionality default is true
			--attach-to-session # Whether to attach to a session specified in "session-name" if it exists [possible
			--auto-layout # Whether to lay out panes in a predefined set of layouts whenever possible [possible
			--copy-clipboard (system|primary) # OSC52 destination clipboard [possible values: system, primary]
			--copy-command # Switch to using a user supplied command for clipboard instead of OSC52
			--copy-on-select (true|false) # Automatically copy when selecting text (true or false) [possible values: true, false]
			--default-cwd # Set the default cwd
			--default-layout # Set the default layout
			--default-mode # Set the default mode
			--default-shell # Set the default shell
			--disable-mouse-mode @bool # Disable handling of mouse events
			--disable-session-metadata (true|false) # If true, will disable writing session metadata to disk [possible values: true, false]
			--layout-dir # Set the layout_dir, defaults to subdirectory of config dir
			--mirror-session # Mirror session when multiple users are connected (true or false) [possible values: true,
			--mouse-mode # Set the handling of mouse events (true or false) Can be temporarily bypassed by the
			--no-pane-frames @bool # Disable display of pane frames
			--on-force-close # Set behaviour on force close (quit or detach)
			--pane-frames (true|false) # Set display of the pane frames (true or false) [possible values: true, false]
			--post-command-discovery-hook # A command to run after the discovery of running commands when serializing, for the
			--scroll-buffer-size
			--scrollback-editor # Explicit full path to open the scrollback editor (default is $EDITOR or $VISUAL)
			--scrollback-lines-to-serialize # Scrollback lines to serialize along with the pane viewport when serializing sessions, 0
			--serialization-interval # The interval at which to serialize sessions for resurrection (in seconds)
			--serialize-pane-viewport # Whether pane viewports are serialized along with the session, default is false [possible
			--session-name # The name of the session to create when starting Zellij
			--session-serialization # Whether sessions should be serialized to the HD so that they can be later resurrected,
			--show-release-notes # Whether to show release notes on first run of a new version default is true [possible
			--show-startup-tips # Whether to show startup tips when starting a new session default is true [possible
			--simplified-ui # Allow plugins to use a more simplified layout that is compatible with more fonts (true
			--stacked-resize # Whether to stack panes when resizing beyond a certain size default is true [possible
			--styled-underlines (true|false) # Whether to use ANSI styled underlines [possible values: true, false]
			--support-kitty-keyboard-protocol # Whether to enable support for the Kitty keyboard protocol (must also be supported by
			--theme # Set the default theme
			--theme-dir # Set the theme_dir, defaults to subdirectory of config dir
			--web-server # Whether to make sure a local web server is running when a new Zellij session starts.
			--web-sharing # Whether to allow new sessions to be shared through a local web server, assuming one
	convert-config
	convert-layout
	convert-theme
	delete-all-sessions # Delete all sessions [aliases: da]
		--force|-f @bool # Kill the sessions if they're running before deleting them
		--yes|-y @bool # Automatic yes to prompts
	delete-session # Delete a specific session [aliases: d]
		--force|-f @bool # Kill the session if it's running before deleting it
	edit # Edit file with default $EDITOR / $VISUAL [aliases: e]
		--cwd # Change the working directory of the editor
		--direction|-d # Direction to open the new pane in
		--floating|-f @bool # Open the new pane in floating mode
		--height # The height if the pane is floating as a bare integer (eg. 1)
		--in-place|-i @bool # Open the new pane in place of the current pane, temporarily
		--line-number|-l # Open the file in the specified line number
		--pinned # Whether to pin a floating pane so that it is always on top
		--width # The width if the pane is floating as a bare integer (eg. 1)
		--x|-x # The x coordinates if the pane is floating as a bare integer
		--y|-y # The y coordinates if the pane is floating as a bare integer
	kill-all-sessions # Kill all sessions [aliases: ka]
		--yes|-y @bool # Automatic yes to prompts
	kill-session # Kill a specific session [aliases: k]
	list-aliases # List existing plugin aliases [aliases: la]
	list-sessions # List active sessions [aliases: ls]
		--no-formatting|-n @bool # Do not add colors and formatting to the list (useful for parsing)
		--reverse|-r @bool # List the sessions in reverse order (default is ascending order)
		--short|-s @bool # Print just the session name
	options # Change the behaviour of zellij
		--advanced-mouse-actions # Whether to enable mouse hover effects and pane grouping functionality default is true
		--attach-to-session # Whether to attach to a session specified in "session-name" if it exists [possible
		--auto-layout # Whether to lay out panes in a predefined set of layouts whenever possible [possible
		--copy-clipboard (system|primary) # OSC52 destination clipboard [possible values: system, primary]
		--copy-command # Switch to using a user supplied command for clipboard instead of OSC52
		--copy-on-select (true|false) # Automatically copy when selecting text (true or false) [possible values: true, false]
		--default-cwd # Set the default cwd
		--default-layout # Set the default layout
		--default-mode # Set the default mode
		--default-shell # Set the default shell
		--disable-mouse-mode @bool # Disable handling of mouse events
		--disable-session-metadata (true|false) # If true, will disable writing session metadata to disk [possible values: true, false]
		--layout-dir # Set the layout_dir, defaults to subdirectory of config dir
		--mirror-session # Mirror session when multiple users are connected (true or false) [possible values: true,
		--mouse-mode # Set the handling of mouse events (true or false) Can be temporarily bypassed by the
		--no-pane-frames @bool # Disable display of pane frames
		--on-force-close # Set behaviour on force close (quit or detach)
		--pane-frames (true|false) # Set display of the pane frames (true or false) [possible values: true, false]
		--post-command-discovery-hook # A command to run after the discovery of running commands when serializing, for the
		--scroll-buffer-size
		--scrollback-editor # Explicit full path to open the scrollback editor (default is $EDITOR or $VISUAL)
		--scrollback-lines-to-serialize # Scrollback lines to serialize along with the pane viewport when serializing sessions, 0
		--serialization-interval # The interval at which to serialize sessions for resurrection (in seconds)
		--serialize-pane-viewport # Whether pane viewports are serialized along with the session, default is false [possible
		--session-name # The name of the session to create when starting Zellij
		--session-serialization # Whether sessions should be serialized to the HD so that they can be later resurrected,
		--show-release-notes # Whether to show release notes on first run of a new version default is true [possible
		--show-startup-tips # Whether to show startup tips when starting a new session default is true [possible
		--simplified-ui # Allow plugins to use a more simplified layout that is compatible with more fonts (true
		--stacked-resize # Whether to stack panes when resizing beyond a certain size default is true [possible
		--styled-underlines (true|false) # Whether to use ANSI styled underlines [possible values: true, false]
		--support-kitty-keyboard-protocol # Whether to enable support for the Kitty keyboard protocol (must also be supported by
		--theme # Set the default theme
		--theme-dir # Set the theme_dir, defaults to subdirectory of config dir
		--web-server # Whether to make sure a local web server is running when a new Zellij session starts.
		--web-sharing # Whether to allow new sessions to be shared through a local web server, assuming one
	pipe # Send data to one or more plugins, launch them if they are not running
		--name|-n # The name of the pipe
		--args|-a # The args of the pipe
		--plugin|-p # The plugin url (eg. file:/tmp/my-plugin.wasm) to direct this pipe to, if not specified,
		--plugin-configuration|-c # The plugin configuration (note: the same plugin with different configuration is
	plugin # Load a plugin [aliases: p]
		--configuration|-c # Plugin configuration
		--floating|-f @bool # Open the new pane in floating mode
		--height # The height if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
		--in-place|-i @bool # Open the new pane in place of the current pane, temporarily suspending it
		--pinned # Whether to pin a floating pane so that it is always on top
		--skip-plugin-cache|-s @bool # Skip the memory and HD cache and force recompile of the plugin (good for development)
		--width # The width if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
		--x|-x # The x coordinates if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
		--y|-y # The y coordinates if the pane is floating as a bare integer (eg. 1) or percent (eg. 10%)
	run # Run a command in a new pane [aliases: r]
		--close-on-exit|-c @bool # Close the pane immediately when its command exits
		--cwd # Change the working directory of the new pane
		--direction|-d # Direction to open the new pane in
		--floating|-f @bool # Open the new pane in floating mode
		--height # The height if the pane is floating as a bare integer (eg. 1) or
		--in-place|-i @bool # Open the new pane in place of the current pane, temporarily
		--name|-n # Name of the new pane
		--pinned # Whether to pin a floating pane so that it is always on top
		--start-suspended|-s @bool # Start the command suspended, only running after you first presses
		--stacked @bool
		--width # The width if the pane is floating as a bare integer (eg. 1) or
		--x|-x # The x coordinates if the pane is floating as a bare integer (eg.
		--y|-y # The y coordinates if the pane is floating as a bare integer (eg.
	setup # Setup zellij and check its configuration
		--check @bool # Checks the configuration of zellij and displays currently used directories
		--clean @bool # Disables loading of configuration file at default location, loads the defaults that
		--dump-config @bool # Dump the default configuration file to stdout
		--dump-layout # Dump specified layout to stdout
		--dump-swap-layout # Dump the specified swap layout file to stdout
		--generate-auto-start # Generates auto-start script for the specified shell
		--generate-completion # Generates completion for the specified shell
	web # Run a web server to serve terminal sessions
		--start @bool # Start the server (default unless other arguments are
		--stop @bool # Stop the server
		--status @bool # Get the server status
		--daemonize|-d @bool # Run the server in the background
		--create-token @bool # Create a login token for the web interface, will only be
		--revoke-all-tokens @bool # Revoke all login tokens
		--list-tokens @bool # List token names and their creation dates (cannot show actual
		--ip # The ip address to listen on locally for connections (defaults
		--port # The port to listen on locally for connections (defaults to
		--cert # The path to the SSL certificate (required if not listening
		--key # The path to the SSL key (required if not listening on
