# Dotnet CLI schema for fast-completer
# Generated from dotnet 10.0.103

dotnet # .NET CLI
	--additionalprobingpath # Path containing probing policy and assemblies to probe for.
	--additional-deps # Path to additional deps.json file.
	--depsfile @bool # Path to <application>.deps.json file.
	--fx-version # Version of the installed Shared Framework to use to run the application.
	--roll-forward # Roll forward to framework version  (LatestPatch, Minor, LatestMinor, Major, LatestMajor, Disable).
	--runtimeconfig @bool # Path to <application>.runtimeconfig.json file.
	--diagnostics|-d @bool # Enable diagnostic output.
	--info @bool # Display .NET information.
	--list-runtimes @bool # Display the installed runtimes.
	--list-sdks @bool # Display the installed SDKs.
	--version @bool # Display .NET SDK version in use.
	build # Build a .NET project.
		--use-current-runtime @bool # Use current runtime as the target
		--framework|-f # The target framework to build for. The
		--configuration|-c # The configuration to use for building
		--runtime|-r # The target runtime to build for.
		--version-suffix # Set the value of the $(VersionSuffix)
		--no-restore @bool # Do not restore the project before
		--interactive @bool # Allows the command to stop and wait for
		-verbosity|-v # Set the MSBuild verbosity level. Allowed
		--debug @bool # [default: False]
		--output|-o # The output directory to place built
		--artifacts-path # The artifacts path. All output from the
		--no-incremental @bool # Do not use incremental building.
		--no-dependencies @bool # Do not build project-to-project
		--nologo @bool # Do not display the startup banner or the
		--self-contained @bool # Publish the .NET runtime with your
		--no-self-contained @bool # Publish your application as a framework
		--arch|-a # The target architecture.
		--os # The target operating system.
		--disable-build-servers @bool # Force the command to ignore any
	build-server # Interact with servers started by a build.
		shutdown
			--msbuild @bool # Shut down the MSBuild build server. [default: False]
			--vbcscompiler @bool # Shut down the VB/C# compiler build server. [default: False]
			--razor @bool # Shut down the Razor build server. [default: False]
	clean # Clean build outputs of a .NET project.
		--framework|-f # The target framework to clean for. The
		--runtime|-r # The target runtime to clean for.
		--configuration|-c # The configuration to clean for. The
		--interactive @bool # Allows the command to stop and wait for
		--verbosity|-v # Set the MSBuild verbosity level. Allowed
		--output|-o # The directory containing the build
		--artifacts-path # The artifacts path. All output from the
		--nologo @bool # Do not display the startup banner or the
		--disable-build-servers @bool # Force the command to ignore any
	dev-certs # Create and manage development certificates.
		https
			--export-path @bool # Full path to the exported certificate
			--password|-p @bool # Password to use when exporting the certificate with the private key into a pfx file or to encrypt the Pem exported key
			--no-password @bool # Explicitly request that you don't use a password for the key when exporting a certificate to a PEM format
			--check|-c @bool # Check for the existence of the certificate but do not perform any action
			--clean @bool # Cleans all HTTPS development certificates from the machine.
			--import|-i @bool # Imports the provided HTTPS development certificate into the machine. All other HTTPS developer certificates will be cleared out
			--format @bool # Export the certificate in the given format. Valid values are Pfx and Pem. Pfx is the default.
			--trust|-t @bool # When not combined with the --check option, trusts the certificate on the current platform, creating one if necessary.
			--verbose|-v @bool # Display more debug information.
			--quiet|-q @bool # Display warnings and errors only.
			--check-trust-machine-readable @bool # Same as running --check --trust, but output the results in json.
	format # Apply style preferences to a project or solution.
		--diagnostics # A space separated list of diagnostic ids to use as a filter when fixing code style or 3rd party issues.
		--exclude-diagnostics # A space separated list of diagnostic ids to ignore when fixing code style or 3rd party issues.
		--severity # The severity of diagnostics to fix. Allowed values are info, warn, and error.
		--no-restore @bool # Doesn't execute an implicit restore before formatting.
		--verify-no-changes @bool # Verify no formatting changes would be performed. Terminates with a non-zero exit code if any files would have been formatted.
		--include # A list of relative file or folder paths to include in formatting. All files are formatted if empty.
		--exclude # A list of relative file or folder paths to exclude from formatting.
		--include-generated @bool # Format files generated by the SDK.
		--verbosity|-v # Set the verbosity level. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic]
		--binarylog # Log all project or solution load information to a binary log file.
		--report # Accepts a file path which if provided will produce a json report in the given directory.
		--version @bool # Show version information
	fsi # Start F# Interactive / execute F# scripts.
	msbuild # Run Microsoft Build Engine (MSBuild) commands.
		--disable-build-servers @bool # Force the command to ignore any persistent build
	new # Create a new .NET project or file.
		--output|-o # Location to place the generated output.
		--name|-n # The name for the output being created. If no name is
		--dry-run @bool # Displays a summary of what would happen if the given
		--force @bool # Forces content to be generated even if it would
		--no-update-check @bool # Disables checking for the template package updates
		--project # The project that should be used for context
		--verbosity|-v # Sets the verbosity level. Allowed values are
		--diagnostics|-d @bool # Enables diagnostic output. [default: False]
		update
			--interactive @bool # Allows the command to stop and wait for user
			--nuget-source @bool # Specifies a NuGet source to use.
			--check-only @bool # Only checks for updates and display the
			--verbosity|-v # Sets the verbosity level. Allowed values are
			--diagnostics|-d @bool # Enables diagnostic output. [default: False]
	nuget # Provides additional NuGet commands.
		--version @bool # Show version information
		add
			client-cert
				--package-source|-s @bool # Package source name.
				--path @bool # Path to certificate file.
				--password @bool # Password for the certificate, if needed.
				--store-password-in-clear-text @bool # Enables storing password for the certificate by disabling password encryption.
				--store-location @bool # Certificate store location (see docs).
				--store-name @bool # Certificate store name (see docs).
				--find-by @bool # Search method to find certificate in certificate store (see docs).
				--find-value @bool # Search the certificate store for the supplied value. Used with FindValue (see docs).
				--force|-f @bool # Skip certificate validation.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			source
				--name|-n @bool # Name of the source.
				--username|-u @bool # Username to be used when connecting to an authenticated source.
				--password|-p @bool # Password to be used when connecting to an authenticated source.
				--store-password-in-clear-text @bool # Enables storing portable package source credentials by disabling password encryption.
				--valid-authentication-types @bool # Comma-separated list of valid authentication types for this source. Set this to basic if the server advertises NTLM or Negotiate and your credentials must be sent using the Basic mechanism, for instance when using a PAT with on-premises Azure DevOps Server. Other valid values include negotiate, kerberos, ntlm, and digest, but these values are unlikely to be useful.
				--protocol-version @bool # The NuGet server protocol version to be used. Currently supported versions are 2 and 3. See https://learn.microsoft.com/nuget/api/overview for information about the version 3 protocol. Defaults to 2 if not specified.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
				--allow-insecure-connections @bool # Allows HTTP connections for adding or updating packages. Note: This method is not secure. For secure options, see https://aka.ms/nuget-https-everywhere for more information.
		config
			paths
				--working-directory # Specifies the directory to start from when listing configuration files. If not specified, the current directory is used.
		delete
			--force-english-output @bool # Forces the application to run using an invariant, English-based culture.
			--source|-s # Package source (URL, UNC/folder path or package source name) to use. Defaults to DefaultPushSource if specified in NuGet.Config.
			--non-interactive @bool # Do not prompt for user input or confirmations.
			--api-key|-k # The API key for the server.
			--no-service-endpoint @bool # Does not append "api/v2/package" to the source URL.
			--interactive @bool # Allow the command to block and require manual action for operations like authentication.
		disable
			source
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
		enable
			source
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
		list
			client-cert
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			source
				--format @bool # The format of the list command output: `Detailed` (the default) and `Short`.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
		locals
			--force-english-output @bool # Forces the application to run using an invariant, English-based culture.
			--clear|-c @bool # Clear the selected local resources or cache location(s).
			--list|-l @bool # List the selected local resources or cache location(s).
		push
			--force-english-output @bool # Forces the application to run using an invariant, English-based culture.
			--source|-s # Package source (URL, UNC/folder path or package source name) to use. Defaults to DefaultPushSource if specified in NuGet.Config.
			--allow-insecure-connections @bool # Allows pushing to HTTP sources (insecure).
			--symbol-source # Symbol server URL to use.
			--timeout|-t # Timeout for pushing to a server in seconds. Defaults to 300 seconds (5 minutes).
			--api-key|-k # The API key for the server.
			--symbol-api-key # The API key for the symbol server.
			--disable-buffering|-d @bool # Disable buffering when pushing to an HTTP(S) server to decrease memory usage.
			--no-symbols|-n @bool # If a symbols package exists, it will not be pushed to a symbols server.
			--no-service-endpoint @bool # Does not append "api/v2/package" to the source URL.
			--interactive @bool # Allow the command to block and require manual action for operations like authentication.
			--skip-duplicate @bool # If a package and version already exists, skip it and continue with the next package in the push, if any.
			--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
		remove
			client-cert
				--package-source|-s @bool # Package source name.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			source
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
		sign
			--output|-o @bool # Directory where the signed package(s) should be saved. By default the original package is overwritten by the signed package.
			--certificate-path @bool # File path to the certificate to be used while signing the package.
			--certificate-store-name @bool # Name of the X.509 certificate store to use to search for the certificate. Defaults to "My", the X.509 certificate store for personal certificates.
			--certificate-store-location @bool # Name of the X.509 certificate store use to search for the certificate. Defaults to "CurrentUser", the X.509 certificate store used by the current user.
			--certificate-subject-name @bool # Subject name of the certificate used to search a local certificate store for the certificate.
			--certificate-fingerprint @bool # SHA-256, SHA-384 or SHA-512 fingerprint of the certificate used to search a local certificate store for the certificate. The certificate store can be specified by --certificate-store-name and --certificate-store-location options.
			--certificate-password @bool # Password for the certificate, if needed.
			--hash-algorithm @bool # Hash algorithm to be used to sign the package. Defaults to SHA256.
			--timestamper @bool # URL to an RFC 3161 timestamping server.
			--timestamp-hash-algorithm @bool # Hash algorithm to be used by the RFC 3161 timestamp server. Defaults to SHA256.
			--overwrite @bool # Switch to indicate if the current signature should be overwritten. By default the command will fail if the package already has a signature.
			--verbosity|-v @bool # Set the verbosity level of the command. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic].
		trust
			--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			--verbosity|-v @bool # Set the verbosity level of the command. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic].
			author
				--allow-untrusted-root @bool # [default: False]
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
			certificate
				--allow-untrusted-root @bool # [default: False]
				--algorithm # [default: SHA256]
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed
			list
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
			remove
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
			repository
				--allow-untrusted-root @bool # [default: False]
				--owners
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
			source
				--owners
				--source-url
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
			sync
				--configfile
				--verbosity|-v # Set the MSBuild verbosity level. Allowed values
		update
			client-cert
				--package-source|-s @bool # Package source name.
				--path @bool # Path to certificate file.
				--password @bool # Password for the certificate, if needed.
				--store-password-in-clear-text @bool # Enables storing password for the certificate by disabling password encryption.
				--store-location @bool # Certificate store location (see docs).
				--store-name @bool # Certificate store name (see docs).
				--find-by @bool # Search method to find certificate in certificate store (see docs).
				--find-value @bool # Search the certificate store for the supplied value. Used with FindValue (see docs).
				--force|-f @bool # Skip certificate validation.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			source
				--source|-s @bool # Path to the package source.
				--username|-u @bool # Username to be used when connecting to an authenticated source.
				--password|-p @bool # Password to be used when connecting to an authenticated source.
				--store-password-in-clear-text @bool # Enables storing portable package source credentials by disabling password encryption.
				--valid-authentication-types @bool # Comma-separated list of valid authentication types for this source. Set this to basic if the server advertises NTLM or Negotiate and your credentials must be sent using the Basic mechanism, for instance when using a PAT with on-premises Azure DevOps Server. Other valid values include negotiate, kerberos, ntlm, and digest, but these values are unlikely to be useful.
				--protocol-version @bool # The NuGet server protocol version to be used. Currently supported versions are 2 and 3. See https://learn.microsoft.com/nuget/api/overview for information about the version 3 protocol. Defaults to 2 if not specified.
				--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
				--allow-insecure-connections @bool # Allows HTTP connections for adding or updating packages. Note: This method is not secure. For secure options, see https://aka.ms/nuget-https-everywhere for more information.
		verify
			--all @bool # Specifies that all verifications possible should be performed to the package(s).
			--certificate-fingerprint @bool # Verify that the signer certificate matches with one of the specified SHA256 fingerprints. A certificate SHA256 fingerprint is a SHA256 hash of the certificate used to identify the certificate. Multiple inputs should be separated by space.
			--configfile @bool # The NuGet configuration file. If specified, only the settings from this file will be used. If not specified, the hierarchy of configuration files from the current directory will be used. For more information, see https://docs.microsoft.com/nuget/consume-packages/configuring-nuget-behavior.
			--verbosity|-v @bool # Set the verbosity level of the command. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic].
		why
			--framework|-f # The target framework(s) for which dependency graphs are shown.
	pack # Create a NuGet package.
		--output|-o # The output directory to place built
		--artifacts-path # The artifacts path. All output from the
		--no-build @bool # Do not build the project before packing.
		--include-symbols @bool # Include packages with symbols in
		--include-source @bool # Include PDBs and source files. Source
		--serviceable|-s @bool # Set the serviceable flag in the package.
		--nologo @bool # Do not display the startup banner or the
		--interactive @bool # Allows the command to stop and wait for
		--no-restore @bool # Do not restore the project before
		-verbosity|-v # Set the MSBuild verbosity level. Allowed
		--version-suffix # Set the value of the $(VersionSuffix)
		--version # The version of the package to create
		--configuration|-c # The configuration to use for building
		--disable-build-servers @bool # Force the command to ignore any
		--use-current-runtime @bool # Use current runtime as the target
		--runtime|-r # The target runtime to build for.
	package # Search for, add, remove, or list PackageReferences for a .NET project.
		list
			--verbosity|-v # Set the MSBuild verbosity level.
			--outdated @bool # Lists packages that have newer
			--deprecated @bool # Lists packages that have been
			--vulnerable @bool # Lists packages that have known
			--include-transitive @bool # Lists transitive and top-level
			--include-prerelease @bool # Consider packages with prerelease
			--highest-patch @bool # Consider only the packages with a
			--highest-minor @bool # Consider only the packages with a
			--configfile # The path to the NuGet config file to
			--source|-s # The NuGet sources to use when searching
			--interactive @bool # Allows the command to stop and wait for
			--format # Specifies the output format type for
			--output-version # Specifies the version of
			--no-restore @bool # Do not restore before running the
			--project # The project file to operate on. If a
	publish # Publish a .NET project for deployment.
		--use-current-runtime @bool # Use current runtime as the target
		--output|-o # The output directory to place the
		--artifacts-path # The artifacts path. All output from the
		--manifest # The path to a target manifest file that
		--no-build @bool # Do not build the project before
		--self-contained @bool # Publish the .NET runtime with your
		--no-self-contained @bool # Publish your application as a framework
		--nologo @bool # Do not display the startup banner or the
		--framework|-f # The target framework to publish for. The
		--runtime|-r # The target runtime to publish for. This
		--configuration|-c # The configuration to publish for. The
		--version-suffix # Set the value of the $(VersionSuffix)
		--interactive @bool # Allows the command to stop and wait for
		--no-restore @bool # Do not restore the project before
		-verbosity|-v # Set the MSBuild verbosity level. Allowed
		--arch|-a # The target architecture.
		--os # The target operating system.
		--disable-build-servers @bool # Force the command to ignore any
	reference # Add, remove, or list ProjectReferences for a .NET project.
		--project # The project file to operate on. If a file is not
		list
			--project @bool # The project file to operate on. If a file is not specified,
	restore # Restore dependencies specified in a .NET project.
		--disable-build-servers @bool # Force the command to ignore any
		--source|-s # The NuGet package source to use for the
		--packages # The directory to restore packages to.
		--use-current-runtime @bool # Use current runtime as the target
		--disable-parallel @bool # Prevent restoring multiple projects in
		--configfile # The NuGet configuration file to use.
		--no-http-cache @bool # Disable Http Caching for packages.
		--ignore-failed-sources @bool # Treat package source failures as
		--force|-f @bool # Force all dependencies to be resolved
		--runtime|-r # The target runtime to restore packages
		--no-dependencies @bool # Do not restore project-to-project
		--verbosity|-v # Set the MSBuild verbosity level. Allowed
		--interactive @bool # Allows the command to stop and wait for
		--artifacts-path # The artifacts path. All output from the
		--use-lock-file @bool # Enables project lock file to be generated
		--locked-mode @bool # Don't allow updating project lock file.
		--lock-file-path # Output location where project lock file
		--force-evaluate @bool # Forces restore to reevaluate all
		--arch|-a # The target architecture.
		--os # The target operating system.
	run # Build and run a .NET project output.
		--configuration|-c # The configuration to run for. The
		--framework|-f # The target framework to run for. The
		--runtime|-r # The target runtime to run for.
		--project # The path to the project file to run
		--file # The path to the file-based app to run
		--launch-profile # The name of the launch profile (if
		--no-launch-profile @bool # Do not attempt to use
		--no-build @bool # Do not build the project before
		--interactive @bool # Allows the command to stop and wait
		--no-restore @bool # Do not restore the project before
		--no-cache @bool # Skip up to date checks and always
		--self-contained @bool # Publish the .NET runtime with your
		--no-self-contained @bool # Publish your application as a
		-verbosity|-v # Set the MSBuild verbosity level.
		--arch|-a # The target architecture.
		--os # The target operating system.
		--disable-build-servers @bool # Force the command to ignore any
		--artifacts-path # The artifacts path. All output from
		--environment|-e # Sets the value of an environment
	sdk # Manage .NET SDK installation.
		check
	solution # Modify Visual Studio solution files.
		list
			--solution-folders @bool # Display solution folder paths. [default: False]
	store # Store the specified assemblies in the runtime package store.
		--manifest|-m # The XML file that contains the list of
		--framework-version @bool # The Microsoft.NETCore.App package version
		--output|-o # The output directory to store the given
		--working-dir|-w # The working directory used by the command
		--skip-optimization @bool # Skip the optimization phase. [default:
		--skip-symbols @bool # Skip creating symbol files which can be
		--framework|-f # The target framework to store packages
		--runtime|-r # The target runtime to store packages for.
		-verbosity|-v # Set the MSBuild verbosity level. Allowed
		--use-current-runtime @bool # Use current runtime as the target
		--disable-build-servers @bool # Force the command to ignore any
	test # Run unit tests using the test runner specified in a .NET project.
		--settings|-s # The settings file to use when running
		--list-tests|-t @bool # List the discovered tests instead of
		--environment|-e # Sets the value of an environment
		--filter # Run tests that match the given
		--filter @bool # "FullyQualifiedName=Namespace.ClassName.M
		--test-adapter-path # The path to the custom adapters to use
		--logger|-l # The logger to use for test results.
		--output|-o # The output directory to place built
		--artifacts-path # The artifacts path. All output from the
		--diag|-d # Enable verbose logging to the specified
		--no-build @bool # Do not build the project before testing.
		--results-directory # The directory where the test results
		--collect # The friendly name of the data collector
		--blame @bool # Runs the tests in blame mode. This
		--blame-crash @bool # Runs the tests in blame mode and
		--blame-crash-dump-type # The type of crash dump to be collected.
		--blame-crash-collect-always @bool # Enables collecting crash dump on
		--blame-hang @bool # Run the tests in blame mode and enables
		--blame-hang-dump-type # The type of crash dump to be collected.
		--blame-hang. @bool
		--blame-hang-timeout # Per-test timeout, after which hang dump
		--nologo @bool # Run test(s), without displaying
		--configuration|-c # The configuration to use for running
		--framework|-f # The target framework to run tests for.
		--runtime|-r # The target runtime to test for.
		--no-restore @bool # Do not restore the project before
		--interactive @bool # Allows the command to stop and wait for
		-verbosity|-v # Set the MSBuild verbosity level. Allowed
		--arch|-a # The target architecture.
		--os # The target operating system.
		--disable-build-servers @bool # Force the command to ignore any
	tool # Install or manage tools that extend the .NET experience.
		restore
			--configfile # The NuGet configuration file to use.
			--add-source # Add an additional NuGet package source to use
			--tool-manifest # Path to the manifest file.
			--disable-parallel @bool # Prevent restoring multiple projects in parallel.
			--ignore-failed-sources @bool # Treat package source failures as warnings.
			--no-http-cache @bool # Do not cache packages and http requests. [default:
			--interactive @bool # Allows the command to stop and wait for user input
			--verbosity|-v # Set the MSBuild verbosity level. Allowed values are
	user-jwts # Manage JSON Web Tokens in development.
		--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
		--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
		clear
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--force @bool # Don't prompt for confirmation before deleting JWTs.
			--appsettings-file @bool # The appSettings configuration file to add the test scheme to.
		create
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--scheme @bool # The scheme name to use for the generated token. Defaults to 'Bearer'.
			--name|-n @bool # The name of the user to create the JWT for. Defaults to the current environment user.
			--audience @bool # The audiences to create the JWT for. Defaults to the URLs configured in the project's launchSettings.json.
			--issuer @bool # The issuer of the JWT. Defaults to 'dotnet-user-jwts'.
			--scope @bool # A scope claim to add to the JWT. Specify once for each scope.
			--role @bool # A role claim to add to the JWT. Specify once for each role.
			--claim @bool # Claims to add to the JWT. Specify once for each claim in the format "name=value".
			--not-before @bool # The UTC date & time the JWT should not be valid before in the format 'yyyy-MM-dd [[HH:mm[[:ss]]]]'. Defaults to the date & time the JWT is created.
			--expires-on @bool # The UTC date & time the JWT should expire in the format 'yyyy-MM-dd [[[[HH:mm]]:ss]]'. Defaults to 3 months after the --not-before date. Do not use this option in conjunction with the --valid-for option.
			--valid-for @bool # The period the JWT should expire after. Specify using a number followed by a duration type like 'd' for days, 'h' for hours, 'm' for minutes, and 's' for seconds, e.g. '365d'. Do not use this option in conjunction with the --expires-on option.
			--appsettings-file @bool # The appSettings configuration file to add the test scheme to.
		key
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--scheme @bool # The scheme name associated with the signing key to be reset or displayed. Defaults to 'Bearer'.
			--issuer @bool # The issuer associated with the signing key to be reset or displayed. Defaults to 'dotnet-user-jwts'.
			--reset @bool # Reset the signing key. This will invalidate all previously issued JWTs for this project.
			--force @bool # Don't prompt for confirmation before resetting the signing key.
		list
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--show-tokens @bool # Indicates whether JWT base64 strings should be shown.
		print
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--show-all @bool # Whether to show all details associated with the JWT.
		remove
			--project|-p @bool # The path of the project to operate on. Defaults to the project in the current directory.
			--output|-o @bool # The format to use for displaying output from the command. Can be one of 'default', 'token', or 'json'.
			--appsettings-file @bool # The appSettings configuration file to add the test scheme to.
	user-secrets # Manage development user secrets.
		--version @bool # Show version information
		--verbose|-v @bool # Show verbose output
		--project|-p # Path to project. Defaults to searching the current directory.
		--file|-f # Path to file-based app.
		--configuration|-c # The project configuration to use. Defaults to 'Debug'.
		--id @bool # The user secret ID to use.
		clear
			--verbose|-v @bool # Show verbose output
			--project|-p # Path to project. Defaults to searching the current directory.
			--file|-f # Path to file-based app.
			--configuration|-c # The project configuration to use. Defaults to 'Debug'.
			--id @bool # The user secret ID to use.
		init
			--verbose|-v @bool # Show verbose output
			--project|-p # Path to project. Defaults to searching the current directory.
			--file|-f # Path to file-based app.
			--configuration|-c # The project configuration to use. Defaults to 'Debug'.
			--id @bool # The user secret ID to use.
		list
			--json @bool # Use json output. JSON is wrapped by '//BEGIN' and '//END'
			--verbose|-v @bool # Show verbose output
			--project|-p # Path to project. Defaults to searching the current directory.
			--file|-f # Path to file-based app.
			--configuration|-c # The project configuration to use. Defaults to 'Debug'.
			--id @bool # The user secret ID to use.
		remove
			--verbose|-v @bool # Show verbose output
			--project|-p # Path to project. Defaults to searching the current directory.
			--file|-f # Path to file-based app.
			--configuration|-c # The project configuration to use. Defaults to 'Debug'.
			--id @bool # The user secret ID to use.
		set
			--verbose|-v @bool # Show verbose output
			--project|-p # Path to project. Defaults to searching the current directory.
			--file|-f # Path to file-based app.
			--configuration|-c # The project configuration to use. Defaults to 'Debug'.
			--id @bool # The user secret ID to use.
	vstest # Run Microsoft Test Engine (VSTest) commands.
	watch # Start a file watcher that runs a command when files change.
		--quiet|-q @bool # Suppresses all output except warnings and errors
		--verbose @bool # Show verbose output
		--list @bool # Lists all discovered files without starting the watcher.
		--no-hot-reload @bool # Suppress hot reload for supported apps.
		--non-interactive @bool # Runs dotnet-watch in non-interactive mode. This option is only supported when running with Hot Reload enabled.
		--configuration|-c # The configuration to run for. The default for most projects is 'Debug'.
		--framework|-f # The target framework to run for. The target framework must also be specified in the project file.
		--runtime|-r # The target runtime to run for.
		--interactive @bool # Allows the command to stop and wait for user input or action (for example to complete authentication).
		--no-restore @bool # Do not restore the project before building.
		--self-contained @bool # Publish the .NET runtime with your application so the runtime doesn't need to be installed on the target machine.
		--no-self-contained @bool # Publish your application as a framework dependent application. A compatible .NET runtime must be installed on the target machine to run your application.
		-verbosity|-v # Set the MSBuild verbosity level. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic].
		--arch|-a # The target architecture.
		--os # The target operating system.
		--disable-build-servers @bool # Force the command to ignore any persistent build servers.
		--artifacts-path # The artifacts path. All output from the project, including build, publish, and pack output, will go in subfolders under the specified path.
		--version @bool # Show version information
	workload # Manage optional workloads.
		--info @bool # Display information about installed workloads. [default:
		--version @bool # Display the currently installed workload version. [default:
		--update-mode
		clean
			--all @bool # Causes clean to remove and uninstall all workload components
		config
			--update-mode # Controls whether updates should look
		history
		list
		repair
			--configfile # The NuGet configuration file to use.
			--source|-s # The NuGet package source to use during the restore.
			--verbosity|-v # Set the MSBuild verbosity level. Allowed values are
			--disable-parallel @bool # Prevent restoring multiple projects in parallel.
			--ignore-failed-sources @bool # Treat package source failures as warnings. [default:
			--no-http-cache @bool # Do not cache packages and http requests. [default:
			--interactive @bool # Allows the command to stop and wait for user input
		update
			--configfile # The NuGet configuration file to use.
			--source|-s # The NuGet package source to use during the
			--include-previews @bool # Allow prerelease workload manifests.
			--temp-dir # Specify a temporary directory for this
			--from-previous-sdk @bool # Include workloads installed with earlier
			--advertising-manifests-only @bool # Only update advertising manifests.
			--version # A workload version to display or one or
			--disable-parallel @bool # Prevent restoring multiple projects in
			--ignore-failed-sources @bool # Treat package source failures as warnings.
			--no-http-cache @bool # Do not cache packages and http requests.
			--interactive @bool # Allows the command to stop and wait for
			--verbosity|-v # Set the MSBuild verbosity level. Allowed
			--from-history # Update workloads to a previous version
			--manifests-only # Update to the workload versions specified
