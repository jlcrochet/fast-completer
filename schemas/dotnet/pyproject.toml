[project]
name = "dotnet-schema-export"
version = "0.1.0"
description = "Export Dotnet command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-dotnet = "export_command_tree:main"
