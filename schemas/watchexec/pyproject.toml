[project]
name = "watchexec-schema-export"
version = "0.1.0"
description = "Export Watchexec command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-watchexec = "export_command_tree:main"
