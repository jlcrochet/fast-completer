[project]
name = "twilio-schema-export"
version = "0.1.0"
description = "Export Twilio CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-twilio = "export_command_tree:main"
