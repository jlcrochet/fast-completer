[project]
name = "tailscale-schema-export"
version = "0.1.0"
description = "Export Tailscale command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-tailscale = "export_command_tree:main"
