[project]
name = "helm-schema-export"
version = "0.1.0"
description = "Export Helm command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-helm = "export_command_tree:main"
