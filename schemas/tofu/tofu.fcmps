# OpenTofu CLI schema for fast-completer
# Generated from tofu 1.11.5

tofu # OpenTofu
	-chdir # Switch to a different working directory before executing the
	apply # Create or update infrastructure
		-auto-approve @bool # Skip interactive approval of plan before applying.
		-backup # Path to backup the existing state file before
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings # If OpenTofu produces any warnings, no consolidation
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-destroy @bool # Destroy OpenTofu-managed infrastructure.
		-lock # Don't hold a state lock during the operation.
		-lock-timeout # Duration to retry a state lock.
		-input # Ask for input for variables if not directly set.
		-no-color @bool # If specified, output won't contain any color.
		-concise @bool # Disables progress-related messages in the output.
		-parallelism # Limit the number of parallel resource operations.
		-state # Path to read and save state (unless state-out
		-state-out # Path to write state to that is different than
		-show-sensitive @bool # If specified, sensitive values will be displayed.
		-var # Set a variable in the OpenTofu configuration.
		-var-file # Set variables in the OpenTofu configuration from
		-json @bool # Produce output in a machine-readable JSON format,
	console # Try OpenTofu expressions at an interactive command prompt
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings @bool # If OpenTofu produces any warnings, no consolidation
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-state # Legacy option for the local backend only. See the local
		-var # Set a variable in the OpenTofu configuration. This
		-var-file # Set variables in the OpenTofu configuration from
	destroy # Destroy previously-created infrastructure
	fmt # Reformat your configuration in the standard style
		-list # Don't list files whose formatting differs
		-write # Don't write to source files
		-diff @bool # Display diffs of formatting changes
		-check @bool # Check if the input is formatted. Exit status will be 0 if all
		-no-color @bool # If specified, output won't contain any color.
		-recursive @bool # Also process files in subdirectories. By default, only the
	force-unlock # Release a stuck lock on the current workspace
		-force @bool # Don't ask for input for unlock confirmation.
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	get # Install or upgrade remote OpenTofu modules
		-update @bool # Check already-downloaded modules for available updates
		-no-color @bool # Disable text coloring in the output.
		-test-directory # Set the OpenTofu test directory, defaults to "tests". When set, the
		-json @bool # Produce output in a machine-readable JSON format,
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	graph # Generate a Graphviz graph of the steps in an operation
		-plan # Render graph using the specified plan file instead of the
		-draw-cycles @bool # Highlight any cycles in the graph with colored edges.
		-type # Type of graph to output. Can be: plan, plan-refresh-only,
		-module-depth # (deprecated) In prior versions of OpenTofu, specified the
		-var # Set a value for one of the input variables in the root
	import # Associate existing infrastructure with a OpenTofu resource
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings @bool # If OpenTofu produces any warnings, no consolidation
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-config # Path to a directory of OpenTofu configuration files
		-input # Disable interactive input prompts.
		-lock # Don't hold a state lock during the operation. This is
		-lock-timeout # Duration to retry a state lock.
		-no-color @bool # If specified, output won't contain any color.
		-var # Set a variable in the OpenTofu configuration. This
		-var-file # Set variables in the OpenTofu configuration from
		-ignore-remote-version @bool # A rare option used for the remote backend only. See
	init # Prepare your working directory for other commands
		-backend # Disable backend or cloud backend initialization
		-backend-config # Configuration to be merged with what is in the
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings @bool # If OpenTofu produces any warnings, no consolidation
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-force-copy @bool # Suppress prompts about copying state data when
		-from-module # Copy the contents of the given module into the target
		-get # Disable downloading modules for this configuration.
		-input # Disable interactive prompts. Note that some actions may
		-lock # Don't hold a state lock during backend migration.
		-lock-timeout # Duration to retry a state lock.
		-no-color @bool # If specified, output won't contain any color.
		-plugin-dir @bool # Directory containing plugin binaries. This overrides all
		-reconfigure @bool # Reconfigure a backend, ignoring any saved
		-migrate-state @bool # Reconfigure a backend, and attempt to migrate any
		-upgrade @bool # Install the latest module and provider versions
		-lockfile # Set a dependency lockfile mode.
		-ignore-remote-version @bool # A rare option used for cloud backend and the remote backend
		-test-directory # Set the OpenTofu test directory, defaults to "tests". When set, the
		-json @bool # Produce output in a machine-readable JSON format,
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	login # Obtain and save credentials for a remote host
	logout # Remove locally-stored credentials for a remote host
	metadata # Metadata related commands
		functions
	output # Show output values from your root module
		-state # Path to the state file to read. Defaults to
		-no-color @bool # If specified, output won't contain any color.
		-json @bool # If specified, machine readable output will be
		-raw @bool # For value types that can be automatically
		-show-sensitive @bool # If specified, sensitive values will be displayed.
		-var # Set a value for one of the input variables in the root
	plan # Show changes required by the current configuration
		-destroy @bool # Select the "destroy" planning mode, which creates a
		-refresh-only @bool # Select the "refresh only" planning mode, which checks
		-refresh # Skip checking for external changes to remote objects
		-replace # Force replacement of a particular resource instance
		-target # Limit the planning operation to only the given
		-target-file # Similar to -target, but specifies zero or more
		-exclude # Limit the planning operation to not operate on the
		-exclude-file # Similar to -exclude, but specifies zero or more
		-var # Set a value for one of the input variables in the
		-var-file # Load variable values from the given file, in addition
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings # If OpenTofu produces any warnings, do not
		-consolidate-errors @bool # If OpenTofu produces any errors, attempt to
		-detailed-exitcode @bool # Return detailed exit codes when the command
		-generate-config-out # (Experimental) If import blocks are present in
		-input # Disable prompting for required input variables
		-lock # Don't hold a state lock during the operation.
		-lock-timeout # Duration to retry a state lock, such as "5s"
		-no-color @bool # Disable virtual terminal escape sequences.
		-concise @bool # Disable progress-related messages.
		-out # Write a plan file to the given path. This can be
		-parallelism # Limit the number of concurrent operations.
		-state # A legacy option used for the local backend only.
		-show-sensitive @bool # If specified, sensitive values will not be
		-json @bool # Produce output in a machine-readable JSON
	providers # Show the providers required for this configuration
		-test-directory # Set the OpenTofu test directory, defaults to "tests". When set, the
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
		lock
			-fs-mirror # Consult the given filesystem mirror directory instead
			-net-mirror # Consult the given network mirror (given as a base URL)
			-platform # Choose a target platform to request package checksums
			-var # Set a value for one of the input variables in the root
		mirror
			-platform # Choose which target platform to build a mirror for.
			-var # Set a value for one of the input variables in the root
		schema
			-var # Set a value for one of the input variables in the root
	refresh # Update the state to match remote systems
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-warnings @bool # If OpenTofu produces any warnings, no consolidation
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-exclude # Resource to exclude. Operation will be limited to all
		-input # Ask for input for variables if not directly set.
		-lock # Don't hold a state lock during the operation. This is
		-lock-timeout # Duration to retry a state lock.
		-no-color @bool # If specified, output won't contain any color.
		-concise @bool # Disables progress-related messages in the output.
		-parallelism # Limit the number of concurrent operations. Defaults to 10.
		-target # Resource to target. Operation will be limited to this
		-var # Set a variable in the OpenTofu configuration. This
		-var-file # Set variables in the OpenTofu configuration from
		-json @bool # Produce output in a machine-readable JSON format,
	show # Show the current state or a saved plan
		-state @bool # The latest state snapshot, if any.
		-plan # The plan from a saved plan file.
		-config @bool # Show the current configuration (requires -json).
		-no-color @bool # Disable terminal escape sequences.
		-json @bool # Show the information in a machine-readable form.
		-show-sensitive @bool # If specified, sensitive values will be displayed.
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	state # Advanced state management
		list
			-state # Path to a OpenTofu state file to use to look
			-id # Filters the results to include only instances whose
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		mv
			-dry-run @bool # If set, prints out what would've been moved but doesn't
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-ignore-remote-version @bool # A rare option used for the remote backend only. See
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		pull
			-var # Set a value for one of the input variables in the root
		push
			-force @bool # Write the state even if lineages don't match or the
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		replace-provider
			-auto-approve @bool # Skip interactive approval.
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-ignore-remote-version @bool # A rare option used for the remote backend only. See
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		rm
			-dry-run @bool # If set, prints out what would've been removed but
			-backup # Path where OpenTofu should write the backup
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-state # Path to the state file to update. Defaults to the
			-ignore-remote-version @bool # Continue even if remote and local OpenTofu versions
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		show
			-state # Path to a OpenTofu state file to use to look
			-show-sensitive @bool # If specified, sensitive values will be displayed.
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
	taint # Mark a resource instance as not fully functional
		-allow-missing @bool # If specified, the command will succeed (exit code 0)
		-lock # Don't hold a state lock during the operation. This is
		-lock-timeout # Duration to retry a state lock.
		-ignore-remote-version @bool # A rare option used for the remote backend only. See
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	test # Execute integration tests for OpenTofu modules
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-filter # If specified, OpenTofu will only execute the test files
		-json @bool # If specified, machine readable output will be printed in
		-no-color @bool # If specified, output won't contain any color.
		-test-directory # Set the OpenTofu test directory, defaults to "tests". When set, the
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
		-verbose @bool # Print the plan or state for each test run block as it
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	untaint # Remove the 'tainted' state from a resource instance
		-allow-missing @bool # If specified, the command will succeed (exit code 0)
		-lock # Don't hold a state lock during the operation. This is
		-lock-timeout # Duration to retry a state lock.
		-ignore-remote-version @bool # A rare option used for the remote backend only. See
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	validate # Check whether the configuration is valid
		-compact-warnings @bool # If OpenTofu produces any warnings that are not
		-consolidate-errors @bool # If OpenTofu produces any errors, no consolidation
		-json @bool # Produce output in a machine-readable JSON format,
		-no-color @bool # If specified, output won't contain any color.
		-no-tests @bool # If specified, OpenTofu will not validate test files.
		-test-directory # Set the OpenTofu test directory, defaults to "tests". When set, the
		-var # Set a value for one of the input variables in the root
		-var-file # Load variable values from the given file, in addition
	version # Show the current OpenTofu version
		-json @bool # Output the version information as a JSON object.
	workspace # Workspace management
		delete
			-force @bool # Remove a workspace even if it is managing resources.
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-var # Set a value for one of the input variables in the root
		list
			-var # Set a value for one of the input variables in the root
		new
			-lock # Don't hold a state lock during the operation. This is
			-lock-timeout # Duration to retry a state lock.
			-state # Copy an existing state file into the new workspace.
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		select
			-or-create # Create the OpenTofu workspace if it doesn't exist.
			-var # Set a value for one of the input variables in the root
			-var-file # Load variable values from the given file, in addition
		show
