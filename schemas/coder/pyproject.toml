[project]
name = "coder-schema-export"
version = "0.1.0"
description = "Export Coder command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-coder = "export_command_tree:main"
