[project]
name = "cosign-schema-export"
version = "0.1.0"
description = "Export cosign command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-cosign = "export_command_tree:main"
