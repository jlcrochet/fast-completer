[project]
name = "tokei-schema-export"
version = "0.1.0"
description = "Export Tokei command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-tokei = "export_command_tree:main"
