[project]
name = "hugo-schema-export"
version = "0.1.0"
description = "Export Hugo command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-hugo = "export_command_tree:main"
