[project]
name = "crictl-schema-export"
version = "0.1.0"
description = "Export Crictl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-crictl = "export_command_tree:main"
