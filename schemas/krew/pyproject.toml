[project]
name = "krew-schema-export"
version = "0.1.0"
description = "Export Krew command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-krew = "export_command_tree:main"
