[project]
name = "ruff-schema-export"
version = "0.1.0"
description = "Export Ruff command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-ruff = "export_command_tree:main"
