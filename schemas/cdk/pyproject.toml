[project]
name = "cdk-schema-export"
version = "0.1.0"
description = "Export AWS CDK command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-cdk = "export_command_tree:main"
