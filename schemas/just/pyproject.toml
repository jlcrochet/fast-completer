[project]
name = "just-schema-export"
version = "0.1.0"
description = "Export Just command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-just = "export_command_tree:main"
