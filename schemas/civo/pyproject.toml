[project]
name = "civo-schema-export"
version = "0.1.0"
description = "Export Civo CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-civo = "export_command_tree:main"
