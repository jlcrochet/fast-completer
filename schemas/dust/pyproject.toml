[project]
name = "dust-schema-export"
version = "0.1.0"
description = "Export Dust command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-dust = "export_command_tree:main"
