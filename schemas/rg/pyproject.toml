[project]
name = "rg-schema-export"
version = "0.1.0"
description = "Export ripgrep command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-rg = "export_command_tree:main"
