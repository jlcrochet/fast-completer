[project]
name = "skaffold-schema-export"
version = "0.1.0"
description = "Export Skaffold command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-skaffold = "export_command_tree:main"
