[project]
name = "pacman-schema-export"
version = "0.1.0"
description = "Export Pacman command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-pacman = "export_command_tree:main"
