[project]
name = "wget-schema-export"
version = "0.1.0"
description = "Export Wget command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-wget = "export_command_tree:main"
