[project]
name = "dive-schema-export"
version = "0.1.0"
description = "Export Dive command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-dive = "export_command_tree:main"
