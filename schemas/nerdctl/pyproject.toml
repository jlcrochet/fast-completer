[project]
name = "nerdctl-schema-export"
version = "0.1.0"
description = "Export nerdctl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-nerdctl = "export_command_tree:main"
