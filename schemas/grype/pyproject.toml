[project]
name = "grype-schema-export"
version = "0.1.0"
description = "Export Grype command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-grype = "export_command_tree:main"
