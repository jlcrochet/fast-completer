[project]
name = "cargo-schema-export"
version = "0.1.0"
description = "Export Cargo command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-cargo = "export_command_tree:main"
