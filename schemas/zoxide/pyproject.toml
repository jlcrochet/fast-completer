[project]
name = "zoxide-schema-export"
version = "0.1.0"
description = "Export zoxide command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-zoxide = "export_command_tree:main"
