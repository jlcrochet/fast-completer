[project]
name = "doctl-schema-export"
version = "0.1.0"
description = "Export DigitalOcean CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-doctl = "export_command_tree:main"
