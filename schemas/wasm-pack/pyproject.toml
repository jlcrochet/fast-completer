[project]
name = "wasm-pack-schema-export"
version = "0.1.0"
description = "Export Wasm-pack command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-wasm-pack = "export_command_tree:main"
