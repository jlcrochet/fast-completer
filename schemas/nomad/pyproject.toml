[project]
name = "nomad-schema-export"
version = "0.1.0"
description = "Export Nomad command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-nomad = "export_command_tree:main"
