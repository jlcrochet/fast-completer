[project]
name = "zstd-schema-export"
version = "0.1.0"
description = "Export Zstd command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-zstd = "export_command_tree:main"
