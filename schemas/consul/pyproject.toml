[project]
name = "consul-schema-export"
version = "0.1.0"
description = "Export Consul command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-consul = "export_command_tree:main"
