[project]
name = "kustomize-schema-export"
version = "0.1.0"
description = "Export Kustomize command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-kustomize = "export_command_tree:main"
