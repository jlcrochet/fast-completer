[project]
name = "kubectl-schema-export"
version = "0.1.0"
description = "Export kubectl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-kubectl = "export_command_tree:main"
