[project]
name = "rclone-schema-export"
version = "0.1.0"
description = "Export rclone command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-rclone = "export_command_tree:main"
