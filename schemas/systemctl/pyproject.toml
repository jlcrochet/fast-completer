[project]
name = "systemctl-schema-export"
version = "0.1.0"
description = "Export Systemctl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-systemctl = "export_command_tree:main"
