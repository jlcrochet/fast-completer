[project]
name = "procs-schema-export"
version = "0.1.0"
description = "Export Procs command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-procs = "export_command_tree:main"
