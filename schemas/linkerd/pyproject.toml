[project]
name = "linkerd-schema-export"
version = "0.1.0"
description = "Export Linkerd command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-linkerd = "export_command_tree:main"
