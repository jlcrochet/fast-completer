[project]
name = "kops-schema-export"
version = "0.1.0"
description = "Export Kops command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-kops = "export_command_tree:main"
