[project]
name = "turbo-schema-export"
version = "0.1.0"
description = "Export Turbo command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-turbo = "export_command_tree:main"
