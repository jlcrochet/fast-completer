[project]
name = "sf-schema-export"
version = "0.1.0"
description = "Export Salesforce CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-sf = "export_command_tree:main"
