# Kubeadm CLI schema for fast-completer
# Generated from kubeadm unknown

kubeadm # Kubernetes cluster bootstrapping
	--help @bool # help for kubeadm
	--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
	--v # number for the log level verbosity
	--vmodule # comma-separated list of pattern=N settings for file-filtered logging
	certs # Commands related to handling Kubernetes certificates
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		certificate-key # Generate certificate keys
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		check-expiration # Check certificates expiration for a Kubernetes cluster
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-missing-template-keys @bool # If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
			--cert-dir # The path where to save the certificates
			--config # Path to a kubeadm configuration file.
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--output # Output format. One of: text|json|yaml|kyaml|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file.
			--show-managed-fields @bool # If true, keep the managedFields when printing objects in JSON or YAML format.
		generate-csr # Generate keys and certificate signing requests
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--cert-dir # The path where to save the certificates
			--config # Path to a kubeadm configuration file.
			--kubeconfig-dir # The path where to save the kubeconfig file.
		renew # Renew certificates for a Kubernetes cluster
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			admin.conf # Renew the certificate embedded in the kubeconfig file for the admin to use and for kubeadm itself
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			all # Renew all available certificates
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			apiserver # Renew the certificate for serving the Kubernetes API
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			apiserver-etcd-client # Renew the certificate the apiserver uses to access etcd
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			apiserver-kubelet-client # Renew the certificate for the API server to connect to kubelet
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			controller-manager.conf # Renew the certificate embedded in the kubeconfig file for the controller manager to use
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			etcd-healthcheck-client # Renew the certificate for liveness probes to healthcheck etcd
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			etcd-peer # Renew the certificate for etcd nodes to communicate with each other
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			etcd-server # Renew the certificate for serving etcd
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			front-proxy-client # Renew the certificate for the front proxy client
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			scheduler.conf # Renew the certificate embedded in the kubeconfig file for the scheduler manager to use
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			super-admin.conf # Renew the certificate embedded in the kubeconfig file for the super-admin
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path where to save the certificates
				--config # Path to a kubeadm configuration file.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
	completion # Output shell completion code for the specified shell (bash or zsh)
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		bash
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		zsh
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
	config # Manage configuration for a kubeadm cluster persisted in a ConfigMap in the cluster
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
		images # Interact with container images used by kubeadm
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			list # Print a list of images kubeadm will use. The configuration file is used in case any images or image repositories are customized
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--allow-missing-template-keys @bool # If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
				--config # Path to a kubeadm configuration file.
				--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
				--image-repository # Choose a container registry to pull control plane images from
				--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				--output # Output format. One of: text|json|yaml|kyaml|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file.
				--show-managed-fields @bool # If true, keep the managedFields when printing objects in JSON or YAML format.
			pull # Pull images used by kubeadm
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
				--image-repository # Choose a container registry to pull control plane images from
				--kubernetes-version # Choose a specific Kubernetes version for the control plane.
		migrate # Read an older version of the kubeadm configuration API types from a file, and output the similar config object for the newer version
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-experimental-api @bool # Allow migration to experimental, unreleased APIs.
			--new-config # Path to the resulting equivalent kubeadm config file using the new API version. Optional, if not specified output will be sent to STDOUT.
			--old-config # Path to the kubeadm config file that is using an old API version and should be converted. This flag is mandatory.
		print # Print configuration
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			init-defaults # Print default init configuration, that can be used for 'kubeadm init'
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--component-configs # A comma-separated list for component config API objects to print the default values for. Available values: [KubeProxyConfiguration KubeletConfiguration]. If this flag is not set, no component configs will be printed.
			join-defaults # Print default join configuration, that can be used for 'kubeadm join'
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			reset-defaults # Print default reset configuration, that can be used for 'kubeadm reset'
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			upgrade-defaults # Print default upgrade configuration, that can be used for 'kubeadm upgrade'
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		validate # Read a file containing the kubeadm configuration API and report any validation problems
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-experimental-api @bool # Allow validation of experimental, unreleased APIs.
			--config # Path to a kubeadm configuration file.
	help # Help about any command
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		certs # Commands related to handling Kubernetes certificates
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			certificate-key # Generate certificate keys
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			check-expiration # Check certificates expiration for a Kubernetes cluster
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			generate-csr # Generate keys and certificate signing requests
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			renew # Renew certificates for a Kubernetes cluster
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				admin.conf # Renew the certificate embedded in the kubeconfig file for the admin to use and for kubeadm itself
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Renew all available certificates
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				apiserver # Renew the certificate for serving the Kubernetes API
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				apiserver-etcd-client # Renew the certificate the apiserver uses to access etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				apiserver-kubelet-client # Renew the certificate for the API server to connect to kubelet
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				controller-manager.conf # Renew the certificate embedded in the kubeconfig file for the controller manager to use
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				etcd-healthcheck-client # Renew the certificate for liveness probes to healthcheck etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				etcd-peer # Renew the certificate for etcd nodes to communicate with each other
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				etcd-server # Renew the certificate for serving etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				front-proxy-client # Renew the certificate for the front proxy client
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				scheduler.conf # Renew the certificate embedded in the kubeconfig file for the scheduler manager to use
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				super-admin.conf # Renew the certificate embedded in the kubeconfig file for the super-admin
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		completion # Output shell completion code for the specified shell (bash or zsh)
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		config # Manage configuration for a kubeadm cluster persisted in a ConfigMap in the cluster
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			images # Interact with container images used by kubeadm
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				list # Print a list of images kubeadm will use. The configuration file is used in case any images or image repositories are customized
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				pull # Pull images used by kubeadm
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			migrate # Read an older version of the kubeadm configuration API types from a file, and output the similar config object for the newer version
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			print # Print configuration
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				init-defaults # Print default init configuration, that can be used for 'kubeadm init'
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				join-defaults # Print default join configuration, that can be used for 'kubeadm join'
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				reset-defaults # Print default reset configuration, that can be used for 'kubeadm reset'
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				upgrade-defaults # Print default upgrade configuration, that can be used for 'kubeadm upgrade'
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			validate # Read a file containing the kubeadm configuration API and report any validation problems
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		help # Help about any command
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		init # Run this command in order to set up the Kubernetes control plane
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			phase # Use this command to invoke single phase of the "init" workflow
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				addon # Install required addons for passing conformance tests
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Install all the addons
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					coredns # Install the CoreDNS addon to a Kubernetes cluster
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kube-proxy # Install the kube-proxy addon to a Kubernetes cluster
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				bootstrap-token # Generates bootstrap tokens used to join a node to a cluster
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				certs # Certificate generation
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Generate all certificates
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					apiserver # Generate the certificate for serving the Kubernetes API
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					apiserver-etcd-client # Generate the certificate the apiserver uses to access etcd
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					apiserver-kubelet-client # Generate the certificate for the API server to connect to kubelet
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					ca # Generate the self-signed Kubernetes CA to provision identities for other Kubernetes components
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					etcd-ca # Generate the self-signed CA to provision identities for etcd
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					etcd-healthcheck-client # Generate the certificate for liveness probes to healthcheck etcd
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					etcd-peer # Generate the certificate for etcd nodes to communicate with each other
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					etcd-server # Generate the certificate for serving etcd
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					front-proxy-ca # Generate the self-signed CA to provision identities for front proxy
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					front-proxy-client # Generate the certificate for the front proxy client
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					sa # Generate a private key for signing service account tokens along with its public key
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				control-plane # Generate all static Pod manifest files necessary to establish the control plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Generate all static Pod manifest files
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					apiserver # Generates the kube-apiserver static Pod manifest
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					controller-manager # Generates the kube-controller-manager static Pod manifest
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					scheduler # Generates the kube-scheduler static Pod manifest
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				etcd # Generate static Pod manifest file for local etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					local # Generate the static Pod manifest file for a local, single-node local etcd instance
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				kubeconfig # Generate all kubeconfig files necessary to establish the control plane and the admin kubeconfig file
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					admin # Generate a kubeconfig file for the admin to use and for kubeadm itself
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Generate all kubeconfig files
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					controller-manager # Generate a kubeconfig file for the controller manager to use
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubelet # Generate a kubeconfig file for the kubelet to use *only* for cluster bootstrapping purposes
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					scheduler # Generate a kubeconfig file for the scheduler to use
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					super-admin # Generate a kubeconfig file for the super-admin
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				kubelet-finalize # Updates settings relevant to the kubelet after TLS bootstrap
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Run all kubelet-finalize phases
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					enable-client-cert-rotation # Enable kubelet client certificate rotation
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				kubelet-start # Write kubelet settings and (re)start the kubelet
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				mark-control-plane # Mark a node as a control-plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				preflight # Run pre-flight checks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				show-join-command # Show the join command for control-plane and worker node
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				upload-certs # Upload certificates to kubeadm-certs
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				upload-config # Upload the kubeadm and kubelet configuration to a ConfigMap
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Upload all configuration to a config map
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubeadm # Upload the kubeadm ClusterConfiguration to a ConfigMap
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubelet # Upload the kubelet component config to a ConfigMap
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				wait-control-plane # Wait for the control plane to start
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		join # Run this on any machine you wish to join an existing cluster
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			phase # Use this command to invoke single phase of the "join" workflow
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				control-plane-join # Join a machine as a control plane instance
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Join a machine as a control plane instance
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					mark-control-plane # Mark a node as a control-plane
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				control-plane-prepare # Prepare the machine for serving a control plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Prepare the machine for serving a control plane
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					certs # Generate the certificates for the new control plane components
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					control-plane # Generate the manifests for the new control plane components
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					download-certs # Download certificates shared among control-plane nodes from the kubeadm-certs Secret
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubeconfig # Generate the kubeconfig for the new control plane components
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				etcd-join # Join etcd for control plane nodes
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				kubelet-start # Write kubelet settings, certificates and (re)start the kubelet
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				kubelet-wait-bootstrap # Wait for the kubelet to bootstrap itself
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				preflight # Run join pre-flight checks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				wait-control-plane # Wait for the control plane to start
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		kubeconfig # Kubeconfig file utilities
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			user # Output a kubeconfig file for an additional user
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		reset # Performs a best effort revert of changes made to this host by 'kubeadm init' or 'kubeadm join'
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			phase # Use this command to invoke single phase of the "reset" workflow
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				cleanup-node # Run cleanup node.
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				preflight # Run reset pre-flight checks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				remove-etcd-member # Remove a local etcd member.
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		token # Manage bootstrap tokens
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			create # Create bootstrap tokens on the server
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			delete # Delete bootstrap tokens on the server
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			generate # Generate and print a bootstrap token, but do not create it on the server
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			list # List bootstrap tokens on the server
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		upgrade # Upgrade your cluster smoothly to a newer version with this command
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			apply # Upgrade your Kubernetes cluster to the specified version
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				phase # Use this command to invoke single phase of the "apply" workflow
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					addon # Upgrade the default kubeadm addons
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						all # Upgrade all the addons
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						coredns # Upgrade the CoreDNS addon
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						kube-proxy # Upgrade the kube-proxy addon
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					bootstrap-token # Configures bootstrap token and cluster-info RBAC rules
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					control-plane # Upgrade the control plane
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubelet-config # Upgrade the kubelet configuration for this node
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					post-upgrade # Run post upgrade tasks
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					preflight # Run preflight checks before upgrade
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					upload-config # Upload the kubeadm and kubelet configurations to ConfigMaps
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						all # Upload all the configurations to ConfigMaps
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						kubeadm # Upload the kubeadm ClusterConfiguration to a ConfigMap
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						kubelet # Upload the kubelet configuration to a ConfigMap
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			diff # Show what differences would be applied to existing static pod manifests. See also: kubeadm upgrade apply --dry-run
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			node # Upgrade commands for a node in the cluster
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				phase # Use this command to invoke single phase of the "node" workflow
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					addon # Upgrade the default kubeadm addons
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						all # Upgrade all the addons
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						coredns # Upgrade the CoreDNS addon
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						kube-proxy # Upgrade the kube-proxy addon
							--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
							--v # number for the log level verbosity
							--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					control-plane # Upgrade the control plane instance deployed on this node, if any
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					kubelet-config # Upgrade the kubelet configuration for this node
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					post-upgrade # Run post upgrade tasks
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					preflight # Run upgrade node pre-flight checks
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			plan # Check which versions are available to upgrade to and validate whether your current cluster is upgradeable.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		version # Print the version of kubeadm
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
	init # Run this command in order to set up the Kubernetes control plane
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
		--apiserver-bind-port # Port for the API Server to bind to.
		--apiserver-cert-extra-sans # Optional extra Subject Alternative Names (SANs) to use for the API Server serving certificate. Can be both IP addresses and DNS names.
		--cert-dir # The path where to save and store the certificates.
		--certificate-key # Key used to encrypt the control-plane certificates in the kubeadm-certs Secret. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
		--config # Path to a kubeadm configuration file.
		--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
		--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
		--dry-run @bool # Don't apply any changes; just output what would be done.
		--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
		--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
		--image-repository # Choose a container registry to pull control plane images from
		--kubernetes-version # Choose a specific Kubernetes version for the control plane.
		--node-name # Specify the node name.
		--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
		--pod-network-cidr # Specify range of IP addresses for the pod network. If set, the control plane will automatically allocate CIDRs for every node.
		--service-cidr # Use alternative range of IP address for service VIPs.
		--service-dns-domain # Use alternative domain for services, e.g. "myorg.internal".
		--skip-certificate-key-print @bool # Don't print the key used to encrypt the control-plane certificates.
		--skip-phases # List of phases to be skipped
		--skip-token-print @bool # Skip printing of the default bootstrap token generated by 'kubeadm init'.
		--token # The token to use for establishing bidirectional trust between nodes and control-plane nodes. The format is [a-z0-9]{6}\.[a-z0-9]{16} - e.g. abcdef.0123456789abcdef
		--token-ttl # The duration before the token is automatically deleted (e.g. 1s, 2m, 3h). If set to '0', the token will never expire
		--upload-certs @bool # Upload control-plane certificates to the kubeadm-certs Secret.
		phase # Use this command to invoke single phase of the "init" workflow
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			addon # Install required addons for passing conformance tests
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Install all the addons
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
					--image-repository # Choose a container registry to pull control plane images from
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--pod-network-cidr # Specify range of IP addresses for the pod network. If set, the control plane will automatically allocate CIDRs for every node.
					--service-cidr # Use alternative range of IP address for service VIPs.
					--service-dns-domain # Use alternative domain for services, e.g. "myorg.internal".
				coredns # Install the CoreDNS addon to a Kubernetes cluster
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
					--image-repository # Choose a container registry to pull control plane images from
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--print-manifest @bool # Print the addon manifests to STDOUT instead of installing them
					--service-cidr # Use alternative range of IP address for service VIPs.
					--service-dns-domain # Use alternative domain for services, e.g. "myorg.internal".
				kube-proxy # Install the kube-proxy addon to a Kubernetes cluster
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--image-repository # Choose a container registry to pull control plane images from
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--pod-network-cidr # Specify range of IP addresses for the pod network. If set, the control plane will automatically allocate CIDRs for every node.
					--print-manifest @bool # Print the addon manifests to STDOUT instead of installing them
			bootstrap-token # Generates bootstrap tokens used to join a node to a cluster
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--skip-token-print @bool # Skip printing of the default bootstrap token generated by 'kubeadm init'.
			certs # Certificate generation
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Generate all certificates
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-cert-extra-sans # Optional extra Subject Alternative Names (SANs) to use for the API Server serving certificate. Can be both IP addresses and DNS names.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--service-cidr # Use alternative range of IP address for service VIPs.
					--service-dns-domain # Use alternative domain for services, e.g. "myorg.internal".
				apiserver # Generate the certificate for serving the Kubernetes API
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-cert-extra-sans # Optional extra Subject Alternative Names (SANs) to use for the API Server serving certificate. Can be both IP addresses and DNS names.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--service-cidr # Use alternative range of IP address for service VIPs.
					--service-dns-domain # Use alternative domain for services, e.g. "myorg.internal".
				apiserver-etcd-client # Generate the certificate the apiserver uses to access etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				apiserver-kubelet-client # Generate the certificate for the API server to connect to kubelet
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				ca # Generate the self-signed Kubernetes CA to provision identities for other Kubernetes components
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				etcd-ca # Generate the self-signed CA to provision identities for etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				etcd-healthcheck-client # Generate the certificate for liveness probes to healthcheck etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				etcd-peer # Generate the certificate for etcd nodes to communicate with each other
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				etcd-server # Generate the certificate for serving etcd
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				front-proxy-ca # Generate the self-signed CA to provision identities for front proxy
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				front-proxy-client # Generate the certificate for the front proxy client
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				sa # Generate a private key for signing service account tokens along with its public key
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
			control-plane # Generate all static Pod manifest files necessary to establish the control plane
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Generate all static Pod manifest files
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
					--image-repository # Choose a container registry to pull control plane images from
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					--pod-network-cidr # Specify range of IP addresses for the pod network. If set, the control plane will automatically allocate CIDRs for every node.
					--service-cidr # Use alternative range of IP address for service VIPs.
				apiserver # Generates the kube-apiserver static Pod manifest
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--feature-gates # A set of key=value pairs that describe feature gates for various features. Options are:
					--image-repository # Choose a container registry to pull control plane images from
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					--service-cidr # Use alternative range of IP address for service VIPs.
				controller-manager # Generates the kube-controller-manager static Pod manifest
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--image-repository # Choose a container registry to pull control plane images from
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					--pod-network-cidr # Specify range of IP addresses for the pod network. If set, the control plane will automatically allocate CIDRs for every node.
				scheduler # Generates the kube-scheduler static Pod manifest
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--image-repository # Choose a container registry to pull control plane images from
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			etcd # Generate static Pod manifest file for local etcd
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				local # Generate the static Pod manifest file for a local, single-node local etcd instance
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--image-repository # Choose a container registry to pull control plane images from
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			kubeconfig # Generate all kubeconfig files necessary to establish the control plane and the admin kubeconfig file
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				admin # Generate a kubeconfig file for the admin to use and for kubeadm itself
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				all # Generate all kubeconfig files
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--node-name # Specify the node name.
				controller-manager # Generate a kubeconfig file for the controller manager to use
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				kubelet # Generate a kubeconfig file for the kubelet to use *only* for cluster bootstrapping purposes
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
					--node-name # Specify the node name.
				scheduler # Generate a kubeconfig file for the scheduler to use
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
				super-admin # Generate a kubeconfig file for the super-admin
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # The IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # Port for the API Server to bind to.
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--control-plane-endpoint # Specify a stable IP address or DNS name for the control plane.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig-dir # The path where to save the kubeconfig file.
					--kubernetes-version # Choose a specific Kubernetes version for the control plane.
			kubelet-finalize # Updates settings relevant to the kubelet after TLS bootstrap
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Run all kubelet-finalize phases
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
				enable-client-cert-rotation # Enable kubelet client certificate rotation
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--cert-dir # The path where to save and store the certificates.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Don't apply any changes; just output what would be done.
			kubelet-start # Write kubelet settings and (re)start the kubelet
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--image-repository # Choose a container registry to pull control plane images from
				--node-name # Specify the node name.
				--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			mark-control-plane # Mark a node as a control-plane
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--node-name # Specify the node name.
			preflight # Run pre-flight checks
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
				--image-repository # Choose a container registry to pull control plane images from
			show-join-command # Show the join command for control-plane and worker node
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			upload-certs # Upload certificates to kubeadm-certs
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--certificate-key # Key used to encrypt the control-plane certificates in the kubeadm-certs Secret. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
				--config # Path to a kubeadm configuration file.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				--skip-certificate-key-print @bool # Don't print the key used to encrypt the control-plane certificates.
				--upload-certs @bool # Upload control-plane certificates to the kubeadm-certs Secret.
			upload-config # Upload the kubeadm and kubelet configuration to a ConfigMap
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Upload all configuration to a config map
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				kubeadm # Upload the kubeadm ClusterConfiguration to a ConfigMap
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				kubelet # Upload the kubelet component config to a ConfigMap
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			wait-control-plane # Wait for the control plane to start
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
	join # Run this on any machine you wish to join an existing cluster
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
		--apiserver-bind-port # If the node should host a new control plane instance, the port for the API Server to bind to.
		--certificate-key # Use this key to decrypt the certificate secrets uploaded by init. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
		--config # Path to a kubeadm configuration file.
		--control-plane @bool # Create a new control plane instance on this node
		--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
		--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
		--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
		--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
		--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
		--dry-run @bool # Don't apply any changes; just output what would be done.
		--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
		--node-name # Specify the node name.
		--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
		--skip-phases # List of phases to be skipped
		--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
		--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
		phase # Use this command to invoke single phase of the "join" workflow
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			control-plane-join # Join a machine as a control plane instance
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Join a machine as a control plane instance
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--node-name # Specify the node name.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				mark-control-plane # Mark a node as a control-plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--node-name # Specify the node name.
			control-plane-prepare # Prepare the machine for serving a control plane
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				all # Prepare the machine for serving a control plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # If the node should host a new control plane instance, the port for the API Server to bind to.
					--certificate-key # Use this key to decrypt the certificate secrets uploaded by init. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
					--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
					--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
					--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--node-name # Specify the node name.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
					--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
				certs # Generate the certificates for the new control plane components
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
					--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
					--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
					--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--node-name # Specify the node name.
					--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
					--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
				control-plane # Generate the manifests for the new control plane components
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
					--apiserver-bind-port # If the node should host a new control plane instance, the port for the API Server to bind to.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				download-certs # Download certificates shared among control-plane nodes from the kubeadm-certs Secret
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--certificate-key # Use this key to decrypt the certificate secrets uploaded by init. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
					--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
					--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
					--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
					--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
				kubeconfig # Generate the kubeconfig for the new control plane components
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--certificate-key # Use this key to decrypt the certificate secrets uploaded by init. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
					--config # Path to a kubeadm configuration file.
					--control-plane @bool # Create a new control plane instance on this node
					--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
					--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
					--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
					--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
					--dry-run @bool # Don't apply any changes; just output what would be done.
					--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
					--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
			etcd-join # Join etcd for control plane nodes
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
				--config # Path to a kubeadm configuration file.
				--control-plane @bool # Create a new control plane instance on this node
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--node-name # Specify the node name.
				--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			kubelet-start # Write kubelet settings, certificates and (re)start the kubelet
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
				--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
				--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
				--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--node-name # Specify the node name.
				--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
				--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
			kubelet-wait-bootstrap # Wait for the kubelet to bootstrap itself
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--config # Path to a kubeadm configuration file.
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--dry-run @bool # Don't apply any changes; just output what would be done.
			preflight # Run join pre-flight checks
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--apiserver-advertise-address # If the node should host a new control plane instance, the IP address the API Server will advertise it's listening on. If not set the default network interface will be used.
				--apiserver-bind-port # If the node should host a new control plane instance, the port for the API Server to bind to.
				--certificate-key # Use this key to decrypt the certificate secrets uploaded by init. The certificate key is a hex encoded string that is an AES key of size 32 bytes.
				--config # Path to a kubeadm configuration file.
				--control-plane @bool # Create a new control plane instance on this node
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--discovery-file # For file-based discovery, a file or URL from which to load cluster information.
				--discovery-token # For token-based discovery, the token used to validate cluster information fetched from the API server.
				--discovery-token-ca-cert-hash # For token-based discovery, validate that the root CA public key matches this hash (format: "<type>:<value>").
				--discovery-token-unsafe-skip-ca-verification @bool # For token-based discovery, allow joining without --discovery-token-ca-cert-hash pinning.
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
				--node-name # Specify the node name.
				--tls-bootstrap-token # Specify the token used to temporarily authenticate with the Kubernetes Control Plane while joining the node.
				--token # Use this token for both discovery-token and tls-bootstrap-token when those values are not provided.
			wait-control-plane # Wait for the control plane to start
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
	kubeconfig # Kubeconfig file utilities
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		user # Output a kubeconfig file for an additional user
			--client-name # The name of user. It will be used as the CN if client certificates are created
	reset # Performs a best effort revert of changes made to this host by 'kubeadm init' or 'kubeadm join'
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--cert-dir # The path to the directory where the certificates are stored. If specified, clean this directory.
		--cleanup-tmp-dir @bool # Cleanup the "/etc/kubernetes/tmp" directory
		--config # Path to a kubeadm configuration file.
		--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
		--dry-run @bool # Don't apply any changes; just output what would be done.
		--force @bool # Reset the node without prompting for confirmation.
		--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
		--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
		--skip-phases # List of phases to be skipped
		phase # Use this command to invoke single phase of the "reset" workflow
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			cleanup-node # Run cleanup node.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--cert-dir # The path to the directory where the certificates are stored. If specified, clean this directory.
				--cleanup-tmp-dir @bool # Cleanup the "/etc/kubernetes/tmp" directory
				--cri-socket # Path to the CRI socket to connect. If empty kubeadm will try to auto-detect this value; use this option only if you have more than one CRI installed or if you have non-standard CRI socket.
				--dry-run @bool # Don't apply any changes; just output what would be done.
			preflight # Run reset pre-flight checks
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--force @bool # Reset the node without prompting for confirmation.
				--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
			remove-etcd-member # Remove a local etcd member.
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				--dry-run @bool # Don't apply any changes; just output what would be done.
				--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
	token # Manage bootstrap tokens
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--dry-run @bool # Whether to enable dry-run mode or not
		--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
		create # Create bootstrap tokens on the server
			--dry-run @bool # Whether to enable dry-run mode or not
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--certificate-key # When used together with '--print-join-command', print the full 'kubeadm join' flag needed to join the cluster as a control-plane. To create a new certificate key you must use 'kubeadm init phase upload-certs --upload-certs'.
			--config # Path to a kubeadm configuration file.
			--description # A human friendly description of how this token is used.
			--groups # Extra groups that this token will authenticate as when used for authentication. Must match "\\Asystem:bootstrappers:[a-z0-9:-]{0,255}[a-z0-9]\\z"
			--print-join-command @bool # Instead of printing only the token, print the full 'kubeadm join' flag needed to join the cluster using the token.
			--ttl # The duration before the token is automatically deleted (e.g. 1s, 2m, 3h). If set to '0', the token will never expire
			--usages # Describes the ways in which this token can be used. You can pass --usages multiple times or provide a comma separated list of options. Valid options: [signing,authentication]
		delete # Delete bootstrap tokens on the server
			--dry-run @bool # Whether to enable dry-run mode or not
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		generate # Generate and print a bootstrap token, but do not create it on the server
			--dry-run @bool # Whether to enable dry-run mode or not
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		list # List bootstrap tokens on the server
			--dry-run @bool # Whether to enable dry-run mode or not
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-missing-template-keys @bool # If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
			--output # Output format. One of: text|json|yaml|kyaml|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file.
			--show-managed-fields @bool # If true, keep the managedFields when printing objects in JSON or YAML format.
	upgrade # Upgrade your cluster smoothly to a newer version with this command
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		apply # Upgrade your Kubernetes cluster to the specified version
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-experimental-upgrades @bool # Show unstable versions of Kubernetes as an upgrade alternative and allow upgrading to an alpha/beta/release candidate versions of Kubernetes.
			--allow-release-candidate-upgrades @bool # Show release candidate versions of Kubernetes as an upgrade alternative and allow upgrading to a release candidate versions of Kubernetes.
			--certificate-renewal @bool # Perform the renewal of certificates used by component changed during upgrades.
			--config # Path to a kubeadm configuration file.
			--dry-run @bool # Do not change any state, just output what actions would be performed.
			--etcd-upgrade @bool # Perform the upgrade of etcd.
			--force @bool # Force upgrading although some requirements might not be met. This also implies non-interactive mode.
			--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			--print-config @bool # Specifies whether the configuration file that will be used in the upgrade should be printed or not.
			--skip-phases # List of phases to be skipped
			--yes @bool # Perform the upgrade and do not prompt for confirmation (non-interactive mode).
			phase # Use this command to invoke single phase of the "apply" workflow
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				addon # Upgrade the default kubeadm addons
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Upgrade all the addons
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
						--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					coredns # Upgrade the CoreDNS addon
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
						--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					kube-proxy # Upgrade the kube-proxy addon
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				bootstrap-token # Configures bootstrap token and cluster-info RBAC rules
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output what actions would be performed.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				control-plane # Upgrade the control plane
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--certificate-renewal @bool # Perform the renewal of certificates used by component changed during upgrades.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output what actions would be performed.
					--etcd-upgrade @bool # Perform the upgrade of etcd.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				kubelet-config # Upgrade the kubelet configuration for this node
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output what actions would be performed.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				post-upgrade # Run post upgrade tasks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output what actions would be performed.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				preflight # Run preflight checks before upgrade
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--allow-experimental-upgrades @bool # Show unstable versions of Kubernetes as an upgrade alternative and allow upgrading to an alpha/beta/release candidate versions of Kubernetes.
					--allow-release-candidate-upgrades @bool # Show release candidate versions of Kubernetes as an upgrade alternative and allow upgrading to a release candidate versions of Kubernetes.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output what actions would be performed.
					--force @bool # Force upgrading although some requirements might not be met. This also implies non-interactive mode.
					--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--yes @bool # Perform the upgrade and do not prompt for confirmation (non-interactive mode).
				upload-config # Upload the kubeadm and kubelet configurations to ConfigMaps
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Upload all the configurations to ConfigMaps
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					kubeadm # Upload the kubeadm ClusterConfiguration to a ConfigMap
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					kubelet # Upload the kubelet configuration to a ConfigMap
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output what actions would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
		diff # Show what differences would be applied to existing static pod manifests. See also: kubeadm upgrade apply --dry-run
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--config # Path to a kubeadm configuration file.
			--context-lines # How many lines of context in the diff
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
		node # Upgrade commands for a node in the cluster
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--certificate-renewal @bool # Perform the renewal of certificates used by component changed during upgrades.
			--config # Path to a kubeadm configuration file.
			--dry-run @bool # Do not change any state, just output the actions that would be performed.
			--etcd-upgrade @bool # Perform the upgrade of etcd.
			--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
			--skip-phases # List of phases to be skipped
			phase # Use this command to invoke single phase of the "node" workflow
				--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
				--v # number for the log level verbosity
				--vmodule # comma-separated list of pattern=N settings for file-filtered logging
				addon # Upgrade the default kubeadm addons
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					all # Upgrade all the addons
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output the actions that would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
						--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					coredns # Upgrade the CoreDNS addon
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output the actions that would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
						--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
					kube-proxy # Upgrade the kube-proxy addon
						--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
						--v # number for the log level verbosity
						--vmodule # comma-separated list of pattern=N settings for file-filtered logging
						--config # Path to a kubeadm configuration file.
						--dry-run @bool # Do not change any state, just output the actions that would be performed.
						--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				control-plane # Upgrade the control plane instance deployed on this node, if any
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--certificate-renewal @bool # Perform the renewal of certificates used by component changed during upgrades.
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output the actions that would be performed.
					--etcd-upgrade @bool # Perform the upgrade of etcd.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				kubelet-config # Upgrade the kubelet configuration for this node
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output the actions that would be performed.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
					--patches # Path to a directory that contains files named "target[suffix][+patchtype].extension". For example, "kube-apiserver0+merge.yaml" or just "etcd.json". "target" can be one of "kube-apiserver", "kube-controller-manager", "kube-scheduler", "etcd", "kubeletconfiguration", "corednsdeployment". "patchtype" can be one of "strategic", "merge" or "json" and they match the patch formats supported by kubectl. The default "patchtype" is "strategic". "extension" must be either "json" or "yaml". "suffix" is an optional string that can be used to determine which patches are applied first alpha-numerically.
				post-upgrade # Run post upgrade tasks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--dry-run @bool # Do not change any state, just output the actions that would be performed.
					--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
				preflight # Run upgrade node pre-flight checks
					--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
					--v # number for the log level verbosity
					--vmodule # comma-separated list of pattern=N settings for file-filtered logging
					--config # Path to a kubeadm configuration file.
					--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
		plan # Check which versions are available to upgrade to and validate whether your current cluster is upgradeable.
			--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
			--v # number for the log level verbosity
			--vmodule # comma-separated list of pattern=N settings for file-filtered logging
			--allow-experimental-upgrades @bool # Show unstable versions of Kubernetes as an upgrade alternative and allow upgrading to an alpha/beta/release candidate versions of Kubernetes.
			--allow-missing-template-keys @bool # If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
			--allow-release-candidate-upgrades @bool # Show release candidate versions of Kubernetes as an upgrade alternative and allow upgrading to a release candidate versions of Kubernetes.
			--config # Path to a kubeadm configuration file.
			--etcd-upgrade @bool # Perform the upgrade of etcd.
			--ignore-preflight-errors # A list of checks whose errors will be shown as warnings. Example: 'IsPrivilegedUser,Swap'. Value 'all' ignores errors from all checks.
			--kubeconfig # The kubeconfig file to use when talking to the cluster. If the flag is not set, a set of standard locations can be searched for an existing kubeconfig file.
			--output # Output format. One of: text|json|yaml|kyaml|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file.
			--print-config @bool # Specifies whether the configuration file that will be used in the upgrade should be printed or not.
			--show-managed-fields @bool # If true, keep the managedFields when printing objects in JSON or YAML format.
	version # Print the version of kubeadm
		--rootfs # The path to the 'real' host root filesystem. This will cause kubeadm to chroot into the provided path.
		--v # number for the log level verbosity
		--vmodule # comma-separated list of pattern=N settings for file-filtered logging
		--output # Output format; available options are 'yaml', 'json' and 'short'
