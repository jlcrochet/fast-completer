[project]
name = "golangci-lint-schema-export"
version = "0.1.0"
description = "Export golangci-lint command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-golangci-lint = "export_command_tree:main"
