[project]
name = "argocd-schema-export"
version = "0.1.0"
description = "Export Argo CD command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-argocd = "export_command_tree:main"
