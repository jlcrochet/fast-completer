[project]
name = "nats-schema-export"
version = "0.1.0"
description = "Export Nats command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-nats = "export_command_tree:main"
