[project]
name = "kind-schema-export"
version = "0.1.0"
description = "Export kind command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-kind = "export_command_tree:main"
