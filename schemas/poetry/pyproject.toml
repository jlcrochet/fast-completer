[project]
name = "poetry-schema-export"
version = "0.1.0"
description = "Export Poetry command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-poetry = "export_command_tree:main"
