[project]
name = "netlify-schema-export"
version = "0.1.0"
description = "Export Netlify CLI command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-netlify = "export_command_tree:main"
