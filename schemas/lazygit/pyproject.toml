[project]
name = "lazygit-schema-export"
version = "0.1.0"
description = "Export Lazygit command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-lazygit = "export_command_tree:main"
