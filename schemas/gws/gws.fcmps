# Google Workspace CLI schema for fast-completer
# Generated from gws 0.6.2

gws # Google Workspace CLI
	--account # Use a specific authenticated account
	--api-version # Override the API version (for example: v2, v3)
	admin # Manage users, groups, and devices
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		asps
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		chromeosdevices
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			action
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			moveDevicesToOu
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		customer
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			devices
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				chromeos
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchChangeStatus
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					commands
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
					countChromeOsDevices
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					issueCommand
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
		customers
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			chrome
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				printServers
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchCreatePrintServers
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					batchDeletePrintServers
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				printers
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchCreatePrinters
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					batchDeletePrinters
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					listPrinterModels
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		domainAliases
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		domains
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		groups
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			aliases
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		members
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			hasMember
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		mobiledevices
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			action
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		orgunits
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		privileges
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		resources
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			buildings
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			calendars
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			features
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				rename
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		roleAssignments
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		roles
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		schemas
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		tokens
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		twoStepVerification
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			turnOff
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		users
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			aliases
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				watch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			createGuest
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			makeAdmin
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			photos
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			signOut
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			undelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		verificationCodes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			generate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			invalidate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	admin-reports # Audit logs and usage reports
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		activities
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		customerUsageReports
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		entityUsageReports
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		userUsageReport
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	alertcenter # Manage Workspace security alerts
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		alerts
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchDelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			batchUndelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			feedback
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getMetadata
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			undelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		v1beta1
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			getSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	apps-script # Manage and execute Apps Script projects
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+push
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--script # Script Project ID
			--dir # Directory containing script files (defaults to current dir)
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
		processes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listScriptProcesses
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		projects
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			deployments
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getContent
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getMetrics
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateContent
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			versions
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		scripts
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			run
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	auth # Authenticate and manage credentials
		default # Set the default account
			--account # Account to set as default
		export # Print decrypted credentials to stdout
			--unmasked @bool # Include sensitive fields in clear text
		list # List all registered accounts
		login # Authenticate via OAuth2 (opens browser)
			--account # Associate credentials with a specific account
			--readonly @bool # Request read-only scopes
			--full @bool # Request all scopes including pubsub and cloud-platform
			--scopes # Comma-separated custom OAuth scopes
			--services|-s # Comma-separated services for scope picker
		logout # Clear saved credentials and token cache
			--account # Logout a specific account (default: all accounts)
		setup # Configure GCP project and OAuth client (requires gcloud)
			--project # Use a specific GCP project
		status # Show current authentication state
	calendar # Manage calendars and events
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+agenda
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--today @bool # Show today's events
			--dry-run @bool # Validate the request locally without sending it to the API
			--tomorrow @bool # Show tomorrow's events
			--format # Output format: json (default), table, yaml, csv
			--week @bool # Show this week's events
			--days # Number of days ahead to show
			--calendar # Filter to specific calendar name or ID
		+insert
			--calendar # Calendar ID (default: primary) [default: primary]
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--summary # Event summary/title
			--format # Output format: json (default), table, yaml, csv
			--start # Start time (ISO 8601, e.g., 2024-01-01T10:00:00Z)
			--end # End time (ISO 8601)
			--location # Event location
			--description # Event description/body
			--attendee # Attendee email (can be used multiple times)
		acl
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		calendarList
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		calendars
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			clear
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		colors
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		events
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			import
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			instances
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			move
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			quickAdd
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		freebusy
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			query
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		settings
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	chat # Manage Chat spaces and messages
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+send
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--space # Space name (e.g. spaces/AAAA...)
			--dry-run @bool # Validate the request locally without sending it to the API
			--text # Message text (plain text)
			--format # Output format: json (default), table, yaml, csv
		customEmojis
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		media
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			download
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			upload
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--upload # Local file path to upload as media content (multipart upload)
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		spaces
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			completeImport
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			findDirectMessage
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			members
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			messages
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				attachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				reactions
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			search
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			setup
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			spaceEvents
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		users
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			spaces
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				getSpaceReadState
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				spaceNotificationSetting
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				threads
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					getThreadReadState
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateSpaceReadState
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	classroom # Manage classes, rosters, and coursework
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		courses
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			aliases
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			announcements
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				addOnAttachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getAddOnContext
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				modifyAssignees
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			courseWork
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				addOnAttachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					studentSubmissions
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						patch
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getAddOnContext
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				modifyAssignees
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				rubrics
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				studentSubmissions
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					modifyAttachments
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					reclaim
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					return
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					turnIn
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateRubric
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			courseWorkMaterials
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				addOnAttachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getAddOnContext
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getGradingPeriodSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			posts
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				addOnAttachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					studentSubmissions
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						patch
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
				getAddOnContext
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			studentGroups
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				studentGroupMembers
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
			students
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			teachers
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			topics
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateGradingPeriodSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		invitations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			accept
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		registrations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		userProfiles
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			guardianInvitations
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			guardians
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	cloudidentity # Manage identity groups and memberships
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		customers
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			userinvitations
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				cancel
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				isInvitableUser
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				send
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		devices
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			cancelWipe
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			deviceUsers
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				approve
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				block
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				cancelWipe
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				clientStates
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				lookup
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				wipe
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			wipe
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		groups
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getSecuritySettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			lookup
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			memberships
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				checkTransitiveMembership
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getMembershipGraph
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				lookup
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				modifyMembershipRoles
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				searchDirectGroups
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				searchTransitiveGroups
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				searchTransitiveMemberships
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			search
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateSecuritySettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		inboundOidcSsoProfiles
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		inboundSamlSsoProfiles
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			idpCredentials
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				add
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		inboundSsoAssignments
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		policies
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	directory # Alias for admin
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		asps
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		chromeosdevices
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			action
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			moveDevicesToOu
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		customer
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			devices
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				chromeos
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchChangeStatus
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					commands
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
					countChromeOsDevices
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					issueCommand
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
		customers
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			chrome
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				printServers
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchCreatePrintServers
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					batchDeletePrintServers
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				printers
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					batchCreatePrinters
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					batchDeletePrinters
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					listPrinterModels
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		domainAliases
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		domains
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		groups
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			aliases
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		members
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			hasMember
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		mobiledevices
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			action
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		orgunits
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		privileges
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		resources
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			buildings
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			calendars
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			features
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				rename
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		roleAssignments
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		roles
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		schemas
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		tokens
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		twoStepVerification
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			turnOff
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		users
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			aliases
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				watch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			createGuest
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			makeAdmin
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			photos
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			signOut
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			undelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		verificationCodes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			generate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			invalidate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	docs # Read and write Google Docs
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+write
			--document # Document ID
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--text # Text to append (plain text)
			--format # Output format: json (default), table, yaml, csv
		documents
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchUpdate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	drive # Manage files, folders, and shared drives
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+upload
			--parent # Parent folder ID
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--name # Target filename (defaults to source filename)
			--format # Output format: json (default), table, yaml, csv
		about
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		accessproposals
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			resolve
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		approvals
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		apps
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		changes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			getStartPageToken
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		comments
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		drives
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			hide
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			unhide
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		files
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			copy
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--upload # Local file path to upload as media content (multipart upload)
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			download
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			emptyTrash
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			export
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			generateIds
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listLabels
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			modifyLabels
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--upload # Local file path to upload as media content (multipart upload)
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		operations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		permissions
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		replies
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		revisions
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		teamdrives
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	events # Subscribe to Google Workspace events
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+renew
			--name # Subscription name to reactivate (e.g., subscriptions/SUB_ID)
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--all @bool # Renew all subscriptions expiring within --within window
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			--within # Time window for --all (e.g., 1h, 30m, 2d) [default: 1h]
		+subscribe
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--target # Workspace resource URI (e.g., //chat.googleapis.com/spaces/SPACE_ID)
			--dry-run @bool # Validate the request locally without sending it to the API
			--event-types # Comma-separated CloudEvents types to subscribe to
			--format # Output format: json (default), table, yaml, csv
			--project # GCP project ID for Pub/Sub resources
			--subscription # Existing Pub/Sub subscription name (skip setup)
			--max-messages # Max messages per pull batch (default: 10) [default: 10]
			--poll-interval # Seconds between pulls (default: 5) [default: 5]
			--once @bool # Pull once and exit
			--cleanup @bool # Delete created Pub/Sub resources on exit
			--no-ack @bool # Don't auto-acknowledge messages
			--output-dir # Write each event to a separate JSON file in this directory
		message
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stream
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		operations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		subscriptions
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			reactivate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		tasks
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			cancel
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			pushNotificationConfigs
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			subscribe
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	forms # Read and write Google Forms
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		forms
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchUpdate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			responses
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			setPublishSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watches
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				renew
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	generate-skills # Generate SKILL.md files from API metadata
		--output-dir # Output directory for generated skills
		--filter # Substring filter for selected skills
	gmail # Send, read, and manage email
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+send
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--to # Recipient email address
			--dry-run @bool # Validate the request locally without sending it to the API
			--subject # Email subject
			--body # Email body (plain text)
			--format # Output format: json (default), table, yaml, csv
		+triage
			--max # Maximum messages to show (default: 20) [default: 20]
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--query # Gmail search query (default: is:unread)
			--format # Output format: json (default), table, yaml, csv
			--labels @bool # Include label names in output
		+watch
			--project # GCP project ID for Pub/Sub resources
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--subscription # Existing Pub/Sub subscription name (skip setup)
			--format # Output format: json (default), table, yaml, csv
			--topic # Existing Pub/Sub topic with Gmail push permission already granted
			--label-ids # Comma-separated Gmail label IDs to filter (e.g., INBOX,UNREAD)
			--max-messages # Max messages per pull batch [default: 10]
			--poll-interval # Seconds between pulls [default: 5]
			--msg-format (full|metadata|minimal|raw) # Gmail message format: full, metadata, minimal, raw [default: full] [possible values: full, metadata, minimal, raw]
			--once @bool # Pull once and exit
			--cleanup @bool # Delete created Pub/Sub resources on exit
			--output-dir # Write each message to a separate JSON file in this directory
		users
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			drafts
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				send
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			getProfile
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			history
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			labels
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				patch
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			messages
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				attachments
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchDelete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchModify
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				import
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				insert
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				modify
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				send
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--upload # Local file path to upload as media content (multipart upload)
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				trash
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				untrash
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			settings
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				cse
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					identities
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						create
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						delete
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						list
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						patch
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
					keypairs
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						create
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						disable
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						enable
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						list
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						obliterate
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
				delegates
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				filters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				forwardingAddresses
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				getAutoForwarding
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getImap
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getLanguage
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getPop
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getVacation
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				sendAs
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					patch
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					smimeInfo
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--format # Output format: json (default), table, yaml, csv
						delete
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						get
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						insert
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--json # JSON string for the request body
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						list
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
						setDefault
							--params # JSON string for URL/Query parameters
							--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
							--dry-run @bool # Validate the request locally without sending it to the API
							--output|-o # Output file path for binary responses
							--format # Output format: json (default), table, yaml, csv
							--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
							--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
							--page-delay # Delay in milliseconds between page fetches (default: 100)
					update
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					verify
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateAutoForwarding
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateImap
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateLanguage
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				updatePop
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				updateVacation
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			threads
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				modify
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				trash
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				untrash
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	groupssettings # Manage Google Groups settings
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		groups
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	keep # Manage Google Keep notes
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		media
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			download
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		notes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			permissions
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				batchCreate
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchDelete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	licensing # Manage product licenses
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		licenseAssignments
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listForProduct
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listForProductAndSku
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	mcp # Start the MCP server over stdio
		--services|-s # Comma separated list of services to expose (e.g., drive,gmail,all) [default: ""]
		--workflows|-w @bool # Expose workflows as tools
		--helpers|-e @bool # Expose service-specific helpers as tools
		--tool-mode (compact|full) # Tool granularity: 'compact' (1 tool/service + discover) or 'full' (1 tool/method) [default: full] [possible values: compact, full]
	meet # Manage Google Meet conferences
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		conferenceRecords
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			participants
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				participantSessions
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
			recordings
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			transcripts
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				entries
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					get
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		spaces
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			endActiveConference
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	modelarmor # Filter user-generated content for safety
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+create-template
			--project # GCP project ID
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--location # GCP location (e.g. us-central1)
			--format # Output format: json (default), table, yaml, csv
			--template-id # Template ID to create
			--preset (jailbreak) # Use a preset template: jailbreak [possible values: jailbreak]
			--json # JSON body for the template configuration (overrides --preset)
		+sanitize-prompt
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--template # Full template resource name (projects/PROJECT/locations/LOCATION/templates/TEMPLATE)
			--dry-run @bool # Validate the request locally without sending it to the API
			--text # Text content to sanitize
			--format # Output format: json (default), table, yaml, csv
			--json # Full JSON request body (overrides --text)
		+sanitize-response
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--template # Full template resource name (projects/PROJECT/locations/LOCATION/templates/TEMPLATE)
			--dry-run @bool # Validate the request locally without sending it to the API
			--text # Text content to sanitize
			--format # Output format: json (default), table, yaml, csv
			--json # Full JSON request body (overrides --text)
	people # Manage contacts and profiles
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		contactGroups
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchGet
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			members
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				modify
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		otherContacts
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			copyOtherContactToMyContactsGroup
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			search
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		people
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchCreateContacts
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			batchDeleteContacts
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			batchUpdateContacts
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			connections
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			createContact
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			deleteContact
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			deleteContactPhoto
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getBatchGet
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listDirectoryPeople
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			searchContacts
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			searchDirectoryPeople
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateContact
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateContactPhoto
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	reports # Alias for admin-reports
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		activities
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			watch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		channels
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			stop
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		customerUsageReports
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		entityUsageReports
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		userUsageReport
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	reseller # Manage Workspace subscriptions
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		customers
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		resellernotify
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			getwatchdetails
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			register
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			unregister
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		subscriptions
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			activate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			changePlan
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			changeRenewalSettings
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			changeSeats
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			startPaidService
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			suspend
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	schema # Inspect request/response schema for a method
		--resolve-refs @bool # Inline $ref schemas recursively
	script # Alias for apps-script
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+push
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--script # Script Project ID
			--dir # Directory containing script files (defaults to current dir)
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
		processes
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			listScriptProcesses
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		projects
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			deployments
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getContent
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getMetrics
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			updateContent
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			versions
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
		scripts
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			run
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	sheets # Read and write spreadsheets
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+append
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--spreadsheet # Spreadsheet ID
			--dry-run @bool # Validate the request locally without sending it to the API
			--values # Comma-separated values (simple strings)
			--format # Output format: json (default), table, yaml, csv
			--json-values # JSON array of rows, e.g. '[["a","b"],["c","d"]]'
		+read
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--spreadsheet # Spreadsheet ID
			--dry-run @bool # Validate the request locally without sending it to the API
			--range # Range to read (e.g. 'Sheet1!A1:B2')
			--format # Output format: json (default), table, yaml, csv
		spreadsheets
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchUpdate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			developerMetadata
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				search
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			getByDataFilter
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			sheets
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				copyTo
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			values
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				append
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchClear
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchClearByDataFilter
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchGet
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchGetByDataFilter
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchUpdate
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				batchUpdateByDataFilter
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				clear
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	slides # Read and write presentations
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		presentations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			batchUpdate
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			pages
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				getThumbnail
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
	tasks # Manage task lists and tasks
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		tasklists
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		tasks
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			clear
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			insert
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			move
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			patch
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	vault # Manage eDiscovery holds and exports
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		matters
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			addPermissions
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			close
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			count
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			create
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			exports
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			holds
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				accounts
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--format # Output format: json (default), table, yaml, csv
					create
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--json # JSON string for the request body
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					delete
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
					list
						--params # JSON string for URL/Query parameters
						--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
						--dry-run @bool # Validate the request locally without sending it to the API
						--output|-o # Output file path for binary responses
						--format # Output format: json (default), table, yaml, csv
						--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
						--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
						--page-delay # Delay in milliseconds between page fetches (default: 100)
				addHeldAccounts
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				removeHeldAccounts
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				update
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			removePermissions
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			reopen
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			savedQueries
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--format # Output format: json (default), table, yaml, csv
				create
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--json # JSON string for the request body
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				delete
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				get
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
				list
					--params # JSON string for URL/Query parameters
					--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
					--dry-run @bool # Validate the request locally without sending it to the API
					--output|-o # Output file path for binary responses
					--format # Output format: json (default), table, yaml, csv
					--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
					--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
					--page-delay # Delay in milliseconds between page fetches (default: 100)
			undelete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			update
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
		operations
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
			cancel
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--json # JSON string for the request body
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			delete
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			get
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
			list
				--params # JSON string for URL/Query parameters
				--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
				--dry-run @bool # Validate the request locally without sending it to the API
				--output|-o # Output file path for binary responses
				--format # Output format: json (default), table, yaml, csv
				--page-all @bool # Auto-paginate through all results, outputting one JSON line per page (NDJSON)
				--page-limit # Maximum number of pages to fetch when using --page-all (default: 10)
				--page-delay # Delay in milliseconds between page fetches (default: 100)
	wf # Alias for workflow
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+email-to-task
			--message-id # Gmail message ID to convert
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--tasklist # Task list ID (default: @default) [default: @default]
			--format # Output format: json (default), table, yaml, csv
		+file-announce
			--file-id # Drive file ID to announce
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--space # Chat space name (e.g. spaces/SPACE_ID)
			--message # Custom announcement message
			--format # Output format: json (default), table, yaml, csv
		+meeting-prep
			--calendar # Calendar ID (default: primary) [default: primary]
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
		+standup-report
			--format # Output format: json (default), table, yaml, csv
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
		+weekly-digest
			--format # Output format: json (default), table, yaml, csv
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
	workflow # Cross-service productivity workflows
		--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
		--dry-run @bool # Validate the request locally without sending it to the API
		--format # Output format: json (default), table, yaml, csv
		+email-to-task
			--message-id # Gmail message ID to convert
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--tasklist # Task list ID (default: @default) [default: @default]
			--format # Output format: json (default), table, yaml, csv
		+file-announce
			--file-id # Drive file ID to announce
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--space # Chat space name (e.g. spaces/SPACE_ID)
			--message # Custom announcement message
			--format # Output format: json (default), table, yaml, csv
		+meeting-prep
			--calendar # Calendar ID (default: primary) [default: primary]
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
			--format # Output format: json (default), table, yaml, csv
		+standup-report
			--format # Output format: json (default), table, yaml, csv
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
		+weekly-digest
			--format # Output format: json (default), table, yaml, csv
			--sanitize # Sanitize API responses through a Model Armor template. Requires cloud-platform scope. Format: projects/PROJECT/locations/LOCATION/templates/TEMPLATE. Also reads GWS_SANITIZE_TEMPLATE env var.
			--dry-run @bool # Validate the request locally without sending it to the API
