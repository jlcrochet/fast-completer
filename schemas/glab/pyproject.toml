[project]
name = "glab-schema-export"
version = "0.1.0"
description = "Export Glab command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-glab = "export_command_tree:main"
