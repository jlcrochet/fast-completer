[project]
name = "rustup-schema-export"
version = "0.1.0"
description = "Export Rustup command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-rustup = "export_command_tree:main"
