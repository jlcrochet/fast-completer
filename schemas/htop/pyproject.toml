[project]
name = "htop-schema-export"
version = "0.1.0"
description = "Export Htop command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-htop = "export_command_tree:main"
