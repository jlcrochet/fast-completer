[project]
name = "deno-schema-export"
version = "0.1.0"
description = "Export Deno command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-deno = "export_command_tree:main"
