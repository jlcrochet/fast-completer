[project]
name = "stern-schema-export"
version = "0.1.0"
description = "Export stern command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-stern = "export_command_tree:main"
