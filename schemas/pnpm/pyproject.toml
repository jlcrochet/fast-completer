[project]
name = "pnpm-schema-export"
version = "0.1.0"
description = "Export pnpm command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-pnpm = "export_command_tree:main"
