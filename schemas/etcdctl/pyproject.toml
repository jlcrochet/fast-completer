[project]
name = "etcdctl-schema-export"
version = "0.1.0"
description = "Export Etcdctl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-etcdctl = "export_command_tree:main"
