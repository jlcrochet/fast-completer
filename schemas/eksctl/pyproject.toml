[project]
name = "eksctl-schema-export"
version = "0.1.0"
description = "Export eksctl command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-eksctl = "export_command_tree:main"
