[project]
name = "velero-schema-export"
version = "0.1.0"
description = "Export Velero command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-velero = "export_command_tree:main"
