[project]
name = "earthly-schema-export"
version = "0.1.0"
description = "Export Earthly command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-earthly = "export_command_tree:main"
