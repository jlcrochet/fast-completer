[project]
name = "cilium-schema-export"
version = "0.1.0"
description = "Export Cilium command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-cilium = "export_command_tree:main"
