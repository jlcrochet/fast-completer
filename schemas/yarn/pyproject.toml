[project]
name = "yarn-schema-export"
version = "0.1.0"
description = "Export yarn command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-yarn = "export_command_tree:main"
