[project]
name = "podman-schema-export"
version = "0.1.0"
description = "Export Podman command tree for fast-completer"
requires-python = ">=3.8"

[project.scripts]
export-podman = "export_command_tree:main"
